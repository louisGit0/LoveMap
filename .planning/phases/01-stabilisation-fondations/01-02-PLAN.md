---
phase: 01-stabilisation-fondations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/haptics.ts
  - components/ui/AppText.tsx
autonomous: true
requirements: [FOND-03, FOND-04]
must_haves:
  truths:
    - "lib/haptics.ts expose une API par intention en fire-and-forget (jamais d'await), suivant la philosophie équilibrée selection/impact/notification (D-01, D-03)"
    - "AppText borne le Dynamic Type par variant et mappe vers les tokens F.xxx, sans nouvelle source de vérité typographique (D-04, D-06)"
    - "AppText est créé mais NON appliqué en masse : aucun <Text> existant n'est migré en Phase 1 (D-05)"
  artifacts:
    - path: "lib/haptics.ts"
      provides: "Wrapper centralisé expo-haptics, API select/tap/press/success/warn/error"
      exports: ["haptics"]
    - path: "components/ui/AppText.tsx"
      provides: "Primitive texte avec maxFontSizeMultiplier par variant, allowFontScaling true"
      exports: ["AppText"]
  key_links:
    - from: "lib/haptics.ts"
      to: "expo-haptics"
      via: "import * as Haptics (top-level, déjà dépendance ~15.0.8)"
      pattern: "import \\* as Haptics from 'expo-haptics'"
    - from: "components/ui/AppText.tsx"
      to: "constants/fonts.ts"
      via: "variant -> F.xxx fontFamily"
      pattern: "from '@/constants/fonts'"
---

<objective>
Créer les deux primitives JS de la refonte : le helper haptique centralisé `lib/haptics.ts` (FOND-03) et le composant `AppText` qui borne le Dynamic Type (FOND-04). Aucune application en masse : `AppText` est créé mais PAS substitué aux `<Text>` existants (D-05), et le câblage haptique des écrans est traité dans le plan 01-03.

Purpose: Poser les fondations consommées par les phases 2-5 et par le câblage haptique 01-03, sans toucher aux écrans.
Output: `lib/haptics.ts` et `components/ui/AppText.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stabilisation-fondations/01-CONTEXT.md
@.planning/phases/01-stabilisation-fondations/01-PATTERNS.md
@CLAUDE.md

<interfaces>
Tokens typographiques à mapper (constants/fonts.ts lignes 6-21, source de vérité unique — D-06) :
F.serif = 'CormorantGaramond_400Regular_Italic'
F.serifLight = 'CormorantGaramond_300Light_Italic'
F.serifMedium = 'CormorantGaramond_500Medium_Italic'
F.sans = 'InterTight_400Regular'
F.sansLight = 'InterTight_300Light'
F.sansMedium = 'InterTight_500Medium'
F.sansSemi = 'InterTight_600SemiBold'
F.mono = 'JetBrainsMono_400Regular'
F.monoMedium = 'JetBrainsMono_500Medium'

Convention lib/ (lib/notifications.ts lignes 1-3) : import natif top-level + export nommé.
Convention composant UI (components/ui/Button.tsx lignes 1-24) : interface Props + union de string literals pour variant ; PAS de React.FC.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Créer lib/haptics.ts (helper haptique fire-and-forget)</name>
  <read_first>
    - .planning/phases/01-stabilisation-fondations/01-PATTERNS.md (section lib/haptics.ts lignes 24-56 : forme prescrite, note de nommage ligne 52, fire-and-forget)
    - .planning/phases/01-stabilisation-fondations/01-CONTEXT.md (D-01, D-02, D-03 lignes 19-26 ; tableau mapping <specifics> lignes 98-107)
    - lib/notifications.ts (convention module lib/ : import natif top-level + export nommé)
    - .planning/research/ARCHITECTURE.md (section Haptics lignes 181-226)
  </read_first>
  <action>
    Créer `lib/haptics.ts`. Importer `expo-haptics` en top-level (`import * as Haptics from 'expo-haptics'` — dépendance déjà présente ~15.0.8). Exporter un unique objet const nommé `haptics` exposant l'API par intention RÉCONCILIÉE aux noms CONTEXT D-03 (et non aux noms research) :
    - `select()` → `Haptics.selectionAsync()` (changement de sélection)
    - `tap()` → `Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light)` (tap courant, navigation)
    - `press()` → `Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium)` (action importante : ouverture sheet, toggle — couvre D-02 "action importante")
    - `success()` → `Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success)`
    - `warn()` → `Haptics.notificationAsync(Haptics.NotificationFeedbackType.Warning)`
    - `error()` → `Haptics.notificationAsync(Haptics.NotificationFeedbackType.Error)`
    Chaque méthode est fire-and-forget : appeler l'API sans `await` et sans `return` de la promesse (ex. corps `{ Haptics.selectionAsync(); }`). NE PAS exposer `warning`, `drop`, ni `heavy` (noms research écartés). Pas de try/catch (l'OS coupe déjà les haptiques si désactivés — ARCHITECTURE.md). TypeScript strict, aucun `any`.
  </action>
  <verify>
    <automated>npx tsc --noEmit (compile sans erreur) ET rg "select|tap|press|success|warn|error" lib/haptics.ts confirme les 6 clés</automated>
  </verify>
  <acceptance_criteria>
    - lib/haptics.ts exporte un const nommé `haptics` avec exactement les clés : select, tap, press, success, warn, error.
    - Aucune clé `warning`, `drop`, `heavy` présente : `rg "warning|drop|heavy" lib/haptics.ts` ne retourne aucune correspondance.
    - Aucun `await` dans le fichier : `rg "await" lib/haptics.ts` ne retourne aucune correspondance.
    - `npx tsc --noEmit` passe sans erreur sur le projet.
  </acceptance_criteria>
  <done>Le helper haptique est prêt à être importé par les handlers (plan 01-03) ; aucun écran modifié ici.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Créer components/ui/AppText.tsx (Dynamic Type borné)</name>
  <read_first>
    - .planning/phases/01-stabilisation-fondations/01-PATTERNS.md (section AppText lignes 60-105 : forme prescrite, bornes D-04, note ligne 105 "AppText reste mince, couleur via style")
    - .planning/phases/01-stabilisation-fondations/01-CONTEXT.md (D-04, D-05, D-06 lignes 28-31)
    - constants/fonts.ts (objet F — tokens à mapper, source unique)
    - components/ui/Button.tsx (convention imports + interface Props + union variant ; PAS de React.FC)
    - .planning/research/ARCHITECTURE.md (section Dynamic Type lignes 253-284)
  </read_first>
  <action>
    Créer `components/ui/AppText.tsx`. Définir une union de string literals `Variant` alignée sur les usages typographiques (au minimum `body`, `title`, `eyebrow` ; étendre vers les familles F.xxx si pertinent — structure exacte à la discrétion de Claude per D-04 ligne 43). Définir un `Record<Variant, number>` de bornes `maxFontSizeMultiplier` par variant selon D-04 : corps ~2.0, titres serif (Cormorant) ~1.3, eyebrows/mono (JetBrains) ~1.2. Le composant enveloppe le `<Text>` de react-native, force `allowFontScaling` à true et applique `maxFontSizeMultiplier={MAX_SCALE[variant]}`. Chaque variant mappe vers une `fontFamily` issue de `F.xxx` (import depuis `@/constants/fonts`) — pas de fontFamily hardcodée, pas de nouvelle source de vérité (D-06). AppText reste MINCE : il ne possède pas de token couleur ; la couleur est passée par l'appelant via `style` (forward `style` et `children` via `...props`). Typer les props avec `TextProps & { variant?: Variant }` (interface ou type), valeur par défaut `variant = 'body'`. PAS de React.FC, aucun `any`. NE PAS migrer les `<Text>` existants (D-05) — créer le composant uniquement.
  </action>
  <verify>
    <automated>npx tsc --noEmit (compile sans erreur) ET rg "allowFontScaling|maxFontSizeMultiplier|from '@/constants/fonts'" components/ui/AppText.tsx confirme les 3</automated>
  </verify>
  <acceptance_criteria>
    - components/ui/AppText.tsx exporte `AppText` ; props typées via `TextProps & { variant?: Variant }` ; pas de React.FC.
    - Le composant rend toujours `allowFontScaling` true et un `maxFontSizeMultiplier` dépendant du variant (bornes D-04 : body ~2.0, title ~1.3, eyebrow/mono ~1.2).
    - Les fontFamily proviennent de `F.xxx` (import `@/constants/fonts`) ; `rg "fontFamily: '" components/ui/AppText.tsx` ne retourne aucune fontFamily littérale hardcodée.
    - Aucun `<Text>` existant ailleurs n'est modifié (D-05) ; `npx tsc --noEmit` passe.
  </acceptance_criteria>
  <done>AppText est prêt à remplacer progressivement les Text dans les écrans refondus (phases 2-5), sans application en Phase 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Aucune nouvelle frontière de confiance : ces primitives n'acceptent aucune entrée réseau ni utilisateur non fiable. `lib/haptics.ts` enveloppe une API OS locale ; `AppText` est un composant de présentation.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Information Disclosure | AppText forward de props | accept | AppText ne logge rien et ne traite pas de données sensibles ; il forward style/children uniquement |
</threat_model>

<verification>
- `npx tsc --noEmit` passe.
- `lib/haptics.ts` : 6 clés d'intention, fire-and-forget, aucun nom écarté.
- `components/ui/AppText.tsx` : bornes par variant, F.xxx, allowFontScaling true, aucune migration de Text.
</verification>

<success_criteria>
Les critères 4 (helper haptique existe) et 5 (AppText borne le Dynamic Type, prêt à remplacer les Text) de la phase sont satisfaits au niveau code. Le câblage des actions (IOS-03) suit en 01-03.
</success_criteria>

<output>
Create `.planning/phases/01-stabilisation-fondations/01-02-SUMMARY.md` when done
</output>
