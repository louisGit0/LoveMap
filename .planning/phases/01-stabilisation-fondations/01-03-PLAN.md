---
phase: 01-stabilisation-fondations
plan: 03
type: execute
wave: 2
depends_on: ['02']
files_modified:
  - app/(app)/point/new.tsx
  - app/(app)/point/[id].tsx
  - app/(app)/profile/index.tsx
  - app/(app)/friends/requests.tsx
  - components/point/PointListItem.tsx
autonomous: true
requirements: [IOS-03]
must_haves:
  truths:
    - "Sceller un point déclenche haptics.success() (IOS-03 / D-02)"
    - "Accepter un consentement déclenche haptics.success(), refuser déclenche haptics.warn() (D-02)"
    - "Supprimer un point ou un compte déclenche haptics.warn() (acte irréversible, D-02)"
    - "Aucun appel direct à expo-haptics ne subsiste hors de lib/haptics.ts (D-03)"
    - "Aucun appel haptique n'est bloquant (aucun await sur une méthode haptics) (D-03)"
  artifacts:
    - path: "app/(app)/point/new.tsx"
      provides: "handleSubmit câblé haptics.success/warn/error, import direct expo-haptics supprimé"
      contains: "haptics.success"
    - path: "app/(app)/point/[id].tsx"
      provides: "handleConsent (success/warn) + handleDelete (warn/error) câblés"
      contains: "haptics.warn"
    - path: "app/(app)/profile/index.tsx"
      provides: "handleDeleteAccount câblé haptics.warn/error"
      contains: "haptics.warn"
  key_links:
    - from: "app/(app)/point/new.tsx"
      to: "lib/haptics.ts"
      via: "import { haptics } from '@/lib/haptics'"
      pattern: "from '@/lib/haptics'"
    - from: "app/(app)/friends/requests.tsx"
      to: "lib/haptics.ts"
      via: "remplacement des appels directs expo-haptics par haptics.xxx"
      pattern: "haptics\\.(success|tap)"
    - from: "components/point/PointListItem.tsx"
      to: "lib/haptics.ts"
      via: "haptics.tap() remplace l'appel direct expo-haptics sur le press de navigation"
      pattern: "haptics\\.tap"
---

<objective>
Câbler le helper centralisé `lib/haptics.ts` (créé en 01-02) dans les handlers d'actions clés EXISTANTS, sans toucher au design des écrans (périmètre strict D-05 / IOS-03). Deux volets : (1) ajouter le feedback haptique mandaté D-02 sur sceller / consentement / suppression, et (2) éliminer tous les appels directs restants à `expo-haptics` pour qu'il ne soit plus importé que dans `lib/haptics.ts` (D-03).

Purpose: Satisfaire IOS-03 (actions clés → retour haptique approprié) tout en imposant la règle d'or D-03 « aucun appel direct à expo-haptics ailleurs ». Le câblage se fait au niveau ÉCRAN, pas dans les hooks (les hooks signalent par `return false`/`throw`, recommandation PATTERNS).

Output: 5 écrans/composants modifiés ; `expo-haptics` n'est plus importé qu'une seule fois, dans `lib/haptics.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stabilisation-fondations/01-CONTEXT.md
@.planning/phases/01-stabilisation-fondations/01-PATTERNS.md
@CLAUDE.md

<interfaces>
API consommée (source de vérité = lib/haptics.ts, produit par 01-02) :
haptics.select()  -> changement de sélection
haptics.tap()     -> tap courant / navigation (impact light)
haptics.press()   -> action importante (impact medium)
haptics.success() -> résultat positif (notification success)
haptics.warn()    -> alerte / acte irréversible (notification warning)
haptics.error()   -> erreur (notification error)
Toutes fire-and-forget : ne JAMAIS faire `await haptics.xxx()`.

Mapping D-02 (CONTEXT <specifics>) :
Sceller un point -> success · Accepter consentement -> success · Refuser consentement -> warn
Supprimer point/compte -> warn · Erreur réseau -> error (priorité moindre)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Câbler les actions mandatées D-02 (sceller, consentement, suppression)</name>
  <read_first>
    - lib/haptics.ts (source de vérité de l'API par intention — produit par 01-02)
    - .planning/phases/01-stabilisation-fondations/01-PATTERNS.md (section "Haptic Wiring Points (IOS-03)" lignes 167-236 : sites exacts, lignes, mapping ; recommandation hooks lignes 232-236)
    - .planning/phases/01-stabilisation-fondations/01-CONTEXT.md (D-02 lignes 21-25, D-03 ligne 26 ; tableau <specifics> lignes 98-107)
    - app/(app)/point/new.tsx (import direct expo-haptics ligne 17 ; handleSubmit lignes 118-168 ; branches validation lignes 121/127 ; catch lignes 152-156 ; succès ligne 166)
    - app/(app)/point/[id].tsx (handleConsent lignes 139-148 ; handleDelete lignes 99-113)
    - app/(app)/profile/index.tsx (handleDeleteAccount lignes 231-251)
    - CLAUDE.md (règle 3 : erreurs visibles via Snackbar ; règle 4 : pas d'appel haptique dans les hooks UI)
  </read_first>
  <action>
    Importer `{ haptics }` depuis `@/lib/haptics` dans les trois écrans ci-dessous et câbler aux call sites repérés par PATTERNS, en gardant les Snackbar/Alert existants intacts. Tous les appels sont fire-and-forget (jamais d'`await`).

    app/(app)/point/new.tsx — handler `handleSubmit` :
    - Supprimer l'import direct `import * as Haptics from 'expo-haptics'` (ligne 17).
    - Branche succès (ligne ~166) : remplacer l'appel `await Haptics.notificationAsync(...Success)` par `haptics.success();` SANS `await`, juste avant `router.replace('/(app)/map')`.
    - Branche validation « partenaire manquant » (ligne ~121) et « position GPS manquante » (ligne ~127) : ajouter `haptics.warn();` avant le `setSnackbar(...)`.
    - Bloc catch d'erreur de création (lignes ~152-156, là où `setSnackbar('Erreur : ' + msg)`) : ajouter `haptics.error();`.

    app/(app)/point/[id].tsx — handler `handleConsent(accept)` :
    - Branche erreur (ligne ~145, `setSnackbar('Erreur lors de la réponse.')`) : ajouter `haptics.error();`.
    - Branche succès (ligne ~146) : ajouter `accept ? haptics.success() : haptics.warn();` colocalisé avec le `setSnackbar(accept ? 'Page scellée.' : 'Taguage refusé.')`. (Note : `handleSaveAndAccept` appelle `handleConsent(true)` — le success passe automatiquement.)

    app/(app)/point/[id].tsx — handler `handleDelete` (onPress destructif de l'Alert, lignes ~99-113) :
    - Branche `if (ok)` (ligne ~108, avant `router.replace('/(app)/map')`) : ajouter `haptics.warn();` (acte irréversible, D-02).
    - Branche `else` (ligne ~109, `setSnackbar('Erreur lors de la suppression.')`) : ajouter `haptics.error();`.

    app/(app)/profile/index.tsx — handler `handleDeleteAccount` (onPress destructif de l'Alert, lignes ~231-251) :
    - Branche erreur (ligne ~244, `setSnackbar('Erreur lors de la suppression.')`) : ajouter `haptics.error();`.
    - Après `reset()` et avant `router.replace('/(auth)/login')` (lignes ~245-246) : ajouter `haptics.warn();` (suppression de compte = irréversible, D-02).

    NE PAS câbler de haptique à l'intérieur des hooks (`usePoints`, `useFriends`) — recommandation PATTERNS : les hooks signalent par `return false`/`throw`, le câblage reste au niveau écran. NE PAS modifier les `setSnackbar(...)` de plus basse priorité du profil (avatar/email/mot de passe) — hors set mandaté D-02. TypeScript strict, aucun `any`.
  </action>
  <verify>
    <automated>npx tsc --noEmit (compile sans erreur) ; rg -c "haptics\.(success|warn|error)" "app/(app)/point/new.tsx" "app/(app)/point/[id].tsx" "app/(app)/profile/index.tsx" confirme au moins 1 occurrence par fichier ; rg "expo-haptics" "app/(app)/point/new.tsx" ne retourne aucune correspondance</automated>
  </verify>
  <acceptance_criteria>
    - `app/(app)/point/new.tsx` n'importe plus `expo-haptics` ; `rg "expo-haptics" "app/(app)/point/new.tsx"` retourne 0 correspondance.
    - La branche succès de `handleSubmit` appelle `haptics.success()` sans `await` ; `rg "await haptics" "app/(app)/point/new.tsx"` retourne 0 correspondance.
    - `point/[id].tsx` : `handleConsent` câble `haptics.success()` (accept) / `haptics.warn()` (refus) et `haptics.error()` sur erreur ; `handleDelete` câble `haptics.warn()` sur succès et `haptics.error()` sur échec.
    - `profile/index.tsx` : `handleDeleteAccount` câble `haptics.warn()` sur suppression effective et `haptics.error()` sur erreur.
    - Chaque fichier touché importe `from '@/lib/haptics'` ; aucun `<Text>`/style/markup d'écran n'est redesigné (D-05).
    - `npx tsc --noEmit` passe.
  </acceptance_criteria>
  <done>Les trois actions clés mandatées (sceller, consentement, suppression) déclenchent le haptique D-02 ; point/new.tsx n'a plus d'import direct expo-haptics.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrer les deux derniers appels directs expo-haptics vers le helper</name>
  <read_first>
    - lib/haptics.ts (source de vérité de l'API — produit par 01-02)
    - .planning/phases/01-stabilisation-fondations/01-CONTEXT.md (D-03 ligne 26 : « Aucun appel direct à expo-haptics ailleurs dans le code »)
    - app/(app)/friends/requests.tsx (import direct ligne 13 ; accept -> Haptics.notificationAsync Success ligne 120 ; refus -> Haptics.impactAsync Light ligne 122 ; les deux sont actuellement `await`)
    - components/point/PointListItem.tsx (import direct ligne 4 ; onPress -> Haptics.impactAsync Light ligne 29, navigation tap déjà fire-and-forget)
  </read_first>
  <action>
    Deux sites consomment encore `expo-haptics` directement (non repérés par PATTERNS mais imposés par D-03 et par le critère « rg expo-haptics propre hors lib »). Les router vers `lib/haptics.ts` en PRÉSERVANT la sémantique existante (pas de changement de comportement perçu).

    app/(app)/friends/requests.tsx :
    - Supprimer l'import direct `import * as Haptics from 'expo-haptics'` (ligne 13) ; importer `{ haptics }` depuis `@/lib/haptics`.
    - Acceptation d'une demande (ligne ~120) : remplacer `await Haptics.notificationAsync(...Success)` par `haptics.success();` (sans `await`).
    - Refus d'une demande (ligne ~122) : remplacer `await Haptics.impactAsync(...Light)` par `haptics.tap();` (impact light = `tap`, sémantique conservée ; un refus de demande d'ami n'est pas dans le set irréversible D-02, donc on garde le tap léger existant — pas de `warn`).

    components/point/PointListItem.tsx :
    - Supprimer l'import direct `import * as Haptics from 'expo-haptics'` (ligne 4) ; importer `{ haptics }` depuis `@/lib/haptics`.
    - onPress de navigation (ligne ~29) : remplacer `Haptics.impactAsync(...Light)` par `haptics.tap();` (navigation/tap courant, D-01).

    Ne toucher à rien d'autre dans ces fichiers. TypeScript strict, aucun `any`.
  </action>
  <verify>
    <automated>npx tsc --noEmit (compile sans erreur) ; rg -l "expo-haptics" app/ components/ ne retourne AUCUN fichier (l'unique import autorisé est dans lib/haptics.ts, hors de app/ et components/)</automated>
  </verify>
  <acceptance_criteria>
    - `rg "expo-haptics" app/ components/` retourne 0 correspondance (tous les appels directs sont migrés ; seul `lib/haptics.ts` importe encore expo-haptics).
    - `rg "await haptics" app/ components/` retourne 0 correspondance (aucun appel haptique bloquant).
    - `friends/requests.tsx` : acceptation -> `haptics.success()`, refus -> `haptics.tap()` ; comportement perçu inchangé.
    - `PointListItem.tsx` : tap de navigation -> `haptics.tap()`.
    - `npx tsc --noEmit` passe.
  </acceptance_criteria>
  <done>Le code ne référence `expo-haptics` que dans `lib/haptics.ts` ; la règle D-03 est satisfaite à l'échelle du dépôt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Aucune nouvelle frontière de confiance. Ce plan ne fait que router des appels d'API OS locale (retour haptique) déjà présents vers un wrapper centralisé. Aucune entrée réseau ni utilisateur non fiable n'est introduite. Aucune installation de paquet (expo-haptics déjà présent) — pas de volet supply-chain.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Tampering | Modifications de handlers d'action (sceller/consentement/suppression) | mitigate | Les Snackbar/Alert et la logique métier existants restent intacts ; seuls des appels haptiques fire-and-forget sont ajoutés, vérifiés par `tsc` + grep d'acceptance ; aucune branche de sécurité (consentement `is_visible`, `creator_id`) n'est touchée |
| T-01-03-02 | Denial of Service | Appel haptique bloquant | mitigate | API fire-and-forget (D-03) ; acceptance interdit tout `await haptics` — aucun blocage du thread d'action |
</threat_model>

<verification>
- `npx tsc --noEmit` passe.
- `rg "expo-haptics" app/ components/` : 0 correspondance.
- `rg "await haptics" app/ components/` : 0 correspondance.
- Les actions mandatées D-02 (sceller, consentement, suppression point/compte) appellent le bon intent (`success`/`warn`/`error`).
</verification>

<success_criteria>
Le critère 4 de la phase (lib/haptics.ts déclenche le bon feedback sur les actions existantes : création, consentement, navigation, suppression) est satisfait au niveau câblage, et la règle D-03 « aucun appel direct à expo-haptics ailleurs » tient à l'échelle du dépôt. IOS-03 couvert.
</success_criteria>

<output>
Create `.planning/phases/01-stabilisation-fondations/01-03-SUMMARY.md` when done
</output>
