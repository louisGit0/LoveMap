---
phase: 01-stabilisation-fondations
plan: 04
type: execute
wave: 3
depends_on: ['01', '02', '03']
files_modified:
  - package.json
  - package-lock.json
  - app/_layout.tsx
  - app.json
  - components/dev/ReanimatedSmokeTest.tsx
autonomous: false
requirements: [FOND-01, FOND-02]
must_haves:
  truths:
    - "app.json utilise runtimeVersion: { policy: 'fingerprint' } (FOND-01 / D-10)"
    - "react-native-reanimated v4 et react-native-gesture-handler sont installés (New Architecture) (FOND-02 / D-09)"
    - "L'app est enveloppée par un unique GestureHandlerRootView racine dans app/_layout.tsx (FOND-02)"
    - "Un build EAS natif #17 est produit, démarre sans crash et les worklets Reanimated v4 tournent sur device iOS, sans gesture mort"
    - "Le build #17 n'est cuté qu'APRÈS validation STAB (01-01) et atterrissage du JS (01-02/01-03), jamais avant (séquence stricte D-07)"
  artifacts:
    - path: "app.json"
      provides: "runtimeVersion policy fingerprint avant le build #17"
      contains: "\"policy\": \"fingerprint\""
    - path: "app/_layout.tsx"
      provides: "GestureHandlerRootView racine au-dessus de PaperProvider"
      contains: "GestureHandlerRootView"
    - path: "package.json"
      provides: "dépendances natives reanimated + gesture-handler alignées SDK 54"
      contains: "react-native-reanimated"
  key_links:
    - from: "app/_layout.tsx"
      to: "react-native-gesture-handler"
      via: "import { GestureHandlerRootView } et wrapper racine flex:1"
      pattern: "GestureHandlerRootView"
    - from: "app.json"
      to: "EAS Update runtime"
      via: "policy fingerprint -> toute modif native force une nouvelle runtime"
      pattern: "fingerprint"
---

<objective>
Poser le socle natif de la refonte et produire le build TestFlight #17 qui embarque tout (corrections #15/#16 déjà validées en 01-01, primitives JS de 01-02, câblage haptique de 01-03). Conformément à D-07, c'est la DERNIÈRE vague : on ne cute #17 qu'après validation STAB (01-01) et atterrissage de tout le JS (01-02/01-03), pour ne pas mélanger vérif de bugs et nouveau natif.

Purpose: Isoler tôt le risque natif TestFlight (D-09) en installant reanimated v4 + gesture-handler et en passant `runtimeVersion` en policy fingerprint AVANT le build (D-10), sans usage fonctionnel immédiat — juste le wrapper racine et une preuve de vie des worklets.

Output: deps installées, `GestureHandlerRootView` racine, `app.json` en fingerprint, build EAS natif #17 vérifié sur device iOS, composant smoke jetable retiré.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stabilisation-fondations/01-CONTEXT.md
@.planning/phases/01-stabilisation-fondations/01-PATTERNS.md
@.planning/research/PITFALLS.md
@CLAUDE.md

<interfaces>
Cible app/_layout.tsx (PATTERNS lignes 109-141) — bloc return principal actuel à envelopper :
  <PaperProvider theme={paperTheme}>
    <StatusBar .../>
    <Stack screenOptions={{ headerShown: false }} />
  </PaperProvider>
=> envelopper par <GestureHandlerRootView style={{ flex: 1 }}> comme élément le plus externe.
Le return anticipé `!fontsLoaded` (lignes 97-103) ne reçoit PAS le wrapper.

Cible app.json (PATTERNS lignes 145-163) — clé actuelle ligne 80 :
  "runtimeVersion": "1.0.0"  =>  "runtimeVersion": { "policy": "fingerprint" }
Édition d'une seule clé ; ne pas toucher aux autres clés du bloc expo.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Installer le socle natif, wrapper racine, runtimeVersion fingerprint, smoke test jetable</name>
  <read_first>
    - .planning/research/PITFALLS.md (§Reanimated/Gestures pièges 1-3 lignes 117-136 : reanimated v4.1.x + worklets, Nouvelle Archi, NE PAS éditer babel.config.js, GestureHandlerRootView racine, dédup gesture-handler ; §Redesign-Wide piège 1 lignes 148-155 runtimeVersion fingerprint ; piège 6 lignes 185-189 secret Mapbox)
    - .planning/phases/01-stabilisation-fondations/01-PATTERNS.md (section app/_layout.tsx lignes 109-141 : point d'insertion exact + notes ; section app.json lignes 145-163 : édition mono-clé)
    - .planning/phases/01-stabilisation-fondations/01-CONTEXT.md (D-09 ligne 38, D-10 ligne 39, smoke test "Claude's Discretion" ligne 42, « Ne pas éditer babel.config.js » ligne 44)
    - app/_layout.tsx (return principal lignes 105-110 ; return anticipé fontsLoaded lignes 97-103)
    - app.json (clé runtimeVersion ligne 80)
    - babel.config.js (à NE PAS modifier — vérifier qu'il ne contient que module-resolver)
  </read_first>
  <action>
    Installer les modules natifs alignés SDK 54 via Expo : `npx expo install react-native-reanimated react-native-gesture-handler`. Si la podspec réclame le peer `react-native-worklets`, l'installer explicitement via `npx expo install react-native-worklets` (Reanimated v4 dépend de worklets, Nouvelle Architecture uniquement — PITFALLS piège 1). Vérifier l'unicité de version de gesture-handler (`npm ls react-native-gesture-handler` : une seule version résolue — PITFALLS piège 3). NE PAS éditer `babel.config.js` : le plugin worklets est géré par `babel-preset-expo` (D-09 / PITFALLS piège 1) ; le laisser tel quel (module-resolver seul).

    Envelopper l'arbre racine : dans `app/_layout.tsx`, importer `{ GestureHandlerRootView }` depuis `react-native-gesture-handler` et envelopper UNIQUEMENT le return principal (lignes ~105-110) par `<GestureHandlerRootView style={{ flex: 1 }}>` comme élément le plus externe, au-dessus de `<PaperProvider>`. Ne PAS envelopper le return anticipé `!fontsLoaded`. Ne PAS ajouter de `SafeAreaProvider` (auto-injecté par Expo Router — PATTERNS note ligne 139).

    Passer la policy de runtime : dans `app.json`, remplacer `"runtimeVersion": "1.0.0"` (ligne 80) par `"runtimeVersion": { "policy": "fingerprint" }` (D-10 / FOND-01). Édition mono-clé, ne pas toucher aux autres clés du bloc `expo`.

    Créer le smoke test jetable `components/dev/ReanimatedSmokeTest.tsx` (preuve de vie worklets, discrétion D-09) : un composant qui ne se monte QUE sous `__DEV__`, utilisant `useSharedValue` + `withTiming` (ou `withRepeat`) sur une valeur numérique (opacity ou translateX) rendue dans un `Animated.View` de react-native-reanimated, avec un libellé mono discret. Le monter temporairement à un endroit visible en dev (ex. au-dessus du `Stack` dans `app/_layout.tsx`, sous garde `__DEV__`). Aucune lecture de state JS depuis le worklet (n'animer qu'une valeur numérique — PITFALLS piège 4). Ce composant sera retiré en Task 3 après vérification on-device. TypeScript strict, aucun `any`.
  </action>
  <verify>
    <automated>npx tsc --noEmit (compile sans erreur) ; rg "react-native-reanimated" package.json ET rg "react-native-gesture-handler" package.json confirment les deps ; rg "GestureHandlerRootView" "app/_layout.tsx" confirme le wrapper ; rg "\"policy\": \"fingerprint\"" app.json confirme la policy ; rg -v '^#' babel.config.js | rg -c "reanimated/plugin|worklets/plugin" retourne 0 (aucun plugin manuel ajouté)</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` liste `react-native-reanimated` (v4.x alignée SDK 54) et `react-native-gesture-handler` ; `npm ls react-native-gesture-handler` montre une seule version résolue.
    - `app/_layout.tsx` contient `<GestureHandlerRootView` enveloppant le return principal uniquement ; l'import provient de `react-native-gesture-handler`.
    - `app.json` contient `"runtimeVersion": { "policy": "fingerprint" }` ; aucune autre clé du bloc expo modifiée.
    - `babel.config.js` est inchangé (aucun plugin reanimated/worklets ajouté) — `rg -v '^#' babel.config.js | rg "reanimated/plugin|worklets/plugin"` retourne 0 correspondance.
    - `components/dev/ReanimatedSmokeTest.tsx` existe, gardé par `__DEV__`, anime une valeur numérique via worklet (pas de state JS lu en worklet).
    - `npx tsc --noEmit` passe.
  </acceptance_criteria>
  <done>Le socle natif est installé et câblé (wrapper + fingerprint) ; le smoke test est prêt à être vérifié sur device via le build #17.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Le socle natif (reanimated v4 + gesture-handler + GestureHandlerRootView racine), app.json en policy fingerprint, et un smoke test Reanimated jetable gardé par __DEV__. Changements NATIFS : un build EAS #17 est requis (OTA inopérant chez l'utilisateur — CLAUDE.md / PITFALLS piège 6). À séquencer APRÈS la validation STAB de 01-01 (D-07).</what-built>
  <how-to-verify>
    1. Pré-build : vérifier que le secret EAS `RNMAPBOX_MAPS_DOWNLOAD_TOKEN` existe (`eas env:list`) — sinon le pod Mapbox échoue (PITFALLS piège 6). Vérifier aussi que 01-01 a bien conclu PASS sur STAB-01/02/03 (D-07).
    2. Produire le build natif #17 : `eas build --platform ios --profile production` (build natif, PAS `eas update` — OTA inopérant pour ce projet).
    3. Soumettre/installer le build #17 sur un device iOS via TestFlight.
    4. Lancer l'app : elle doit démarrer SANS crash (pas d'erreur « must be a descendant of GestureHandlerRootView », pas d'erreur Babel/worklets plugin).
    5. Observer le smoke test Reanimated (visible en build dev/__DEV__ si applicable, ou via un build dev client) : la micro-animation tourne de façon fluide -> les worklets Reanimated v4 fonctionnent sur device.
    6. Vérifier qu'aucun geste existant n'est cassé (navigation tabs, scroll des listes) — « aucun gesture mort ».
  </how-to-verify>
  <acceptance_criteria>
    - Le build #17 est produit avec succès (pod Mapbox OK grâce au secret présent).
    - L'app démarre sans crash sur device iOS ; aucune erreur GestureHandlerRootView ni plugin worklets.
    - La micro-animation Reanimated tourne (preuve worklets v4) ; aucun geste existant cassé.
    - Si échec à une étape : décrire précisément l'erreur (log de crash, message de build) avant clôture — le socle devra être corrigé avant de clore la phase.
  </acceptance_criteria>
  <resume-signal>Répondre "validé" si le build #17 démarre et que les worklets tournent sans gesture mort, sinon décrire l'erreur exacte (build ou runtime).</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Retirer le smoke test jetable après validation</name>
  <read_first>
    - .planning/phases/01-stabilisation-fondations/01-CONTEXT.md (smoke test "Claude's Discretion" ligne 42 : « vérifié une fois sur device iOS [...] puis retiré avant clôture de phase »)
    - app/_layout.tsx (le point de montage __DEV__ du smoke test ajouté en Task 1)
    - components/dev/ReanimatedSmokeTest.tsx (fichier à supprimer)
  </read_first>
  <action>
    Une fois la vérification on-device confirmée (Task 2 « validé »), retirer toute trace du smoke test : supprimer le fichier `components/dev/ReanimatedSmokeTest.tsx`, retirer son import et son montage `__DEV__` dans `app/_layout.tsx`. Conserver le `GestureHandlerRootView` racine et la dépendance reanimated (le socle reste, seule la preuve jetable part). Cette suppression est purement JS et ne change pas la nature native du build #17 déjà produit (le composant était gardé par `__DEV__`, absent du bundle de production). TypeScript strict.
  </action>
  <verify>
    <automated>npx tsc --noEmit (compile sans erreur) ; rg -l "ReanimatedSmokeTest" app/ components/ ne retourne AUCUN fichier ; rg "GestureHandlerRootView" "app/_layout.tsx" confirme que le wrapper racine est conservé</automated>
  </verify>
  <acceptance_criteria>
    - `components/dev/ReanimatedSmokeTest.tsx` n'existe plus ; `rg "ReanimatedSmokeTest" app/ components/` retourne 0 correspondance.
    - `app/_layout.tsx` conserve `<GestureHandlerRootView` (socle préservé) et n'a plus de montage __DEV__ du smoke test.
    - `npx tsc --noEmit` passe.
  </acceptance_criteria>
  <done>Le socle natif (reanimated + gesture-handler + wrapper racine + fingerprint) est en place et propre, le code jetable retiré, build #17 validé.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Frontière de livraison TestFlight (build natif vs OTA) : le changement de `runtimeVersion` en policy fingerprint protège justement cette frontière en empêchant un OTA d'atterrir sur un binaire qui ne contient pas les nouveaux natifs (PITFALLS piège 1 CRITIQUE). Frontière supply-chain : installation de modules natifs tiers (reanimated, gesture-handler, worklets).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Denial of Service | OTA sur binaire incompatible (runtimeVersion statique) | mitigate | Passage à `runtimeVersion: { policy: fingerprint }` (D-10) AVANT le build #17 : tout ajout natif force une nouvelle runtime, l'OTA ne peut plus crasher un binaire TestFlight existant |
| T-01-04-02 | Tampering | Doublon de version gesture-handler -> crash natif | mitigate | Installation via `expo install` (versions alignées SDK 54) + `npm ls react-native-gesture-handler` montrant une seule version résolue (PITFALLS piège 3) |
| T-01-04-SC | Tampering | Installs npm (reanimated, gesture-handler, worklets) | mitigate | Paquets first-party officiels (Software Mansion / Expo), documentés et nommés dans PITFALLS.md §Reanimated/Gestures (issues GitHub réelles, docs Expo) ; installés exclusivement via `npx expo install` qui épingle les versions compatibles SDK ; le checkpoint blocking-human (Task 2) vérifie le démarrage et les worklets sur device avant clôture. Aucun paquet ASSUMED/SUS/SLOP à risque dans ce set canonique. |
</threat_model>

<verification>
- `npx tsc --noEmit` passe.
- `app.json` : `"runtimeVersion": { "policy": "fingerprint" }`.
- `package.json` : reanimated v4.x + gesture-handler ; `npm ls react-native-gesture-handler` une seule version.
- `app/_layout.tsx` : `GestureHandlerRootView` racine conservé ; babel.config.js inchangé.
- Build #17 vérifié sur device iOS (démarrage OK, worklets OK, aucun gesture mort) ; smoke test retiré.
</verification>

<success_criteria>
Les critères 2 (app.json fingerprint + build natif produit) et 3 (reanimated v4 + gesture-handler installés, GestureHandlerRootView racine, aucun gesture mort) de la phase sont satisfaits. FOND-01 et FOND-02 couverts. La séquence D-07 est respectée : #17 est le dernier livrable, postérieur à la validation STAB et à tout le JS.
</success_criteria>

<output>
Create `.planning/phases/01-stabilisation-fondations/01-04-SUMMARY.md` when done
</output>
