---
phase: 01-stabilisation-fondations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-stabilisation-fondations/STAB-CHECKLIST.md
autonomous: false
requirements: [STAB-01, STAB-02, STAB-03]
must_haves:
  truths:
    - "Sur TestFlight, la photo de profil s'ouvre sans crash (STAB-01, build #15)"
    - "Sur TestFlight, les pins restent visibles à tous les niveaux de dézoom (STAB-02, build #16)"
    - "Le partenaire tagué voit le taguage en attente et peut consentir (STAB-03, build #16)"
    - "La validation STAB se fait sur les builds TestFlight ACTUELS avant toute production du build #17 (séquence stricte D-07)"
    - "STAB-01/02/03 sont validés via une checklist manuelle cochée par l'utilisateur, sans test automatisé (D-08)"
  artifacts:
    - path: ".planning/phases/01-stabilisation-fondations/STAB-CHECKLIST.md"
      provides: "Checklist de validation manuelle des 3 bugs #15/#16 + résultat coché par l'utilisateur"
      contains: "STAB-01"
  key_links:
    - from: "STAB-CHECKLIST.md items"
      to: "CONTEXT <specifics> validation steps"
      via: "transcription des 3 scénarios TestFlight (photo, dézoom, mention partenaire)"
      pattern: "STAB-0[123]"
---

<objective>
Produire la checklist de validation manuelle des corrections des builds #15/#16 et capturer le résultat coché par l'utilisateur sur les builds TestFlight ACTUELS. C'est la première étape de la séquence stricte D-07 : on confirme que les bugs sont réglés AVANT de cuter le build #17 avec les fondations natives.

Purpose: Isoler les régressions (D-07). Si #15/#16 ne tiennent pas, on doit le savoir avant d'introduire du natif neuf dans #17.
Output: `.planning/phases/01-stabilisation-fondations/STAB-CHECKLIST.md` rempli avec le verdict utilisateur (STAB-01/02/03 cochés ou anomalies décrites).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stabilisation-fondations/01-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rédiger la checklist de validation STAB-01/02/03</name>
  <read_first>
    - .planning/phases/01-stabilisation-fondations/01-CONTEXT.md (bloc <specifics> "Checklist de validation STAB" lignes 90-96 — les 3 scénarios exacts ; D-08 ligne 35 ; D-07 ligne 34)
    - CLAUDE.md (tableau des phases : #15 = photo, #16 = zoom/mention ; UI français, zéro emoji)
  </read_first>
  <action>
    Créer `.planning/phases/01-stabilisation-fondations/STAB-CHECKLIST.md` en français, sans emoji. Transcrire fidèlement les 3 scénarios de CONTEXT <specifics> sous forme de cases à cocher numérotées avec étapes ordonnées et résultat attendu :
    - STAB-01 (photo profil, build #15) : onglet Profil, tap sur la photo/avatar, la galerie iOS s'ouvre sans crash, sélectionner une image, l'avatar se met à jour.
    - STAB-02 (pins au dézoom, build #16) : carte avec au moins 1 point, dézoomer au maximum, les pins restent visibles à tous les niveaux.
    - STAB-03 (mention partenaire, build #16) : testeur 1 crée un point en taguant testeur 2, testeur 2 ouvre Cercle puis Demandes, la section Taguages affiche le point, testeur 2 peut Sceller ou Refuser. Mentionner la note CONTEXT : nécessite 2 comptes, sinon valider en best-effort.
    Préciser en tête du fichier que ces tests s'exécutent sur les builds TestFlight ACTUELS (#15 pour la photo, #16 pour zoom/mention) AVANT le build #17. Ajouter pour chaque item une case `[ ]` à cocher (PASS) et un champ "Anomalie observée :" à remplir si échec. Ne PAS écrire de test automatisé (D-08).
  </action>
  <verify>
    <automated>rg -c "STAB-0[123]" .planning/phases/01-stabilisation-fondations/STAB-CHECKLIST.md confirme au moins 3 occurrences</automated>
  </verify>
  <acceptance_criteria>
    - STAB-CHECKLIST.md existe et contient une section dédiée pour chacun des 3 items STAB-01, STAB-02, STAB-03.
    - Chaque item porte les étapes exactes de CONTEXT <specifics> et une case à cocher + champ anomalie.
    - Aucun emoji dans le fichier ; tout le texte est en français.
    - L'en-tête indique explicitement "builds TestFlight actuels (#15/#16) AVANT le build #17".
  </acceptance_criteria>
  <done>Le fichier checklist est prêt à être coché par l'utilisateur.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>La checklist STAB-01/02/03 à exécuter sur les builds TestFlight actuels (#15 photo, #16 zoom/mention).</what-built>
  <how-to-verify>
    1. Ouvrir l'app LoveMap installée depuis TestFlight (build #15 ou #16 selon l'item).
    2. Dérouler STAB-CHECKLIST.md item par item :
       - STAB-01 : Profil → tap photo → galerie s'ouvre sans crash → choisir image → avatar à jour.
       - STAB-02 : carte avec ≥1 point → dézoom maximum → pins toujours visibles.
       - STAB-03 : avec 2 comptes, taguer le testeur 2 → testeur 2 voit le taguage dans Cercle → Demandes → Taguages et peut Sceller/Refuser (best-effort si un seul compte).
    3. Cocher chaque item qui passe ; décrire toute anomalie dans le champ prévu.
  </how-to-verify>
  <acceptance_criteria>
    - Les 3 items sont marqués PASS, OU les anomalies sont décrites dans le fichier.
    - Si un item échoue, le plan se conclut en signalant un blocage avant de lancer le build #17 (un correctif sera nécessaire d'abord).
  </acceptance_criteria>
  <resume-signal>Répondre "validé" si les 3 items passent, sinon décrire l'anomalie par item.</resume-signal>
</task>

</tasks>

<verification>
- STAB-CHECKLIST.md existe avec les 3 items et le résultat utilisateur capturé.
- Le verdict utilisateur est enregistré (PASS ou anomalies) avant toute production du build #17.
</verification>

<success_criteria>
Les corrections des builds #15/#16 sont confirmées (ou les anomalies documentées) sur TestFlight, satisfaisant le critère 1 de la phase et débloquant la séquence native D-07.
</success_criteria>

<output>
Create `.planning/phases/01-stabilisation-fondations/01-01-SUMMARY.md` when done
</output>
