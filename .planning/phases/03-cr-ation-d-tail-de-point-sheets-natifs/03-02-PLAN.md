---
phase: 03-cr-ation-d-tail-de-point-sheets-natifs
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - components/map/PointMarker.tsx
  - app/(app)/(tabs)/map/index.tsx
autonomous: true
requirements: [IOS-02]
must_haves:
  truths:
    - "Tapping a pin on the map navigates straight to the detail form sheet (no custom preview Modal appears)"
    - "After closing the detail sheet, re-tapping the same pin re-opens it"
    - "Deleting a point still works (from the detail sheet only — no functionality lost)"
  artifacts:
    - path: "components/map/PointMarker.tsx"
      provides: "PointAnnotation whose onSelected router.push-es to the detail route; the custom Modal subtree, its styles, and isOwner/onDelete props are gone"
      contains: "router.push"
    - path: "app/(app)/(tabs)/map/index.tsx"
      provides: "PointMarker call-site without isOwner/onDelete; dead handleDelete removed"
  key_links:
    - from: "components/map/PointMarker.tsx"
      to: "/(app)/point/[id]"
      via: "PointAnnotation onSelected → router.push"
      pattern: "router\\.push.*point"
---

<objective>
Complete the IOS-02 migration: a pin tap opens the native detail sheet directly (D-06). Remove the entire custom preview `<Modal>` from `PointMarker.tsx` and the now-dead delete plumbing from `map/index.tsx`. Delete remains fully available from the detail sheet, so no functionality is lost.

Purpose: This is the "Modal custom → native route" half of IOS-02. After this plan, tapping a moment on the map "opens the page" with a native gesture, and the codebase sheds a chunk of custom JS chrome.
Output: A lean `PointMarker` that is just pins + a navigating `PointAnnotation`; a `map/index.tsx` with no orphaned delete code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cr-ation-d-tail-de-point-sheets-natifs/03-CONTEXT.md
@.planning/phases/03-cr-ation-d-tail-de-point-sheets-natifs/03-RESEARCH.md
@.planning/phases/03-cr-ation-d-tail-de-point-sheets-natifs/03-PATTERNS.md
@.planning/phases/03-cr-ation-d-tail-de-point-sheets-natifs/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the preview Modal from PointMarker and navigate on select</name>
  <files>components/map/PointMarker.tsx</files>
  <read_first>
  - components/map/PointMarker.tsx (full file — Modal subtree 150-234, styles 240-369, props 18-22 + signature 117, state 120, imports 4-14)
  - 03-PATTERNS.md §"components/map/PointMarker.tsx ... CLEANUP" (delete table)
  - 03-RESEARCH.md §"Pattern 3" (lines 232-248) + §"Common Pitfalls / Pitfall 3" (pin re-selection)
  </read_first>
  <action>
  Change `onSelected` to `() => { setSelected(true); router.push('/(app)/point/' + point.id); }` and keep `onDeselected={() => setSelected(false)}`. Import `router` from `expo-router`. Keep the `useEffect(() => { annRef.current?.refresh(); }, [selected])` re-snapshot (Phase 2 visual state) and the `selected` prop on `PointAnnotation`. The `onDeselected` reset is the mitigation for the iOS re-selection quirk (Pitfall 3) — relying on Mapbox deselecting when the user taps elsewhere/the map; flag this for device validation in Plan 05.
  Delete: the entire `<Modal>…</Modal>` subtree (lines 150-234); all Modal-only styles (`overlay`, `sheet`, `handle`, `noteRow`, … through `deleteBtn`, lines 240-369); the `modalVisible`/`setModalVisible` state (line 120); the `isOwner` and `onDelete` fields from the `Props` interface (18-22) and from the component signature (117). Remove now-unused imports: `Modal`, `TouchableOpacity`, `TouchableWithoutFeedback`, `Text`, `IcoTrash`, and the `formatDuration`/`formatDateRelative` helpers if only the Modal used them (24-41). Keep `PinIcon`/`PinIconSelected` (48-115) and the `PointAnnotation` (138-148).
  Do NOT introduce any new dependency and do NOT add a `borderRadius` to a sheet container (the sheet chrome is native — Plan 01).
  </action>
  <acceptance_criteria>
  - `rg "Modal" components/map/PointMarker.tsx` returns NO matches (subtree + import gone).
  - `rg "isOwner|onDelete" components/map/PointMarker.tsx` returns NO matches.
  - `rg "router.push" components/map/PointMarker.tsx` matches a `/(app)/point/` target inside `onSelected`.
  - `rg "annRef.current\?\.refresh|onDeselected" components/map/PointMarker.tsx` both still match (Phase 2 visual state + re-select reset preserved).
  - `npx tsc --noEmit` — 0 new errors (no orphaned imports/vars left in this file).
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Modal|isOwner|onDelete" "components/map/PointMarker.tsx" ; rg -n "router.push" "components/map/PointMarker.tsx" ; npx tsc --noEmit</automated>
  </verify>
  <done>PointMarker is reduced to pins + a navigating `PointAnnotation`; the Modal, its styles, its state, and the `isOwner`/`onDelete` props are removed; `tsc` clean.</done>
</task>

<task type="auto">
  <name>Task 2: Clean up dead delete plumbing in map/index.tsx</name>
  <files>app/(app)/(tabs)/map/index.tsx</files>
  <read_first>
  - app/(app)/(tabs)/map/index.tsx (handleDelete 95-103, PointMarker call-site 109, `deletePoint` destructure 55, `Alert` import 2)
  - 03-PATTERNS.md §"app/(app)/(tabs)/map/index.tsx ... CLEANUP" (line/action table)
  - 03-RESEARCH.md §"Runtime State Inventory" (lines 268-280 — delete now lives only in detail sheet)
  </read_first>
  <action>
  Update the `PointMarker` call-site (line ~109) to `<PointMarker key={p.id} point={p} />` — remove the `isOwner` and `onDelete` props. Delete `handleDelete` (95-103). Remove the `deletePoint` destructure from the `usePoints()` call (line ~55) ONLY if `handleDelete` was its sole consumer — verify by grepping the file first; if `deletePoint` is used elsewhere, keep it. Remove the `Alert` import (line 2) ONLY if `handleDelete` was its sole use; otherwise keep it. Let `tsc`/eslint confirm no orphaned identifiers remain.
  Keep `handleFabPress`/`handleLongPress` (86-93) and the `haptics.press()` at the FAB (91) — these are the unchanged creation-sheet entry points that `router.push('/(app)/point/new', { params })`. Keep the FAB squircle styling (`T.fab` + `borderCurve`).
  </action>
  <acceptance_criteria>
  - `rg "handleDelete" app/(app)/(tabs)/map/index.tsx` returns NO matches.
  - `rg "isOwner|onDelete" app/(app)/(tabs)/map/index.tsx` returns NO matches; the `<PointMarker` call passes only `key` and `point`.
  - `rg "handleFabPress|handleLongPress|haptics.press" app/(app)/(tabs)/map/index.tsx` still match (creation entry points intact).
  - `npx tsc --noEmit` — 0 new errors and `npx eslint app/(app)/(tabs)/map/index.tsx` reports no unused-import/var errors (orphaned `deletePoint`/`Alert` resolved).
  </acceptance_criteria>
  <verify>
    <automated>rg -n "handleDelete|isOwner|onDelete" "app/(app)/(tabs)/map/index.tsx" ; npx tsc --noEmit ; npx eslint "app/(app)/(tabs)/map/index.tsx"</automated>
  </verify>
  <done>`map/index.tsx` passes only `point` (and `key`) to `PointMarker`; `handleDelete` and any orphaned `deletePoint`/`Alert` are removed; creation entry points unchanged; `tsc` + eslint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Supabase | UNCHANGED. Removing the Modal removes a SECOND delete entry point; delete still flows through the same `usePoints` → RLS-guarded path from the detail sheet. No new boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | Loss of delete capability during Modal removal | accept | Delete is preserved in the detail sheet (`point/[id].tsx handleDelete`, RLS-guarded `creator_id === auth.uid()`). RESEARCH confirms zero functionality loss. |
| T-03-05 | Denial of Service (UX) | Pin re-selection quirk → re-tap does nothing (Pitfall 3) | accept | Mitigated via `onDeselected` reset; NOT a security issue. Validated on device in Plan 05; documented edge case. |
| T-03-02 | Tampering | Supply chain | accept | Zero new dependencies (D-01); no install task. |
</threat_model>

<verification>
- `npx tsc --noEmit && npx eslint .` after the wave — 0 new errors, no orphaned imports/vars.
- `rg` assertions confirm Modal removal + props removal.
- Device validation (tap-pin → sheet, re-tap re-opens, delete from sheet) is the phase gate (Plan 05).
</verification>

<success_criteria>
- The custom preview Modal is gone; tapping a pin navigates to the detail sheet route (IOS-02 / D-06 complete).
- `map/index.tsx` has no dead delete plumbing; creation entry points unchanged.
- `tsc` + eslint clean.
</success_criteria>

<output>
Create `.planning/phases/03-cr-ation-d-tail-de-point-sheets-natifs/03-02-SUMMARY.md` when done.
</output>
