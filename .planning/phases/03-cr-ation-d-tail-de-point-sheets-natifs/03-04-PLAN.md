---
phase: 03-cr-ation-d-tail-de-point-sheets-natifs
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/(app)/point/[id].tsx
autonomous: true
requirements: [UI-04, IOS-02, IOS-01]
must_haves:
  truths:
    - "The detail sheet reads top-to-bottom: stamp mini-map, note Display, italic pull-quote, metadata table, contextual actions"
    - "There is no floating back button — the sheet closes via the native grabber/swipe"
    - "Edit mode uses JJ/MM/AAAA segment inputs (no DatePickerModal) and does not freeze when a dismiss-guard is active"
    - "Consent (seal/refuse), edit/save, and delete still work; deleting recolors to T.danger"
  artifacts:
    - path: "app/(app)/point/[id].tsx"
      provides: "Reading-optimized carnet detail in a form sheet; back button removed; date-segment edit; KAV-wrapped edit form; serif+mono action typography; destructive recolored T.danger"
      contains: "dateSegment"
  key_links:
    - from: "app/(app)/point/[id].tsx edit mode"
      to: "JJ/MM/AAAA segments"
      via: "dayStr/monthStr/yearStr state (pattern reused from new.tsx)"
      pattern: "dateSegment|dayStr"
    - from: "app/(app)/point/[id].tsx handleConsent/handleDelete"
      to: "Supabase (consent trigger / delete)"
      via: "unchanged business logic"
      pattern: "handleConsent|handleDelete"
---

<objective>
Redesign `point/[id]` into the reading-optimized "page de carnet" detail sheet (UI-04, D-08/D-09), remove the floating back button (IOS-02 — dismiss is native), and eliminate the iOS-freeze hazard by swapping the edit-mode `DatePickerModal` for JJ/MM/AAAA segment inputs (RESEARCH Pitfall 2 / #2125). Business logic preserved (D-07): consent seal/refuse, edit + save, delete.

Purpose: The second editorial screen. After this plan, opening a moment feels like re-reading a page (note Display + italic pull-quote + metadata), closing is a native gesture, and editing no longer risks a freeze.
Output: A restyled, sheet-aware, freeze-free `point/[id].tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cr-ation-d-tail-de-point-sheets-natifs/03-CONTEXT.md
@.planning/phases/03-cr-ation-d-tail-de-point-sheets-natifs/03-RESEARCH.md
@.planning/phases/03-cr-ation-d-tail-de-point-sheets-natifs/03-PATTERNS.md
@.planning/phases/03-cr-ation-d-tail-de-point-sheets-natifs/03-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Sheet plumbing — remove back button, swap date picker for segments, KAV-wrap edit, edit dirty-guard</name>
  <files>app/(app)/point/[id].tsx</files>
  <read_first>
  - app/(app)/point/[id].tsx (backBtn JSX 201-204 + style 393-403, DatePickerModal import 15 + JSX 318-328 + showDatePicker state 72 + editDateBtn 313-317, edit TextInputs 303-340, centered states paddingTop 154/162, handleConsent 140-150, handleDelete 100-114, handleSaveAndAccept 125-138, enterEditMode 116-123)
  - app/(app)/point/new.tsx (date-segment pattern 244-276 — dateRow/dateSegment/dateSep + dayStr/monthStr/yearStr — SOURCE to copy)
  - 03-PATTERNS.md §"app/(app)/point/[id].tsx ... REDESIGN" (remove back button / swap date / KAV / edit dirty-guard)
  - 03-RESEARCH.md §"Common Pitfalls / Pitfall 2" (#2125 freeze) + §"Pattern 2" (usePreventRemove)
  - 03-UI-SPEC.md §"Navigation & Routing", §"Dismiss Confirmation / Détail mode édition", §"Keyboard Handling"
  </read_first>
  <action>
  Remove the floating back button: delete the `backBtn` `TouchableOpacity` + `IcoArrow dir="left"` JSX (201-204) and its style (393-403, incl. the `T.bg + 'cc'` overlay). Keep the `IcoArrow` import — it is still used for the CTA arrow `dir="right"` (353).
  Swap the edit date control to JJ/MM/AAAA segments: copy the 3-segment date pattern from `point/new.tsx:244-276` (`dateRow`/`dateSegment`/`dateSep` markup + `dayStr`/`monthStr`/`yearStr` state, pre-filled from the point's date) into the edit block. Delete the `DatePickerModal` JSX (318-328), the `editDateBtn` (313-317), the `showDatePicker` state (72), and the `DatePickerModal` import (15). This removes the RN-Modal freeze cause (#2125) AND the `react-native-paper-dates` dependency on this screen.
  Wrap the edit form's bare `TextInput`s (303-340) in a `KeyboardAvoidingView` (same pattern as `new.tsx`: `behavior={Platform.OS === 'ios' ? 'padding' : undefined}`, `keyboardVerticalOffset={0}`).
  Add the edit-mode dismiss-guard (D-04): `usePreventRemove(isEditDirty, ({ data }) => { /* same Alert as new.tsx: 'Abandonner ce moment ?' / 'Votre saisie ne sera pas enregistrée.' / [Continuer l'écriture · Abandonner] + haptics.warn() + navigation.dispatch(data.action) */ })`, where `isEditDirty = editing && (editNote !== point.note || editComment !== (point.comment ?? '') || /* duration + date deltas */)`. Because the date is now segments (no Modal), the guard does NOT need `&& !showDatePicker` — the #2125 cause is removed.
  Remove `insets.top` from the centered/loading/empty states' `paddingTop` (154, 162) — the sheet starts under the grabber; keep `insets.bottom` room. Keep `loadPoint` (80-98), `handleConsent` (140-150), `handleDelete` (100-114), `handleSaveAndAccept` (125-138), `enterEditMode` (116-123) UNCHANGED (D-07).
  </action>
  <acceptance_criteria>
  - `rg "DatePickerModal|paper-dates|showDatePicker" app/(app)/point/[id].tsx` returns NO matches (Modal + dep + state gone).
  - `rg "backBtn" app/(app)/point/[id].tsx` returns NO matches; `rg "IcoArrow" app/(app)/point/[id].tsx` still matches (CTA arrow kept).
  - `rg "dateSegment|dayStr" app/(app)/point/[id].tsx` matches (segment date pattern present in edit block).
  - `rg "usePreventRemove" app/(app)/point/[id].tsx` matches; `rg "KeyboardAvoidingView" app/(app)/point/[id].tsx` matches.
  - `rg "handleConsent|handleDelete|handleSaveAndAccept" app/(app)/point/[id].tsx` all still match (business logic intact).
  - `npx tsc --noEmit` — 0 new errors.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "DatePickerModal|paper-dates|showDatePicker|backBtn" "app/(app)/point/[id].tsx" ; rg -n "dateSegment|usePreventRemove|KeyboardAvoidingView" "app/(app)/point/[id].tsx" ; npx tsc --noEmit</automated>
  </verify>
  <done>Back button removed, edit date is JJ/MM/AAAA segments (no DatePickerModal), edit form KAV-wrapped with a freeze-free dismiss-guard; consent/edit/delete logic untouched; `tsc` clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Reading-carnet redesign (Display note, pull-quote, metadata, action typography, destructive recolor)</name>
  <files>app/(app)/point/[id].tsx</files>
  <read_first>
  - app/(app)/point/[id].tsx (quoteMark F.serifMedium 443, consentEditText F.sansMedium 549, consentAcceptText F.sansMedium 561, saveBtnLabel F.sansMedium 673, miniMap, photoThumb 460-465, consentBadge 502-507, markerDot 385-392, deleteBtnText T.primary 712, IcoTrash color 366, empty copy 163)
  - 03-PATTERNS.md §"Typography harmonisation" + §"Internal radii" + §"Destructive trigger color"
  - 03-UI-SPEC.md §"Page Composition — Détail", §"Typography / Détail", §"Color", §"Copywriting Contract / Détail", §"Action Placement / Détail"
  - constants/theme.ts (radius tokens 38-43), constants/fonts.ts (F)
  </read_first>
  <action>
  Reading flow (top→bottom): static stamp mini-map → eyebrow «La page» (F.mono) → NOTE Display (serif-giant value + `/10` mono + read-only 10-segment bar) → pull-quote (if comment: `«` / `»` serif italic Heading + comment text serif italic Heading, accent T.primary) → photos row (existing behavior preserved — no photo refactor) → metadata table (mono uppercase faint keys left / serif italic values right: «Lieu» «Date» «Durée» «Avec» + consent badge) → contextual action block → destructive trigger.
  Typography (drop ALL `F.sans*` and `F.serifMedium`; use only `F.serifLight` 300, `F.serif` 400, `F.mono` 400): note value → `F.serifLight` Display `fontSize: 80, lineHeight: 76`; `quoteMark` `F.serifMedium` → `F.serif` 400; `consentEditText` `F.sansMedium` → mono uppercase Eyebrow («Modifier»); `consentAcceptText` `F.sansMedium` → serif italic Heading («Sceller»); `saveBtnLabel` `F.sansMedium` → serif italic Heading («Sauvegarder et sceller»); metadata values serif italic Body 16, keys mono Eyebrow 9.
  Copy (French, exact): empty/introuvable from "Page introuvable." → «Page introuvable. Faites glisser vers le bas pour fermer.»; meta keys «Lieu»/«Date»/«Durée»/«Avec»; consent badge «En attente»/«Accepté»/«Refusé»; consent question «Ce moment vous concerne.\nAcceptez-vous d'y figurer ?»; consent actions «Modifier»/«Sceller»/«Refuser ce taguage»; consent success «Page scellée.» + haptics.success(); refuse «Taguage refusé.» + haptics.warn(); edit eyebrow «Modifier le moment»; edit CTA «Sauvegarder et sceller» / «Scellement…»; edit tertiary «Annuler les modifications»; destructive trigger «Effacer cette page»; delete Alert title «Effacer cette page» / body «Cette action est irréversible.» / actions [«Garder la page» · «Effacer» destructive] + haptics.warn()/error().
  Shapes/colors (D-12): mini-map container `T.radiusMd` (16) + `borderCurve:'continuous'`; photo thumb (460-465) `T.radiusSm` (12) + `borderCurve:'continuous'`; consent badge (502-507) `T.radiusXs` (8); consent/save buttons `T.radiusMd` (16) + `borderCurve:'continuous'`; `markerDot` (385-392) stays round. Destructive recolor: `deleteBtnText` `T.primary` (712) → `T.danger`; `IcoTrash` color (366) → `T.danger` (rose stays reserved for affirmative actions). Reserve `T.primary` per UI-SPEC §Color (note value, active segments, guillemets, accented eyebrow, Sceller/Save fill, accepted-badge border, save arrow, mini-map marker). No hardcoded hex / fontFamily; `makeStyles(T)` memoized via `useMemo([T])`.
  </action>
  <acceptance_criteria>
  - `rg "F\\.sans" app/(app)/point/[id].tsx` and `rg "F\\.serifMedium" app/(app)/point/[id].tsx` BOTH return NO matches.
  - `rg "T\\.danger" app/(app)/point/[id].tsx` matches on both the delete text and the `IcoTrash` color; `rg "deleteBtnText.*T\\.primary" app/(app)/point/[id].tsx` returns NO match.
  - `rg "borderCurve" app/(app)/point/[id].tsx` matches `'continuous'`; `rg "radiusMd|radiusSm|radiusXs" app/(app)/point/[id].tsx` all match.
  - `rg "Faites glisser vers le bas pour fermer|Page scellée|Refuser ce taguage|Effacer cette page" app/(app)/point/[id].tsx` all match (exact copy).
  - `rg "80" app/(app)/point/[id].tsx` matches the note Display fontSize.
  - `npx tsc --noEmit` — 0 new errors.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "F\.sans|F\.serifMedium" "app/(app)/point/[id].tsx" ; rg -n "T\.danger|borderCurve|Page scellée|Effacer cette page" "app/(app)/point/[id].tsx" ; npx tsc --noEmit</automated>
  </verify>
  <done>Detail reads as a carnet (Display note, pull-quote, metadata table), action typography is serif+mono only, destructive is T.danger, D-12 radii applied, locked copy in place; consent/edit/delete logic intact; `tsc` clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Supabase (consent trigger / delete / update) | UNCHANGED. `handleConsent`, `handleDelete`, `handleSaveAndAccept` keep their existing RLS-guarded paths; `is_visible` stays trigger-controlled. Presentation-only redesign + date-control swap. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering | `is_visible` / consent bypass via client | accept (unchanged) | Consent state set only by the SQL trigger; the redesign never writes `is_visible`. Owner-only delete keeps `creator_id === auth.uid()` server check. |
| T-03-08 | Denial of Service | Edit `DatePickerModal` + active dismiss-guard freeze (#2125) | mitigate | Root cause removed: edit date is now JJ/MM/AAAA segments (no RN Modal), so the guard runs without the freeze conflict. |
| T-03-09 | Information Disclosure | Reading another user's `push_token`/`date_of_birth` | accept (unchanged) | `search_users` excludes those columns; the detail screen reads only the concerned point/partner. No new query. |
| T-03-02 | Tampering | Supply chain | accept | Zero new dependencies (D-01); the swap REMOVES `react-native-paper-dates` usage on this screen — no install task. |
</threat_model>

<verification>
- `npx tsc --noEmit && npx eslint .` — 0 new errors.
- `rg` assertions confirm back-button removal, date-segment swap, serif+mono discipline, T.danger destructive, D-12 radii, locked copy.
- Device: native dismiss, edit-mode flow (no freeze), consent + delete are validated in the phase gate (Plan 05).
</verification>

<success_criteria>
- `point/[id]` is a reading-optimized carnet sheet with no floating back button, segment-based edit date, serif+mono action typography, T.danger destructive, and D-12 radii.
- Consent, edit/save, and delete logic preserved (D-07); #2125 freeze cause removed.
- `tsc` + eslint clean.
</success_criteria>

<output>
Create `.planning/phases/03-cr-ation-d-tail-de-point-sheets-natifs/03-04-SUMMARY.md` when done.
</output>
