---
phase: 03-cr-ation-d-tail-de-point-sheets-natifs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(app)/_layout.tsx
  - app/(app)/(tabs)/_layout.tsx
  - app/(app)/(tabs)/map/index.tsx
  - app/(app)/(tabs)/point/list.tsx
  - app/(app)/(tabs)/friends/index.tsx
  - app/(app)/(tabs)/friends/requests.tsx
  - app/(app)/(tabs)/profile/index.tsx
autonomous: true
requirements: [IOS-01, IOS-02]
must_haves:
  truths:
    - "Tapping the FAB on the map opens point/new as a native form sheet (slide-up, grabber) instead of a full screen"
    - "Tapping a notification deep-link opens point/[id] as a native form sheet"
    - "The 5 tabs (Carte, Liste, Amis, Profil + hidden Requests) still navigate exactly as before"
    - "The session guard still redirects unauthenticated users to /(auth)/login"
  artifacts:
    - path: "app/(app)/_layout.tsx"
      provides: "Stack parent declaring (tabs) group + point/new + point/[id] as formSheet routes; session guard preserved"
      contains: "presentation"
    - path: "app/(app)/(tabs)/_layout.tsx"
      provides: "Relocated Tabs navigator (the former (app)/_layout body), point/new & point/[id] href:null lines removed, friends/requests href:null kept"
      contains: "Tabs"
    - path: "app/(app)/(tabs)/map/index.tsx"
      provides: "Map screen relocated under (tabs) (verbatim move, content unchanged in this plan)"
  key_links:
    - from: "app/(app)/_layout.tsx"
      to: "point/new + point/[id]"
      via: "Stack.Screen with presentation:'formSheet' sheetAllowedDetents:[0.92]"
      pattern: "presentation.*formSheet"
    - from: "app/_layout.tsx notif handler"
      to: "/(app)/point/[id]"
      via: "router.push (now resolves to the sheet route)"
      pattern: "router\\.push"
---

<objective>
Restructure the `(app)` navigation so `point/new` and `point/[id]` become native iOS form sheets (IOS-01), and the migration of these screens off hidden full-screen tabs onto Stack routes is complete (IOS-02 foundation). This is the highest-risk task of the phase: a folder move plus a `Tabs`→`Stack` rewrite. Business logic and screen content are NOT touched here — this plan is pure routing structure.

Purpose: Every other Phase 3 plan depends on these two routes being declared as `formSheet` siblings of the tab group. Get the structure right and `tsc`-clean first.
Output: New `(tabs)` group folder with the 5 relocated tab screens; rewritten `(app)/_layout.tsx` as a `<Stack>` declaring the tab group + the two sheet routes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cr-ation-d-tail-de-point-sheets-natifs/03-CONTEXT.md
@.planning/phases/03-cr-ation-d-tail-de-point-sheets-natifs/03-RESEARCH.md
@.planning/phases/03-cr-ation-d-tail-de-point-sheets-natifs/03-PATTERNS.md
@.planning/phases/03-cr-ation-d-tail-de-point-sheets-natifs/03-UI-SPEC.md
</context>

## Phase Goal

**As a** utilisateur de LoveMap, **I want to** créer et consulter un moment dans de vraies bottom sheets iOS natives (poignée, swipe), présentées comme une « page de carnet », **so that** le flux cœur de l'app se sente premium, fluide et natif sur iPhone.

> Story derived faithfully from ROADMAP Phase 3 Goal + Success Criteria 1–4 (no invented scope).

<tasks>

<task type="auto">
  <name>Task 1: Create the (tabs) group and relocate the 5 tab screens</name>
  <files>
  app/(app)/(tabs)/_layout.tsx,
  app/(app)/(tabs)/map/index.tsx,
  app/(app)/(tabs)/point/list.tsx,
  app/(app)/(tabs)/friends/index.tsx,
  app/(app)/(tabs)/friends/requests.tsx,
  app/(app)/(tabs)/profile/index.tsx
  </files>
  <read_first>
  - app/(app)/_layout.tsx (current `<Tabs>` body — lines 12-138: `TabIcon`, `AppLayout`, `makeStyles`)
  - 03-PATTERNS.md §"NAVIGATION RESTRUCTURE" + §"app/(app)/(tabs)/_layout.tsx (NEW — relocated Tabs)"
  - 03-RESEARCH.md §"Recommended Project Structure" (lines 146-165)
  </read_first>
  <action>
  Move the 5 tab screen files VERBATIM (no content change) from `app/(app)/{map/index,point/list,friends/index,friends/requests,profile/index}.tsx` into `app/(app)/(tabs)/{...same relative paths}`. Imports use the `@/` absolute alias (verified in PATTERNS), so deeper nesting does not require relative-import edits — but rely on the plan-end `tsc` gate to catch any breakage.
  Create `app/(app)/(tabs)/_layout.tsx` as the relocated `<Tabs>` navigator: copy the `TabIcon` helper (current lines 12-54), the `<Tabs>` JSX block + tab `Tabs.Screen` entries, and `makeStyles` tab-bar styles (current lines 120-138) from the old `(app)/_layout.tsx`. Two edits only: (1) REMOVE the two lines `<Tabs.Screen name="point/new" options={{ href: null }} />` and `<Tabs.Screen name="point/[id]" options={{ href: null }} />` (they become Stack sheet routes in Task 2); (2) KEEP `<Tabs.Screen name="friends/requests" options={{ href: null }} />` (out of Phase 3 scope). The tab `name=` props are unchanged because the screen files move WITH this layout into `(tabs)/`. This file renders under an already-authed parent, so it carries NO session guard (guard stays in the parent — Task 2).
  Do NOT alter any screen's content, imports beyond the move, or the tab-bar opaque background rule (CLAUDE.md rule 13: `#111114`/`#f2f2f7`).
  </action>
  <acceptance_criteria>
  - All 5 files exist under `app/(app)/(tabs)/` and no longer exist at `app/(app)/` (except `point/new.tsx` and `point/[id].tsx`, which stay put).
  - `rg "href: null" app/(app)/(tabs)/_layout.tsx` matches exactly ONE line, and that line names `friends/requests`.
  - `rg "point/new|point/\[id\]" app/(app)/(tabs)/_layout.tsx` returns NO matches (those routes left the Tabs).
  - `rg "<Tabs" app/(app)/(tabs)/_layout.tsx` matches (Tabs navigator present); the relocated `_layout.tsx` contains NO `useAuthStore`/`session` guard.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "href: null" "app/(app)/(tabs)/_layout.tsx" ; rg -c "point/new" "app/(app)/(tabs)/_layout.tsx"</automated>
  </verify>
  <done>The 5 tab screens live under `(tabs)/`; the relocated Tabs layout exists with the two `point/*` href:null lines removed and `friends/requests` href:null kept. (Full `tsc` runs after Task 2 — the parent still references old paths until then.)</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite (app)/_layout.tsx as a Stack with formSheet sheet routes</name>
  <files>app/(app)/_layout.tsx</files>
  <read_first>
  - app/(app)/_layout.tsx (session guard lines 56-69 — keep verbatim)
  - app/_layout.tsx (root `<Stack>` analog, line ~110)
  - 03-PATTERNS.md §"Sheet options block" + §"Current (app)/_layout.tsx value deltas"
  - 03-RESEARCH.md §"Pattern 1" (lines 167-200) — sheet option values + `contentStyle`
  - 03-UI-SPEC.md §"Native Sheet Config" (the locked option table)
  </read_first>
  <action>
  Replace the `<Tabs>` body with a `<Stack>`. Keep the session guard VERBATIM: `const { session, loading } = useAuthStore()`, the `useEffect` redirect to `/(auth)/login` when `!loading && !session`, and `if (!session) return null`. Keep `const T = useTheme()` for the background token.
  Define a `sheetOptions` const with exactly: `presentation: 'formSheet'`, `sheetAllowedDetents: [0.92]` (D-02, ≥0.7 mitigates iOS 26 #3235), `sheetGrabberVisible: true` (D-03), `sheetCornerRadius: 28` (D-03 = T.radiusXl), `gestureEnabled: true` (IOS-01 swipe-to-dismiss), `headerShown: false` (internal editorial header). Type it `as const`.
  Render `<Stack screenOptions={{ headerShown: false, contentStyle: { backgroundColor: T.bg } }}>` with three children: `<Stack.Screen name="(tabs)" />`, `<Stack.Screen name="point/new" options={sheetOptions} />`, `<Stack.Screen name="point/[id]" options={sheetOptions} />`. The `contentStyle` background mitigates the swipe-dismiss flash (ARCHITECTURE §Gestures). Remove the now-unused `TabIcon` and tab-bar `makeStyles` (they moved to `(tabs)/_layout.tsx` in Task 1). Do NOT force any `borderRadius` on a sheet container — the corner radius is the native `sheetCornerRadius` only (D-03 anti-pattern).
  </action>
  <acceptance_criteria>
  - `rg "presentation" app/(app)/_layout.tsx` matches `'formSheet'`; `rg "sheetAllowedDetents" app/(app)/_layout.tsx` matches `[0.92]`; `sheetGrabberVisible: true`, `sheetCornerRadius: 28`, and `gestureEnabled: true` are all present.
  - `rg "contentStyle" app/(app)/_layout.tsx` matches a `backgroundColor: T.bg` entry.
  - The session guard is intact: `rg "router.replace\('/\(auth\)/login'\)" app/(app)/_layout.tsx` matches, and `if (!session) return null` is present.
  - `rg "<Tabs" app/(app)/_layout.tsx` returns NO matches (it is now a Stack).
  - `npx tsc --noEmit` introduces 0 NEW errors versus the ~38-error baseline (routing resolves: `router.replace('/(app)/map')`, `router.push('/(app)/point/'+id)`, and `router.push({ pathname:'/(app)/point/new', ... })` still type-check — `(tabs)` is URL-transparent and `map` is unique).
  </acceptance_criteria>
  <verify>
    <automated>rg -n "presentation|sheetAllowedDetents|sheetGrabberVisible|sheetCornerRadius|gestureEnabled|contentStyle" "app/(app)/_layout.tsx" ; npx tsc --noEmit</automated>
  </verify>
  <done>`(app)/_layout.tsx` is a `<Stack>` declaring the `(tabs)` group plus the two `formSheet` routes with the locked sheet options; session guard preserved; `tsc` shows 0 new errors and all known route call-sites resolve.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Supabase (RLS/RPC/trigger) | UNCHANGED in Phase 3 (D-07). This plan only re-arranges client-side route declarations; no query, RLS, or trigger is touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | Session guard relocation during Tabs→Stack rewrite | mitigate | Session guard (`if (!session) return null` + redirect) kept VERBATIM in the new `(app)/_layout.tsx` parent; `(tabs)/_layout.tsx` renders only under the authed parent. Acceptance criteria assert the guard is present. |
| T-03-02 | Tampering | Supply chain (npm installs) | accept | Zero new dependencies (D-01). Package Legitimacy Audit is vacuously safe (RESEARCH §Package Legitimacy Audit). No install task in this plan. |
| T-03-03 | Information Disclosure | Notification deep-link now opens a sheet route | accept | `router.push('/(app)/point/'+pointId)` resolves to the sheet under the same session-guarded `(app)` parent; no new data exposed, RLS unchanged. |
</threat_model>

<verification>
- `npx tsc --noEmit` — 0 new errors vs baseline (~38). This is the routing-integrity gate.
- `rg` structural assertions per task above confirm the move and the sheet-route declarations.
- Device validation of the sheet presentation (slide-up, grabber, swipe) is deferred to the phase gate (Plan 05).
</verification>

<success_criteria>
- `point/new` and `point/[id]` are declared as `formSheet` Stack routes (IOS-01 presentation live).
- The 5 tab screens are relocated under `(tabs)/`; the two former `href:null` point routes are gone from the Tabs.
- Session guard preserved; `tsc` clean (0 new errors); all known route call-sites resolve.
</success_criteria>

<output>
Create `.planning/phases/03-cr-ation-d-tail-de-point-sheets-natifs/03-01-SUMMARY.md` when done.
</output>
