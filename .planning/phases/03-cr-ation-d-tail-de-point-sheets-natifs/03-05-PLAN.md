---
phase: 03-cr-ation-d-tail-de-point-sheets-natifs
plan: 05
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-03", "03-04"]
files_modified: []
autonomous: false
requirements: [IOS-01, IOS-02, UI-03, UI-04]
must_haves:
  truths:
    - "On a physical iOS 26 device, the creation and detail screens open as native form sheets with a visible grabber and swipe-to-dismiss"
    - "Tapping a pin opens the detail sheet directly; re-tapping the same pin after closing re-opens it"
    - "Starting creation then swiping down asks 'Abandonner ce moment ?'; empty creation closes directly"
    - "The full business flow (create with mandatory partner, consent seal/refuse, edit, delete) works as before in both themes"
  artifacts:
    - path: ".planning/phases/03-cr-ation-d-tail-de-point-sheets-natifs/03-05-SUMMARY.md"
      provides: "Recorded device-validation results for IOS-01/IOS-02/UI-03/UI-04"
  key_links: []
---

<objective>
Phase gate: validate the native-sheet behaviors that CANNOT be checked statically (this project has no automated test framework — VALIDATION.md). Sheets, gestures, keyboard, and the dismiss-confirm are device-only and the simulator diverges, so a build (or OTA update) plus a TestFlight checklist on a physical iOS 26 device is required before `/gsd:verify-work`.

Purpose: Confirm IOS-01, IOS-02, UI-03, UI-04 on-device and that D-07 business logic is intact in both themes.
Output: A SUMMARY recording the checklist results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-cr-ation-d-tail-de-point-sheets-natifs/03-VALIDATION.md
@.planning/phases/03-cr-ation-d-tail-de-point-sheets-natifs/03-RESEARCH.md
@.planning/phases/03-cr-ation-d-tail-de-point-sheets-natifs/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run static gates and decide build vs OTA</name>
  <files>(none — verification + delivery decision)</files>
  <read_first>
  - 03-RESEARCH.md §"Environment Availability" (lines 386-396 — if NO native change, OTA `eas update` is valid; otherwise rebuild + runtimeVersion bump)
  - 03-VALIDATION.md §"Sampling Rate" + §"Manual-Only Verifications"
  - CLAUDE.md §"Workflow obligatoire" (git push + eas update) + Phase 1 note on native vs OTA (OTA inopérant chez l'utilisateur si natif touché)
  </read_first>
  <action>
  Run the full static suite: `npx tsc --noEmit` (expect 0 new errors vs the ~38 baseline) and `npx eslint .` (no new errors). Confirm Phase 3 introduced ZERO native modules: the only change that could force a rebuild would be a new native dependency — RESEARCH confirms D-01 means none, and the detail screen swap REMOVES a dep usage. Verify no `package.json` dependency was added since the Phase 2 baseline. If clean and no native change → an OTA `eas update --branch main` delivers the validation build. If any native change slipped in → produce a native build (#20) and confirm `runtimeVersion` policy is still `fingerprint` (FOND-01). Surface the chosen delivery path to the user.
  </action>
  <acceptance_criteria>
  - `npx tsc --noEmit` shows 0 new errors vs baseline; `npx eslint .` shows no new errors.
  - No new entry in `package.json` `dependencies` vs the Phase 2 baseline (confirms D-01 zero-dep → OTA viable).
  - The delivery decision (OTA vs native build) is stated explicitly with its rationale.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit ; npx eslint . ; git diff --stat HEAD~1 -- package.json</automated>
  </verify>
  <done>Static gates pass, dependency surface confirmed unchanged, and the OTA-vs-build delivery path is decided and surfaced.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  Phase 3 turned `point/new` and `point/[id]` into native iOS form sheets (grabber, swipe-to-dismiss, detent ~0.9), removed the custom preview Modal so a pin tap opens the detail sheet directly, redesigned both screens as "page de carnet" (note-first creation; reading-optimized detail), added the "Abandonner ce moment ?" dismiss guard, and swapped the edit date for JJ/MM/AAAA segments. The validation build/OTA from Task 1 is deployed to TestFlight.
  </what-built>
  <how-to-verify>
  On a PHYSICAL iPhone running iOS 26 (not the simulator), in BOTH dark and light themes:
  1. IOS-01 — From the map, tap the FAB. The creation screen slides up as a native sheet with a visible grabber and a large detent (~0.9, filled — not shrunken). Swipe the sheet down to dismiss. Repeat from a long-press on the map.
  2. IOS-02 — Tap a pin on the map. The detail sheet opens DIRECTLY (no custom preview popup). Close it (swipe). Re-tap the SAME pin → it re-opens. Tap a notification deep-link to a point (if available) → opens the detail sheet.
  3. UI-03 — In creation: the NOTE (serif-giant) is the first/hero element. Focus the «Commentaire» field → it stays visible above the keyboard and the sheet does not collapse. Set a note ≠ 7 (or pick a partner), then swipe the sheet down → the «Abandonner ce moment ?» alert appears. With nothing entered, swipe down → it closes directly. (Known accepted edge case: tapping the dimmed area ABOVE the sheet may close without the alert — #3568, do not file as a bug.)
  4. UI-04 — In detail: there is NO floating back button; closing is by grabber/swipe. The note shows as a large Display value, the comment as an italic pull-quote, metadata in a key/value table. Enter edit mode → the date is three JJ/MM/AAAA segments (no date-picker popup) and editing does not freeze the app.
  5. D-07 — Create a point (partner is mandatory; sealing creates it). As the tagged partner, open the pending tag → «Sceller» accepts, «Refuser ce taguage» refuses. Edit and save a point. Delete a point → the «Effacer cette page» confirm is red/destructive and the trigger text is rose-free (T.danger). Everything behaves as before.
  6. Themes — Toggle dark/light and re-inspect both sheets: no hardcoded-color regressions, safe areas / home indicator respected.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe the failing step(s).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Supabase | UNCHANGED. This plan ships no code change; it validates the prior plans and delivers the build/OTA. The phase remains iso-security (RESEARCH §Security Domain). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Tampering | OTA delivery when a native change slipped in | mitigate | Task 1 verifies the `package.json` dependency surface is unchanged before choosing OTA; if any native change is detected, a native build (#20) with `runtimeVersion: fingerprint` is produced instead (avoids the TestFlight crash class from Phase 1). |
| T-03-02 | Tampering | Supply chain | accept | Zero new dependencies confirmed in Task 1; no install. |
</threat_model>

<verification>
- Static: `npx tsc --noEmit && npx eslint .` green.
- Manual (device, blocking): the IOS-01/IOS-02/UI-03/UI-04 + D-07 + theme checklist above, on a physical iOS 26 device.
</verification>

<success_criteria>
- Static gates green and dependency surface unchanged (OTA viable, or native build produced if not).
- The full device checklist passes in both themes → IOS-01, IOS-02, UI-03, UI-04 validated on-device, D-07 business logic intact.
</success_criteria>

<output>
Create `.planning/phases/03-cr-ation-d-tail-de-point-sheets-natifs/03-05-SUMMARY.md` recording the delivery path and the device-checklist results.
</output>
