---
phase: 03-cr-ation-d-tail-de-point-sheets-natifs
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/(app)/point/new.tsx
autonomous: true
requirements: [UI-03, IOS-01]
must_haves:
  truths:
    - "The creation sheet leads with the NOTE (serif-giant value + 10 tappable segments) as the first thing the user sets"
    - "Focusing the comment field keeps it visible above the keyboard inside the sheet"
    - "Swiping the sheet down with non-empty input shows an 'Abandonner ce moment ?' confirmation; with empty input it closes directly"
    - "Sealing still creates the point via the create_point RPC with a mandatory partner"
  artifacts:
    - path: "app/(app)/point/new.tsx"
      provides: "Note-first carnet creation screen in a form sheet; KAV+ScrollView keyboard handling; usePreventRemove dirty-guard; business logic preserved"
      contains: "usePreventRemove"
  key_links:
    - from: "app/(app)/point/new.tsx swipe-dismiss"
      to: "Alert 'Abandonner ce moment ?'"
      via: "usePreventRemove(isDirty, callback)"
      pattern: "usePreventRemove"
    - from: "app/(app)/point/new.tsx handleSubmit"
      to: "create_point RPC"
      via: "createPoint (unchanged)"
      pattern: "createPoint"
---

<objective>
Redesign `point/new` into the note-first "page de carnet" creation sheet (UI-03, D-08/D-09/D-10) and wire the native-sheet UX it now lives in: keyboard handling tuned for a sheet that does not resize, and the D-04 dismiss-confirmation guard. ALL business logic is preserved (D-07): mandatory partner, `create_point` RPC, GPS validation, user-entered JJ/MM/AAAA date, existing haptics.

Purpose: This is the first of the two editorial screens. After this plan, opening creation feels like inscribing a page — the note is the hero, the sheet behaves correctly with the keyboard, and abandoning a started page asks for confirmation.
Output: A restyled, sheet-aware `point/new.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cr-ation-d-tail-de-point-sheets-natifs/03-CONTEXT.md
@.planning/phases/03-cr-ation-d-tail-de-point-sheets-natifs/03-RESEARCH.md
@.planning/phases/03-cr-ation-d-tail-de-point-sheets-natifs/03-PATTERNS.md
@.planning/phases/03-cr-ation-d-tail-de-point-sheets-natifs/03-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Sheet plumbing — keyboard handling + D-04 dismiss-guard</name>
  <files>app/(app)/point/new.tsx</files>
  <read_first>
  - app/(app)/point/new.tsx (KAV+ScrollView 174-178, header paddingTop 180, handleSubmit 118-171, state for comment/durationMinutes/note/selectedPartnerId)
  - 03-PATTERNS.md §"app/(app)/point/new.tsx ... Keyboard pattern / Dirty-guard"
  - 03-RESEARCH.md §"Pattern 2" (usePreventRemove) + §"Code Examples / Keyboard" + §"Common Pitfalls / Pitfall 1" (tap-backdrop accepted, Option A)
  - 03-UI-SPEC.md §"Keyboard Handling" + §"Dismiss Confirmation"
  </read_first>
  <action>
  Keyboard: on the existing `KeyboardAvoidingView`, set `behavior={Platform.OS === 'ios' ? 'padding' : undefined}` and add `keyboardVerticalOffset={0}` (no native header in the sheet). On the inner `ScrollView` add `keyboardShouldPersistTaps="handled"`, `showsVerticalScrollIndicator={false}`, and `contentContainerStyle={{ paddingBottom: insets.bottom + 32 }}` (home indicator + page-close room). Do NOT switch to `BottomSheetScrollView`/`BottomSheetTextInput` (gorhom-only, not installed). Remove `insets.top` from the header `paddingTop` (currently `insets.top + 24`) — the sheet already starts under the grabber; keep a plain top pad from the spacing scale.
  Dismiss-guard (D-04): import `usePreventRemove` from `@react-navigation/native`, `useNavigation` from `expo-router`/`@react-navigation/native`, and `Alert` from `react-native` (mirror the `Alert` import used in `map/index.tsx`). Compute `isDirty = comment.trim() !== '' || durationMinutes !== '' || note !== 7 || selectedPartnerId !== null`. Call `usePreventRemove(isDirty, ({ data }) => { haptics.warn(); Alert.alert('Abandonner ce moment ?', 'Votre saisie ne sera pas enregistrée.', [{ text: "Continuer l'écriture", style: 'cancel' }, { text: 'Abandonner', style: 'destructive', onPress: () => navigation.dispatch(data.action) }]); })`. This fires on swipe-down AND on the tertiary "Abandonner la saisie" link (which calls `router.back()`). The tap-backdrop leak (react-native-screens #3568) is ACCEPTED (Option A) — document it in the SUMMARY, do not add workaround code.
  Keep all of `handleSubmit` (118-171), `createPoint` RPC call, GPS validation, and date parsing UNCHANGED (D-07).
  </action>
  <acceptance_criteria>
  - `rg "usePreventRemove" app/(app)/point/new.tsx` matches; `rg "Abandonner ce moment" app/(app)/point/new.tsx` matches the exact title.
  - `rg "keyboardVerticalOffset" app/(app)/point/new.tsx` matches `={0}`; `contentContainerStyle` includes `insets.bottom + 32`.
  - `rg "insets.top" app/(app)/point/new.tsx` no longer matches inside the header padding (sheet starts under grabber).
  - `rg "createPoint" app/(app)/point/new.tsx` still matches (RPC path preserved); `rg "BottomSheet" app/(app)/point/new.tsx` returns NO matches.
  - `npx tsc --noEmit` — 0 new errors.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "usePreventRemove|Abandonner ce moment|keyboardVerticalOffset|createPoint" "app/(app)/point/new.tsx" ; npx tsc --noEmit</automated>
  </verify>
  <done>The creation sheet has sheet-correct keyboard handling and the D-04 dirty-guard wired to the native swipe + tertiary link; business logic untouched; `tsc` clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Note-first carnet redesign (typography, radii, copy)</name>
  <files>app/(app)/point/new.tsx</files>
  <read_first>
  - app/(app)/point/new.tsx (innerBorder JSX 181 + style 402-408, title 183, noteValue fontSize 531, partnerName F.sans 642, cancelText 380, miniMap 455-462, partnerChip 604-613, segment 547-554, CTA 648-689, date segments 244-276)
  - 03-PATTERNS.md §"Per-file value deltas" (the create table)
  - 03-UI-SPEC.md §"Page Composition — Création", §"Typography / Création", §"Color", §"Copywriting Contract / Création", §"Action Placement / Création"
  - constants/theme.ts (radius tokens 38-43), constants/fonts.ts (F)
  </read_first>
  <action>
  Reorder the vertical flow so the NOTE is first (D-10): eyebrow `N° 001 — Nouvelle page` (F.mono, accent T.primary) → NOTE hero (serif-giant value + `/10` mono + 10 tappable segments) → Commentaire → Partenaire (+ `Requis`) → Durée → Date (JJ/MM/AAAA segments, reused as-is) → Lieu (address search → mini-map → resolved address) → CTA inline-fin → tertiary link. Remove the title "Inscrire un moment" as a header; the note is the hero.
  Typography (drop ALL `F.sans*` and `F.serifMedium` on this screen — use only `F.serifLight` 300, `F.serif` 400, `F.mono` 400): set the note value to `F.serifLight` Display `fontSize: 72, lineHeight: 68`; `partnerName` from `F.sans` → `F.serif`; field labels / `Requis` / resolved address / separators → `F.mono` Eyebrow 10. Inputs (comment, duration, search) → `F.serif` italic Body 20.
  Copy (French, exact): page eyebrow «N° 001 — Nouvelle page»; comment placeholder «Décrivez ce moment…»; counter «{n}/500»; partner label «Partenaire» + «Requis»; no-friends state «Ajoutez un ami à votre cercle pour inscrire un moment.»; duration label «Durée (minutes)» placeholder «—»; date label «Date»; address placeholder «Rechercher une adresse» (button `accessibilityLabel="Rechercher"`); CTA eyebrow «Archiver» + label «Sceller la page», in-progress «Scellement…»; tertiary link text from "Annuler" → «Abandonner la saisie» (mono Eyebrow, T.textFaint). Keep existing warn/error snackbar copy + haptics (partner-missing warn, GPS warn, error).
  Shapes/colors (D-12): DELETE the `innerBorder` JSX (181) and its style (402-408) — no skeuomorphism (D-08). Mini-map container → `T.radiusMd` (16) + `borderCurve:'continuous'`; partner chip → `T.radiusSm` (12) + `borderCurve:'continuous'`, chip pad `pillPadX=12`; note segments → `T.radiusXs` (8); CTA block → `T.radiusMd` (16) + `borderCurve:'continuous'`; underline inputs keep no radius. Reserve `T.primary` only for: note value, active note segments, accented eyebrow, `Requis`, CTA fill + arrow, selected partner chip fill, mini-map center dot. CTA disabled (`opacity 0.4`) when `submitting` or `friends.length === 0`. No hardcoded hex / fontFamily; `makeStyles(T)` memoized via `useMemo([T])`. Fire-and-forget `haptics.select()` on segment/chip tap.
  </action>
  <acceptance_criteria>
  - `rg "F\\.sans" app/(app)/point/new.tsx` and `rg "F\\.serifMedium" app/(app)/point/new.tsx` BOTH return NO matches.
  - `rg "innerBorder" app/(app)/point/new.tsx` returns NO matches (skeuomorphism removed).
  - `rg "borderCurve" app/(app)/point/new.tsx` matches `'continuous'`; `rg "radiusMd|radiusSm|radiusXs" app/(app)/point/new.tsx` all match.
  - `rg "N° 001 — Nouvelle page|Sceller la page|Abandonner la saisie|Décrivez ce moment" app/(app)/point/new.tsx` all match (exact copy).
  - `rg "72" app/(app)/point/new.tsx` matches the note Display fontSize; `rg "#" app/(app)/point/new.tsx` finds no hardcoded hex color literal in styles (tokens only).
  - `npx tsc --noEmit` — 0 new errors.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "F\.sans|F\.serifMedium|innerBorder" "app/(app)/point/new.tsx" ; rg -n "borderCurve|N° 001 — Nouvelle page|Sceller la page" "app/(app)/point/new.tsx" ; npx tsc --noEmit</automated>
  </verify>
  <done>Creation screen leads with the note hero, uses only serif+mono families with D-12 radii and the locked copy, `innerBorder` removed, `T.primary` reserved to affirmative accents; business logic intact; `tsc` clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Supabase (`create_point` RPC) | UNCHANGED. Same inputs (note bounded, comment, duration, date, GPS), same RPC, same RLS/trigger (`is_visible` via `on_partner_consent`). Presentation-only redesign. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering | `is_visible` / consent bypass via client | accept (unchanged) | Set only by the SQL trigger `on_partner_consent`; this redesign never writes `is_visible`. Mandatory-partner gate preserved in `handleSubmit`. |
| T-03-07 | Information Disclosure (UX) | Tap-backdrop dismiss bypasses D-04 confirm (#3568) → silent input loss | accept (Option A) | Edge case, no data exposed; documented. At detent 0.92 the backdrop is a ~8% strip. Track #3568 for a future version pin. |
| T-03-05 | Input Validation | Note/comment/duration/date fields | accept (unchanged) | Same fields, same existing validation (bounded note, date parse, GPS check). No new untrusted input. |
| T-03-02 | Tampering | Supply chain | accept | Zero new dependencies (D-01); no install task. |
</threat_model>

<verification>
- `npx tsc --noEmit && npx eslint .` — 0 new errors.
- `rg` assertions confirm note-first hero, serif+mono discipline, D-12 radii, locked copy, no hardcoded hex.
- Device: keyboard-above-comment, dismiss-confirm behavior, and full creation flow are validated in the phase gate (Plan 05).
</verification>

<success_criteria>
- `point/new` is a note-first carnet sheet with serif+mono typography, D-12 radii, locked French copy, and the D-04 dismiss guard.
- `create_point` RPC, mandatory partner, GPS validation, and date entry are preserved (D-07).
- `tsc` + eslint clean.
</success_criteria>

<output>
Create `.planning/phases/03-cr-ation-d-tail-de-point-sheets-natifs/03-03-SUMMARY.md` when done.
</output>
