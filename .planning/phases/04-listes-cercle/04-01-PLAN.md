---
phase: 04-listes-cercle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "app/(app)/(tabs)/point/list.tsx"
  - "components/point/PointListItem.tsx"
  - "components/point/FiltersBottomSheet.tsx"
autonomous: true
requirements: [UI-05]
must_haves:
  truths:
    - "Chaque ligne de moment affiche la note /10 en gros chiffre serif (Display 44) en T.text à gauche, plus de N°00X"
    - "La liste est groupée par mois avec headers eyebrow mono sticky (ex. « JUIN 2026 »)"
    - "Les filtres sont des pills inline en haut de liste (note min Toutes/5+/7+/9+, tri Date/Note) — plus aucune ouverture de FiltersBottomSheet"
    - "pull-to-refresh, skeleton SkeletonRow et snackbar erreur réseau restent fonctionnels"
    - "components/point/FiltersBottomSheet.tsx est supprimé et n'est plus importé nulle part"
  artifacts:
    - path: "components/point/PointListItem.tsx"
      provides: "Ligne archétype table des matières — note Display 44 T.text, chevron mono, sans N°00X"
      contains: "F.serifLight"
    - path: "app/(app)/(tabs)/point/list.tsx"
      provides: "Écran liste : titre « Le carnet », pills de filtre inline, SectionList sticky par mois"
      contains: "groupByMonth"
  key_links:
    - from: "app/(app)/(tabs)/point/list.tsx"
      to: "groupByMonth"
      via: "useMemo([points, minNote, sort])"
      pattern: "groupByMonth\\("
    - from: "app/(app)/(tabs)/point/list.tsx"
      to: "PointListItem"
      via: "renderItem"
      pattern: "PointListItem"
---

<objective>
Refondre l'écran liste des moments (`point/list`, UI-05) selon l'archétype « table des matières » : note /10 en ancre serif géante (T.text, pas rose), groupement par mois à headers sticky, filtres inline en pills (suppression de l'usage et du fichier `FiltersBottomSheet`).

Purpose: UI-05 — l'écran liste doit lire comme un sommaire de magazine (gros chiffre à gauche, métadonnées mono alignées droite, filet de séparation), cohérent avec la discipline typo serif+mono / 2 graisses de la Phase 3.
Output: `PointListItem.tsx` refondu, `list.tsx` refondu (pills inline + sticky), `FiltersBottomSheet.tsx` supprimé. Baseline tsc capturée pour toute la phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-listes-cercle/04-CONTEXT.md
@.planning/phases/04-listes-cercle/04-UI-SPEC.md
@.planning/phases/04-listes-cercle/04-PATTERNS.md
@.planning/phases/04-listes-cercle/04-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Tokens & primitives — utiliser directement, ne pas hardcoder. -->
constants/theme.ts (T): T.text, T.textDim, T.textFaint, T.primary, T.border, T.surface2, T.pill (999), T.radiusSm (12), T.bg
constants/fonts.ts (F): F.serifLight (300 italic), F.serif (400 italic), F.mono (400) — F.sans*/F.serifMedium PROSCRITS sur cet écran
lib/haptics.ts: haptics.tap() (tap ligne), haptics.select() (tap pill) — fire-and-forget, jamais await
components/ui/PressableScale.tsx: feedback scale spring sur tap
components/ui/SkeletonItem.tsx: SkeletonRow (loading)
hooks/usePoints.ts: usePoints().points (MapPoint[]), fetchMyPoints — INCHANGÉ
types/app.types.ts: MapPoint { id, note, comment, happened_at, created_at, partnerUsername, ... }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capturer la baseline tsc + confirmer la suppression sûre de FiltersBottomSheet (Wave 0 gate)</name>
  <files>(mesure — aucun fichier modifié)</files>
  <read_first>
    - .planning/phases/04-listes-cercle/04-VALIDATION.md (Test Infrastructure : baseline ~36, gate = 0 NEW)
    - .planning/phases/04-listes-cercle/04-PATTERNS.md (l. 234-236 — FiltersBottomSheet référencé uniquement par list.tsx)
  </read_first>
  <action>Exécuter `npx tsc --noEmit` et compter les erreurs (`error TS`) — la baseline confirmée est 36. Enregistrer ce nombre dans le SUMMARY comme référence de gate « 0 nouvelle erreur » pour les 3 plans de la phase. Exécuter une recherche de références sur `FiltersBottomSheet`, `DEFAULT_FILTERS`, `countActiveFilters`, `FiltersState`, `FilterSort` dans le dépôt et confirmer qu'elles ne sont référencées que par `app/(app)/(tabs)/point/list.tsx` et par le fichier `FiltersBottomSheet.tsx` lui-même (condition de suppression sûre — D-10). Si une autre référence existe, la signaler dans le SUMMARY avant de supprimer en Task 3.</action>
  <verify>
    <automated>npx tsc --noEmit | grep -c "error TS" → 36 (baseline)</automated>
    <manual>Recherche : aucune référence à FiltersBottomSheet/DEFAULT_FILTERS/countActiveFilters/FilterSort hors list.tsx + FiltersBottomSheet.tsx</manual>
  </verify>
  <acceptance_criteria>
    - Baseline tsc enregistrée = 36 dans le SUMMARY
    - Confirmation écrite : FiltersBottomSheet + ses exports sont orphelins hors list.tsx → suppression sûre
  </acceptance_criteria>
  <done>Baseline 36 documentée ; condition de suppression de FiltersBottomSheet vérifiée.</done>
</task>

<task type="auto">
  <name>Task 2: Refonte PointListItem.tsx — ligne « table des matières »</name>
  <files>components/point/PointListItem.tsx</files>
  <read_first>
    - components/point/PointListItem.tsx (fichier complet — analog de lui-même ; note actuelle l. 78-86 rose 36, N°00X l. 34 + style l. 70-77, arrow l. 55 + style l. 112-116, container l. 60-69)
    - .planning/phases/04-listes-cercle/04-PATTERNS.md (l. 134-153 — assignations PointListItem)
    - .planning/phases/04-listes-cercle/04-UI-SPEC.md (§Typography Display/Heading/Eyebrow, §Color « note en T.text PAS rose » l. 118-119, §Liste point 4 l. 135-140, §Spacing rowGap 12 / paddingVertical 16)
  </read_first>
  <action>Refondre la ligne selon l'archétype. Supprimer le numéro séquentiel : retirer le `<Text>` `N°00X` (l. 34) et son style `number` (l. 70-77) ; la prop `index` devient inutilisée — la conserver dans l'interface (signature appelée par list.tsx) mais ne plus l'utiliser, OU la retirer si list.tsx est ajusté en Task 3 (cohérence à assurer entre les deux). Transformer la note en ancre Display : style `note` en `fontFamily: F.serifLight`, `fontStyle: 'italic'`, `fontSize: 44`, `lineHeight: 44`, `color: T.text` (PLUS `T.primary` — départ délibéré §Color), largeur d'ancre fixe ~64 (textAlign center) pour aligner les corps. Ajouter le dénominateur « /10 » en mono Eyebrow (`fontFamily: F.mono`, `fontSize: 10`, `color: T.textFaint`) discret à droite/sous le chiffre. Passer le commentaire de `fontSize: 17` à 20 (Heading), `fontFamily: F.serif` italic inchangé ; fallback « Sans commentaire » en `T.textFaint`. Passer le chevron `›` de `F.sansLight` (l. 113) à `F.mono` `T.textFaint`. Normaliser le spacing du container : `gap` 14 → `12` (rowGap), `paddingVertical` reste 16 (lg). Conserver le pattern tap (`PressableScale` + `haptics.tap()` + `router.push('/(app)/point/${point.id}')`) et la méta mono (date · @partenaire · adresse). Pattern thème `const T = useTheme(); const styles = useMemo(() => makeStyles(T), [T]);` inchangé. Aucun hex/fontFamily en dur.</action>
  <verify>
    <automated>npx tsc --noEmit | grep -c "error TS" → 36 (0 nouvelle)</automated>
    <manual>TestFlight dark+light : note grande en T.text (blanc dark / noir light, PAS rose), « /10 » mono discret, aucun N°00X, chevron mono</manual>
  </verify>
  <acceptance_criteria>
    - Style `note` : fontFamily F.serifLight, fontSize 44, color T.text (assertion source)
    - Aucune occurrence de `N°` ni du style `number` dans PointListItem.tsx
    - Dénominateur « /10 » rendu en F.mono / T.textFaint
    - `arrow` utilise F.mono (plus F.sansLight)
    - container `gap: 12`, `paddingVertical: 16`
    - Aucune occurrence de F.sans* / F.serifMedium dans le fichier
  </acceptance_criteria>
  <done>La ligne lit comme un sommaire : note T.text Display 44 ancrée, corps serif Heading 20, méta mono droite, chevron mono, filet T.border.</done>
</task>

<task type="auto">
  <name>Task 3: Refonte list.tsx (pills inline + sticky par mois + « Le carnet ») + suppression FiltersBottomSheet.tsx</name>
  <files>app/(app)/(tabs)/point/list.tsx, components/point/FiltersBottomSheet.tsx</files>
  <read_first>
    - app/(app)/(tabs)/point/list.tsx (imports l. 17-24 ; groupByMonth l. 34-62 KEEP verbatim ; état filtres l. 74-77 + 99-121 ; titre/stats l. 150-171 ; filter button l. 173-190 ; sticky l. 204 ; RefreshControl l. 205-212 ; <FiltersBottomSheet> l. 215-220 ; Snackbar l. 222-224 ; section header render l. 141-146 + styles l. 331-352)
    - .planning/phases/04-listes-cercle/04-PATTERNS.md (l. 31-77 assignations list.tsx ; l. 282-284 « No Analog » FilterPill)
    - .planning/phases/04-listes-cercle/04-RESEARCH.md (§Pattern 1 l. 142-159 état inline {minNote,sort} ; §Code Examples FilterPill l. 233-251 ; §Pitfall 4/5 stats row hors spec)
    - .planning/phases/04-listes-cercle/04-UI-SPEC.md (§Liste l. 127-144, §Filters Inline l. 190-207, §Copywriting Liste l. 232-243)
  </read_first>
  <action>Remplacer l'état couplé à FiltersBottomSheet (`filtersOpen`, `filters`, `DEFAULT_FILTERS`, `countActiveFilters`) par un état inline 2 champs : `const [minNote, setMinNote] = useState<MinNote>(0)` (type `MinNote = 0 | 5 | 7 | 9`) et `const [sort, setSort] = useState<Sort>('date')` (type `Sort = 'date' | 'note'`). Recalculer `sections` via `useMemo([points, minNote, sort])` : `points.filter(p => p.note >= minNote)` puis `groupByMonth(filtered, sort === 'note' ? 'note_desc' : 'date_desc')` — réutiliser `groupByMonth` (l. 34-62) VERBATIM. Le type `FilterSort` actuellement importé de FiltersBottomSheet doit être inliné/localisé dans list.tsx (littéraux 'date_desc'/'note_desc' passés à groupByMonth) AVANT la suppression du fichier. Retirer du bloc d'import (l. 17-24) : FiltersBottomSheet, DEFAULT_FILTERS, countActiveFilters, FiltersState, FilterSort, IcoFilter. Construire une rangée de pills inline dans `ListHeaderComponent` (sous le titre) — composant FilterPill local d'après §Code Examples RESEARCH : conteneur `borderRadius: T.pill` + `borderCurve: 'continuous'`, `paddingHorizontal: 12`, hauteur ≥ 44 ; ACTIVE = fond `T.primary` + label mono Eyebrow sur `T.text` ; INACTIVE = transparent + `borderWidth:1` `T.border` + label `T.textDim` ; tap → `haptics.select()`. Pills note min : libellés « Toutes »/« 5+ »/« 7+ »/« 9+ » mappés 0/5/7/9. Pills tri : « Date »/« Note » (toggle). Activer les headers de mois sticky : retirer `stickySectionHeadersEnabled={false}` (l. 204) ou passer `true` ; garder les pills dans `ListHeaderComponent` (elles scrollent, seuls les mois collent). Changer le titre « Vos moments » → « Le carnet » (Title 36 `F.serifLight` italic). Supprimer la rangée de stats « Entrées / Moy. » (l. 150-171) et le `TouchableOpacity` de filtre (l. 173-190) et le `<FiltersBottomSheet>` (l. 215-220) — hors UI-SPEC (Pitfall 5). Conserver (D-04) : `RefreshControl` pull-to-refresh, `SkeletonRow` au chargement, `Snackbar` erreur réseau (copie « Impossible de charger les moments. Réessayez. »). Empty state : titre « Le carnet est vide. » + corps « Posez votre premier moment sur la carte. ». Enfin, SUPPRIMER le fichier `components/point/FiltersBottomSheet.tsx` (D-10) une fois tous les imports retirés. Pattern thème + `useSafeAreaInsets()` inchangés.</action>
  <verify>
    <automated>npx tsc --noEmit | grep -c "error TS" → 36 (0 nouvelle, y compris après suppression du fichier)</automated>
    <manual>TestFlight dark+light : titre « Le carnet » ; pills inline (toggle « 9+ » → seules notes ≥9 ; « Note » → tri par note desc dans le mois) ; headers de mois collants au scroll ; pull-to-refresh + skeleton + snackbar erreur OK ; aucune ouverture de sheet de filtre</manual>
  </verify>
  <acceptance_criteria>
    - list.tsx n'importe plus FiltersBottomSheet/DEFAULT_FILTERS/countActiveFilters/FiltersState/FilterSort/IcoFilter
    - État de filtre = `minNote` + `sort` ; `sections` via groupByMonth(filtered, 'note_desc'|'date_desc')
    - `stickySectionHeadersEnabled` n'est plus à false (mois sticky)
    - Titre rendu = « Le carnet » en F.serifLight
    - Aucune rangée de stats « Entrées/Moy. » ; FilterPill rendu en T.pill + borderCurve continuous, active = T.primary
    - components/point/FiltersBottomSheet.tsx N'EXISTE PLUS
    - Copie exacte UI-SPEC : « Le carnet », « Toutes/5+/7+/9+ », « Date/Note », « Le carnet est vide. », « Impossible de charger les moments. Réessayez. »
  </acceptance_criteria>
  <done>L'écran liste est un sommaire groupé par mois (sticky), filtré par pills inline, sans sheet ; FiltersBottomSheet supprimé ; D-04 préservé.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Supabase (read) | Lecture des moments via `usePoints.fetchMyPoints` (RLS sur `points`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Information Disclosure | `usePoints.fetchMyPoints` / `points` | mitigate | RLS existante sur `points` (créateur + partenaires consentants) ; cet écran ne fait que lire+transformer côté présentation, aucune nouvelle requête ni nouveau champ exposé. |
| T-04-01-02 | Tampering | `is_visible` (points) | accept | Aucune écriture sur `points` dans ce plan ; `is_visible` reste piloté server-side par trigger. Pas de surface d'attaque ajoutée. |
| T-04-01-SC | Tampering | npm installs | accept | Zéro nouvelle dépendance (UI-SPEC « zéro nouvelle dépendance ») — aucun install, pas de slopcheck requis. |
</threat_model>

<verification>
- `npx tsc --noEmit` → 36 erreurs (0 nouvelle sur la baseline) après chaque task.
- Manuel TestFlight (dark + light) : sommaire par mois sticky, note T.text Display 44, pills inline correctes, D-04 préservé, aucun sheet.
- `git push` + build natif (OTA inopérant) selon workflow CLAUDE.md.
</verification>

<success_criteria>
- UI-05 : `point/list` adopte l'archétype « table des matières » (note ancre serif T.text, méta mono droite, filet), groupé par mois sticky, filtres inline en pills.
- `FiltersBottomSheet.tsx` supprimé, plus aucun import résiduel.
- D-04 préservé (pull-to-refresh, skeleton, snackbar). 0 nouvelle erreur tsc.
</success_criteria>

<output>
Create `.planning/phases/04-listes-cercle/04-01-SUMMARY.md` when done (inclure la baseline tsc = 36 pour référence des plans 02/03).
</output>
