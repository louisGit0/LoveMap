---
phase: 04-listes-cercle
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - "app/(app)/(tabs)/friends/index.tsx"
  - "components/friends/FriendItem.tsx"
autonomous: true
requirements: [UI-06]
must_haves:
  truths:
    - "Chaque ami s'affiche en entrée d'annuaire : avatar carré (borderRadius:0) à initiale serif italic rose, nom serif italic Heading 20, @username mono à droite"
    - "L'action « Carte » est un bouton underline T.primary qui ouvre la map en vue ami (comportement existant conservé)"
    - "L'action « Retirer » est un texte mono Eyebrow T.danger qui déclenche une Alert éditoriale « Retirer du cercle ? » (haptics.warn à l'ouverture) puis useFriends.unfriend"
    - "Le titre d'écran est « Le cercle » en Title 36 F.serifLight italic et la recherche est un champ underline (placeholder « Rechercher dans le cercle »)"
    - "pull-to-refresh, skeleton et le debounce 300ms de search_users restent fonctionnels ; un retrait affiche « Retiré du cercle. » (succès) ou « Échec — réessayez. » + haptics.error (échec)"
  artifacts:
    - path: "components/friends/FriendItem.tsx"
      provides: "Ligne annuaire — avatar carré rose, nom serif Heading 20, « Carte » underline T.primary, « Retirer » mono T.danger + Alert éditoriale"
      contains: "Retirer du cercle ?"
    - path: "app/(app)/(tabs)/friends/index.tsx"
      provides: "Écran « Le cercle » : titre Title 36 serifLight, recherche underline (ui/Input), snackbars éditoriaux de retrait"
      contains: "Le cercle"
  key_links:
    - from: "app/(app)/(tabs)/friends/index.tsx"
      to: "useFriends.unfriend"
      via: "handleUnfriend (déjà câblé)"
      pattern: "unfriend\\("
    - from: "components/friends/FriendItem.tsx"
      to: "onUnfriend / onViewMap"
      via: "props d'action de ligne"
      pattern: "onUnfriend"
---

<objective>
Refondre l'écran « le cercle » (`friends/index`, UI-06) en annuaire intime selon l'archétype « table des matières » : avatar carré à initiale serif rose, nom serif, `@username` mono à droite, action « Carte » underline, recherche underline, titre « Le cercle ». Câbler l'action « retirer du cercle » (D-06) en restyle + copie éditoriale + haptiques sur le hook `unfriend` DÉJÀ implémenté (pas de nouvelle plomberie data).

Purpose: UI-06 — le cercle doit lire comme un annuaire éditorial (initiale rose, nom serif italic, métadonnées mono alignées droite, filet de séparation), avec un retrait d'ami opt-in confirmé au ton « journal intime », cohérent avec la discipline typo serif+mono / 2 graisses de la Phase 3.
Output: `FriendItem.tsx` refondu (annuaire + « Retirer » T.danger + Alert éditoriale), `friends/index.tsx` refondu (titre « Le cercle », recherche underline, snackbars de retrait). Aucune nouvelle dépendance, aucun nouvel appel Supabase en composant.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-listes-cercle/04-CONTEXT.md
@.planning/phases/04-listes-cercle/04-UI-SPEC.md
@.planning/phases/04-listes-cercle/04-PATTERNS.md
@.planning/phases/04-listes-cercle/04-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Tokens & primitives — utiliser directement, ne pas hardcoder. -->
constants/theme.ts (T): T.text, T.textDim, T.textFaint, T.primary, T.danger, T.border, T.surface2, T.bg
constants/fonts.ts (F): F.serifLight (300 italic), F.serif (400 italic), F.mono (400) — F.sans*/F.serifMedium PROSCRITS sur cet écran
lib/haptics.ts: haptics.warn() (ouverture Alert retrait), haptics.error() (échec retrait) — fire-and-forget, jamais await
components/ui/Input.tsx: champ underline (borderBottomWidth:1, borderBottomColor T.border) — recherche
components/ui/Button.tsx: variant "underline" (« Carte »)
components/ui/SkeletonItem.tsx: SkeletonRow (loading) — préservé (D-04)
components/ui/PressableScale.tsx: feedback scale spring sur tap de ligne
hooks/useFriends.ts: useFriends().unfriend(friendshipId) DÉJÀ implémenté (delete friendships + removeFriend du store) — réutiliser tel quel
stores/friendStore.ts: removeFriend (retrait optimiste déjà appelé par unfriend) — ne pas re-fetch après retrait
types/app.types.ts: FriendWithProfile { id (friendship.id), profile { username, display_name, ... } }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refonte FriendItem.tsx — entrée d'annuaire + « Retirer » T.danger + Alert éditoriale (D-05/D-06)</name>
  <files>components/friends/FriendItem.tsx</files>
  <read_first>
    - components/friends/FriendItem.tsx (fichier complet — analog de lui-même : Alert handleUnfriend l. 21-30 ; avatar carré rose l. 34-36 + style l. 62-76 ; displayName F.sans 14 l. 38 + style l. 78-82 ; « Carte » l. 41-45 + style l. 90-102 ; « Retirer » l. 46-48 + removeBtnText style l. 103-115)
    - components/friends/FriendRequestItem.tsx (layout avatar/info jumeau — référence de cohérence d'archétype)
    - .planning/phases/04-listes-cercle/04-PATTERNS.md (l. 156-181 — assignations FriendItem ; pattern Alert éditoriale verbatim l. 160-173)
    - .planning/phases/04-listes-cercle/04-UI-SPEC.md (§Le cercle l. 147-163, §Destructive l. 210-225, §Typography Heading/Eyebrow, §Color « initiale rose / Retirer T.danger », §Copywriting Le cercle l. 245-257)
    - constants/theme.ts, constants/fonts.ts (tokens — ne rien hardcoder)
  </read_first>
  <action>Refondre la ligne en entrée d'annuaire. Conserver l'avatar carré INCHANGÉ (D-05 lock) : `borderRadius:0`, fond `T.surface2`, bord `T.border`, initiale serif italic `T.primary` (Heading ~20) — ne pas l'arrondir. Passer le `displayName` de `F.sans` 14 (l. 38, style l. 78-82) à `F.serif` italic 20 (Heading), couleur `T.text`. Afficher le `@username` en mono Eyebrow (`fontFamily: F.mono`, `fontSize: 10`, `color: T.textFaint`) aligné à droite (signature éditoriale). Transformer l'action « Carte » (l. 41-45) en bouton underline `T.primary` (utiliser `components/ui/Button variant="underline"` OU texte mono Eyebrow + filet underline) ; conserver le callback `onViewMap` existant (navigation map vue ami — comportement inchangé). Transformer « Retirer » : `removeBtnText` passe de `T.textFaint` à `T.danger` (l. 103-115), texte mono Eyebrow. Mettre à jour `handleUnfriend` (l. 21-30) : ajouter `haptics.warn()` à l'ouverture, et passer la copie en éditorial — titre « Retirer du cercle ? », corps « {profile.display_name ?? profile.username} ne verra plus vos moments partagés. Cette action est irréversible. », actions `[ { text: 'Garder', style: 'cancel' }, { text: 'Retirer', style: 'destructive', onPress: onUnfriend } ]`. NE PAS appeler Supabase dans le composant : l'exécution du delete reste déléguée à `onUnfriend` (qui appelle `useFriends.unfriend` côté écran). Conserver le null guard sur `profile`. Normaliser le spacing : `paddingVertical` ligne = 16 (lg), gap ancre→corps = 12 (rowGap) ; filet `borderBottom` `T.border`. Pattern thème `const T = useTheme(); const styles = useMemo(() => makeStyles(T), [T]);` inchangé. Aucun hex/fontFamily en dur ; aucune occurrence de `F.sans*`/`F.serifMedium`.</action>
  <verify>
    <automated>npx tsc --noEmit | grep -c "error TS" → 36 (0 nouvelle sur la baseline capturée en 04-01 / 04-VALIDATION)</automated>
    <manual>TestFlight dark+light : avatar carré initiale rose, nom serif italic 20, @username mono à droite ; « Carte » underline rose ouvre la map en vue ami ; « Retirer » rouge (T.danger) ouvre l'Alert « Retirer du cercle ? » (haptique warn) ; « Garder » garde la ligne, « Retirer » la supprime</manual>
  </verify>
  <acceptance_criteria>
    - Avatar reste carré (borderRadius:0), fond T.surface2, initiale via fontFamily F.serif/F.serifLight italic en T.primary
    - `displayName` rendu en fontFamily F.serif (italic), fontSize 20, color T.text (plus F.sans 14)
    - `@username` rendu en F.mono, fontSize 10, T.textFaint, aligné à droite
    - « Carte » = action underline T.primary (variant underline OU texte mono + filet), callback onViewMap conservé
    - `removeBtnText` color = T.danger (plus T.textFaint), F.mono
    - `handleUnfriend` contient `haptics.warn()`, titre exact « Retirer du cercle ? », corps « … ne verra plus vos moments partagés. Cette action est irréversible. », actions [ Garder (cancel) · Retirer (destructive → onUnfriend) ]
    - Aucun appel `supabase.` dans FriendItem.tsx ; aucune occurrence de F.sans*/F.serifMedium
  </acceptance_criteria>
  <done>FriendItem lit comme une entrée d'annuaire : initiale rose carrée, nom serif Heading 20, @username mono droite, « Carte » underline rose, « Retirer » T.danger ouvrant une Alert éditoriale (haptics.warn) qui délègue à onUnfriend.</done>
</task>

<task type="auto">
  <name>Task 2: Refonte friends/index.tsx — titre « Le cercle », recherche underline, snackbars de retrait éditoriaux (D-05/D-06)</name>
  <files>app/(app)/(tabs)/friends/index.tsx</files>
  <read_first>
    - app/(app)/(tabs)/friends/index.tsx (fichier complet — analog de lui-même : debounce search_users l. 68-77 KEEP ; handleUnfriend l. 91-94 + render FriendItem l. 191-200 ; titre « le cercle » l. 102-106 ; recherche raw TextInput + IcoSearch l. 112-123 ; cercleSectionRow eyebrow l. 184-187 + style l. 407-429 ; déviation rule-4 loadFriends l. 49-56 — NE PAS étendre)
    - .planning/phases/04-listes-cercle/04-PATTERNS.md (l. 81-101 — assignations friends/index ; l. 272-276 déviations rule-4 à ne pas copier)
    - .planning/phases/04-listes-cercle/04-RESEARCH.md (§Pitfall 6 recherche raw→ui/Input l. 227-229 ; §Anti-Patterns « ne pas re-fetch après unfriend » l. 184)
    - .planning/phases/04-listes-cercle/04-UI-SPEC.md (§Le cercle l. 147-163, §Destructive l. 210-225, §Typography Title/Heading/Eyebrow, §Copywriting Le cercle l. 245-257)
  </read_first>
  <action>Refondre la présentation de l'écran sans toucher la logique data. Passer le titre « le cercle » (l. 102-106) à « Le cercle » en Title 36 `F.serifLight` italic (poser via `fontFamily: F.serifLight` dans makeStyles — NE PAS se reposer sur `AppText variant="title"` qui mappe F.serif 400 ; cf. caveat AppText). Remplacer la recherche raw `TextInput` + `IcoSearch` (l. 112-123) par `components/ui/Input` (underline only — `borderBottomWidth:1`, `borderBottomColor: T.border`, pas de fond, pas d'icône Material) avec placeholder « Rechercher dans le cercle ». CONSERVER tel quel le debounce 300ms + la RPC `search_users` (l. 68-77) — ne brancher que la présentation sur le nouveau champ. Étoffer `handleUnfriend` (l. 91-94) en feedback éditorial : sur succès → `setSnackbar('Retiré du cercle.')` ; sur échec → `setSnackbar('Échec — réessayez.')` + `haptics.error()`. NE PAS ajouter de `fetchFriends` après `unfriend` — `removeFriend` du store fait déjà le retrait optimiste (anti-pattern RESEARCH). Conserver le pattern de rendu `<FriendItem friend={item} onUnfriend={() => handleUnfriend(item.id)} onViewMap={...} />` (l. 191-200). Conserver (D-04) pull-to-refresh, `SkeletonRow` au chargement, null guards. Empty states (copie exacte) : titre « Votre cercle est vide. » + corps « Cherchez un nom pour inviter quelqu'un. » ; recherche sans résultat « Aucun nom ne correspond. ». NE PAS modifier ni étendre la déviation rule-4 de `loadFriends` (l. 49-56) — hors périmètre cette phase. Pattern thème + `useSafeAreaInsets()` (jamais SafeAreaView) inchangés. Aucun hex/fontFamily en dur ; aucune occurrence de `F.sans*`/`F.serifMedium`.</action>
  <verify>
    <automated>npx tsc --noEmit | grep -c "error TS" → 36 (0 nouvelle)</automated>
    <manual>TestFlight dark+light : titre « Le cercle » grand serif italic ; recherche underline (placeholder « Rechercher dans le cercle ») → tape un nom, résultats via search_users ; retirer un ami → ligne disparaît instantanément + snackbar « Retiré du cercle. » ; en cas d'échec réseau → « Échec — réessayez. » + haptique error</manual>
  </verify>
  <acceptance_criteria>
    - Titre rendu = « Le cercle » via fontFamily F.serifLight (pas variant="title" seul)
    - Recherche = composant ui/Input underline, placeholder « Rechercher dans le cercle » ; debounce 300ms + RPC search_users inchangés
    - `handleUnfriend` : succès → snackbar « Retiré du cercle. » ; échec → snackbar « Échec — réessayez. » + haptics.error()
    - Aucun `fetchFriends`/re-fetch ajouté après unfriend (retrait optimiste du store conservé)
    - Empty states exacts : « Votre cercle est vide. » / « Cherchez un nom pour inviter quelqu'un. » / « Aucun nom ne correspond. »
    - `loadFriends` (l. 49-56) inchangé (déviation rule-4 non étendue) ; aucune occurrence de F.sans*/F.serifMedium
  </acceptance_criteria>
  <done>L'écran « Le cercle » est un annuaire éditorial : titre serifLight, recherche underline, retrait d'ami confirmé avec snackbars « journal intime » et retrait optimiste ; D-04 préservé, zéro nouvel appel Supabase en composant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Supabase (read) | Recherche d'utilisateurs via RPC `search_users` (exclut date_of_birth/push_token) |
| client → Supabase (delete) | Retrait d'ami via `useFriends.unfriend` (delete sur `friendships` sous RLS) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Elevation of Privilege | `useFriends.unfriend` / `friendships` | mitigate | Delete par `friendship.id` ; la RLS `friendships` (relation bidirectionnelle requester/addressee) scope la suppression aux lignes auxquelles l'utilisateur participe. Opération mono-table → pas de récursion croisée de policy (règle 18, pas de `42P17`). Jamais de service key côté client. |
| T-04-02-02 | Information Disclosure | RPC `search_users` | mitigate | La RPC existante exclut déjà `date_of_birth` et `push_token` ; aucun nouveau champ de profil exposé par la refonte. |
| T-04-02-03 | Tampering | `is_visible` (points) | accept | Ce plan n'écrit pas sur `points` ni `point_partners` ; `is_visible` reste piloté server-side par trigger. Aucune surface d'attaque ajoutée. |
| T-04-02-SC | Tampering | npm installs | accept | Zéro nouvelle dépendance (UI-SPEC « zéro nouvelle dépendance ») — aucun install, pas de slopcheck requis. |
</threat_model>

<verification>
- `npx tsc --noEmit` → 36 erreurs (0 nouvelle sur la baseline capturée en 04-01 / documentée 04-VALIDATION) après chaque task.
- Manuel TestFlight (dark + light) : annuaire (avatar carré rose, nom serif, @ mono, « Carte » underline), recherche underline, retrait d'ami avec Alert éditoriale + snackbars + retrait optimiste, D-04 préservé.
- `git push` + build natif (OTA inopérant) selon workflow CLAUDE.md.
</verification>

<success_criteria>
- UI-06 : `friends/index` adopte l'archétype annuaire (avatar carré rose, nom serif Heading 20, @username mono droite, « Carte » underline, recherche underline, titre « Le cercle »).
- D-06 : « retirer du cercle » câblé via `useFriends.unfriend` (existant), confirmation Alert éditoriale « Retirer du cercle ? » (T.danger, haptics.warn), snackbars succès/échec, retrait optimiste.
- Aucun nouvel appel Supabase en composant ; 0 nouvelle erreur tsc.
</success_criteria>

<output>
Create `.planning/phases/04-listes-cercle/04-02-SUMMARY.md` when done.
</output>
