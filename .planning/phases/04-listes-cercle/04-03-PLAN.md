---
phase: 04-listes-cercle
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - "hooks/useFriends.ts"
  - "components/friends/FriendRequestItem.tsx"
  - "app/(app)/(tabs)/friends/requests.tsx"
autonomous: true
requirements: [UI-07]
must_haves:
  truths:
    - "useFriends exporte une méthode respondToTag(pointPartnerId, accept): Promise<boolean> qui met à jour status sur point_partners (mono-table, sans toucher is_visible)"
    - "FriendRequestItem rend des boutons texte (solid/coral affirmatif + ghost négatif) et accepte des props pour mapper les labels amitié (Accepter/Refuser) vs taguage (Sceller/Décliner)"
    - "L'écran Demandes affiche deux sections principales à eyebrow mono : « DEMANDES REÇUES » (amitié) et « TAGUAGES EN ATTENTE » (consentement partenaire)"
    - "Répondre à un taguage se fait INLINE via Sceller/Décliner (appel respondToTag) — plus de navigation « Répondre → » vers le détail du point"
    - "Une 3e section discrète « Envoyées » est conservée sous les deux principales ; une section vide masque son header ; deux sections principales vides → empty state « Pas de page en attente. »"
  artifacts:
    - path: "hooks/useFriends.ts"
      provides: "Méthode hook respondToTag — update mono-table point_partners.status, RLS-safe (mig 010/011), is_visible via trigger only"
      contains: "respondToTag"
    - path: "components/friends/FriendRequestItem.tsx"
      provides: "Ligne demande — nom serif Heading 20, boutons texte solid/ghost paramétrés (amitié vs taguage)"
      contains: "variant"
    - path: "app/(app)/(tabs)/friends/requests.tsx"
      provides: "Écran Demandes : 2 sections eyebrow mono + Envoyées discrète, Sceller/Décliner inline, empty « Pas de page en attente. »"
      contains: "TAGUAGES EN ATTENTE"
  key_links:
    - from: "app/(app)/(tabs)/friends/requests.tsx"
      to: "useFriends.respondToTag"
      via: "handleRespondTag (jumeau de handleRespond)"
      pattern: "respondToTag\\("
    - from: "hooks/useFriends.ts"
      to: "point_partners"
      via: "update status (mono-table)"
      pattern: "point_partners"
---

<objective>
Refondre l'écran Demandes (`friends/requests`, UI-07) selon l'archétype « table des matières » avec deux sections à eyebrow mono (« DEMANDES REÇUES » amitié + « TAGUAGES EN ATTENTE » consentement partenaire) et boutons texte (solid/ghost). Ajouter la réponse INLINE aux taguages (D-08) via une fine méthode hook `respondToTag` (mono-table `point_partners`, RLS-safe), sans naviguer vers le détail du point. Conserver une 3e section discrète « Envoyées » (D-09).

Purpose: UI-07 — les demandes doivent lire comme des entrées éditoriales (avatar carré rose, nom serif italic, actions texte « sceller / décliner »), avec le consentement de taguage répondu directement sur la ligne — ce qui impose d'extraire la logique de consentement (aujourd'hui inline dans `point/[id].tsx`) en méthode hook (règle 4 : pas de Supabase en composant).
Output: `respondToTag` ajouté à `useFriends.ts` ; `FriendRequestItem.tsx` refondu (boutons texte paramétrés) ; `requests.tsx` refondu (2 sections + Envoyées, Sceller/Décliner inline, empty states). Aucune nouvelle dépendance.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-listes-cercle/04-CONTEXT.md
@.planning/phases/04-listes-cercle/04-UI-SPEC.md
@.planning/phases/04-listes-cercle/04-PATTERNS.md
@.planning/phases/04-listes-cercle/04-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Tokens & primitives — utiliser directement, ne pas hardcoder. -->
constants/theme.ts (T): T.text, T.textDim, T.textFaint, T.primary, T.border, T.surface2, T.radiusSm (12), T.bg
constants/fonts.ts (F): F.serifLight (300 italic), F.serif (400 italic), F.mono (400) — F.sans*/F.serifMedium PROSCRITS sur cet écran
lib/haptics.ts: haptics.success() (accepter/sceller), haptics.warn() (refuser/décliner), haptics.error() (échec) — fire-and-forget
components/ui/Button.tsx: variants "solid"/"coral" (affirmatif rempli), "ghost" (négatif bordé) — base borderRadius:0 + F.sansMedium ⇒ override style pour D-12 radiusSm + borderCurve, et label local serif/mono (F.sans* proscrit)
components/ui/SkeletonItem.tsx: SkeletonRow (loading)
hooks/useFriends.ts: respondToRequest (l. 47-61), unfriend (l. 63-76), sendFriendRequest, bloc return l. 78-88 — AJOUTER respondToTag au return
lib/supabase.ts: supabase (client) — accès UNIQUEMENT depuis le hook, jamais le composant
types: pendingReceived (amitié), taguages en attente via point_partners + is_pending_partner (mig 010/011)

<!-- Contrat de la nouvelle méthode hook (interface-first — Task 1 la définit avant que requests.tsx la consomme en Task 3) -->
respondToTag(pointPartnerId: string, accept: boolean): Promise<boolean>
  // update point_partners.set({ status: accept ? 'accepted' : 'rejected', responded_at: now }).eq('id', pointPartnerId)
  // is_visible flips server-side via trigger on_partner_consent — JAMAIS côté client
  // return false + console.error('[useFriends] respondToTag error:', error.message) si erreur ; true sinon
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Ajouter respondToTag à useFriends.ts — contrat hook de consentement de taguage (D-08, interface-first)</name>
  <files>hooks/useFriends.ts</files>
  <read_first>
    - hooks/useFriends.ts (fichier complet — analogs de forme : respondToRequest l. 47-61, unfriend l. 63-76 ; bloc return l. 78-88 où ajouter la méthode)
    - app/(app)/point/[id].tsx (handleConsent l. 153-163 — logique de consentement à extraire : update point_partners status + responded_at ; ne PAS copier l'usage direct de supabase dans un composant)
    - .planning/phases/04-listes-cercle/04-PATTERNS.md (l. 197-224 — assignations respondToTag, snippet de référence l. 215-222)
    - .planning/phases/04-listes-cercle/04-RESEARCH.md (§Code Examples respondToTag l. 254-266 ; §Pitfall 3 l. 215-217 ; Security Domain V4 l. 352)
    - CLAUDE.md (règle 4 : pas de Supabase en composant ; règle 18 : mono-table, pas de récursion croisée ; règle DB : is_visible via trigger uniquement)
  </read_first>
  <action>Ajouter une méthode `respondToTag` à `useFriends` calquée sur `respondToRequest`/`unfriend` (même fichier) : `const respondToTag = useCallback(async (pointPartnerId: string, accept: boolean): Promise<boolean> => { ... }, [])`. Corps : `supabase.from('point_partners').update({ status: accept ? 'accepted' : 'rejected', responded_at: new Date().toISOString() }).eq('id', pointPartnerId)`. Sur erreur → `console.error('[useFriends] respondToTag error:', error.message); return false;`. Sinon `return true`. NE JAMAIS écrire `is_visible` côté client — le trigger `on_partner_consent` le bascule server-side (règle DB CLAUDE.md). C'est une mise à jour MONO-TABLE sur `point_partners` → RLS-safe (mig 010/011 donnent au partenaire tagué l'accès à sa ligne), pas de récursion croisée de policy (règle 18 OK). Exposer `respondToTag` dans le bloc `return { ... }` (l. 78-88) à côté des méthodes existantes. Ne pas modifier `respondToRequest`/`unfriend`/`sendFriendRequest`. (Co-localiser dans `useFriends` car `requests.tsx` importe déjà ce hook — moindre friction.) Pas de hardcode, types stricts (pas de `any`).</action>
  <verify>
    <automated>npx tsc --noEmit | grep -c "error TS" → 36 (0 nouvelle ; respondToTag typé Promise<boolean>)</automated>
    <manual>Lecture source : respondToTag présent dans le bloc return de useFriends ; update point_partners.status uniquement (+ responded_at), aucune écriture is_visible</manual>
  </verify>
  <acceptance_criteria>
    - `useFriends` exporte `respondToTag(pointPartnerId: string, accept: boolean): Promise<boolean>`
    - Corps = update mono-table sur `point_partners` (status accepted/rejected + responded_at), `.eq('id', pointPartnerId)`
    - Aucune écriture sur `is_visible` ; aucune sous-requête cross-table (mono-table, règle 18)
    - Erreur loggée via `console.error('[useFriends] respondToTag error:', error.message)`, return false ; succès return true
    - respondToTag présent dans le bloc `return {}` du hook ; respondToRequest/unfriend inchangés
  </acceptance_criteria>
  <done>useFriends expose un contrat respondToTag mono-table, RLS-safe, qui ne touche jamais is_visible — prêt à être consommé inline par requests.tsx (Task 3).</done>
</task>

<task type="auto">
  <name>Task 2: Refonte FriendRequestItem.tsx — boutons texte paramétrés (amitié vs taguage) + nom serif Heading (D-07)</name>
  <files>components/friends/FriendRequestItem.tsx</files>
  <read_first>
    - components/friends/FriendRequestItem.tsx (fichier complet — analog de lui-même : null guard l. 22 ; avatar carré rose l. 28-30 + style l. 56-70 ; name F.sans 14 l. 32 + style l. 72-76 ; boutons-icônes acceptBtn/rejectBtn l. 35-42 render + styles l. 84-102 ; import IcoCheck/IcoClose l. 5)
    - components/friends/FriendItem.tsx (jumeau d'archétype après sa refonte 04-02 — cohérence avatar/nom)
    - components/ui/Button.tsx (variants solid/coral/ghost ; caveat base borderRadius:0 + F.sansMedium ⇒ override D-12 + label local serif/mono)
    - .planning/phases/04-listes-cercle/04-PATTERNS.md (l. 183-194 — assignations FriendRequestItem ; §Buttons caveat l. 256-258)
    - .planning/phases/04-listes-cercle/04-UI-SPEC.md (§Demandes l. 166-187 tableau boutons texte l. 177-181, §Typography Heading/Eyebrow, §Color, rayons D-12 radiusSm)
  </read_first>
  <action>Refondre la ligne en entrée éditoriale paramétrable. Conserver le null guard (`if (!profile) return null;`) et l'avatar carré rose (l. 28-30, style l. 56-70) — identique à FriendItem (borderRadius:0, T.surface2, initiale serif italic T.primary). Passer le `name` de `F.sans` 14 (l. 32, style l. 72-76) à `F.serif` italic 20 (Heading), `T.text`. Remplacer les boutons-icônes (`IcoCheck`/`IcoClose`, render l. 35-42, styles l. 84-102) par DEUX boutons texte : affirmatif rempli (`Button variant="solid"` ou `"coral"`) + négatif bordé (`Button variant="ghost"`). Retirer l'import `IcoCheck`/`IcoClose` (l. 5) s'il n'est plus utilisé. PARAMÉTRER les labels via props pour servir les deux contextes (D-08) : ajouter des props (ex. `affirmLabel`, `negativeLabel`, et callbacks `onAffirm`/`onNegative`) afin que le même composant rende « Accepter »/« Refuser » (amitié) OU « Sceller »/« Décliner » (taguage). Appliquer les rayons D-12 aux boutons remplis/bordés : `T.radiusSm` (12) + `borderCurve: 'continuous'` (override de style sur Button dont la base est borderRadius:0). Le label affirmatif est serif italic Heading ; le label négatif est mono Eyebrow uppercase `T.textDim` (bord `T.border`) — NE PAS utiliser `F.sans*` (proscrit) : si la base `Button` impose F.sansMedium, fournir un label local serif/mono. `gap` entre boutons = 8 (sm). Pattern thème `const T = useTheme(); const styles = useMemo(() => makeStyles(T), [T]);` inchangé. Aucun hex/fontFamily en dur.</action>
  <verify>
    <automated>npx tsc --noEmit | grep -c "error TS" → 36 (0 nouvelle ; props de labels typées)</automated>
    <manual>TestFlight dark+light : avatar carré rose + nom serif italic 20 ; deux boutons TEXTE (affirmatif rempli rose + négatif ghost bordé), labels variables selon le contexte ; rayons arrondis iOS (borderCurve continuous) ; aucun bouton-icône résiduel</manual>
  </verify>
  <acceptance_criteria>
    - `name` rendu en fontFamily F.serif (italic), fontSize 20, T.text (plus F.sans 14)
    - Boutons d'action = Button variant solid/coral (affirmatif) + ghost (négatif) ; plus de IcoCheck/IcoClose (import retiré si orphelin)
    - Props de paramétrage des labels présentes (affirmatif/négatif) → composant réutilisable amitié ET taguage
    - Boutons remplis/bordés : T.radiusSm + borderCurve:'continuous' (D-12)
    - Label affirmatif serif italic Heading ; label négatif mono Eyebrow T.textDim ; aucun F.sans*/F.serifMedium
    - Avatar carré rose et null guard conservés
  </acceptance_criteria>
  <done>FriendRequestItem rend des boutons texte paramétrés (solid/ghost, rayons D-12) servant aussi bien Accepter/Refuser que Sceller/Décliner, avec nom serif Heading 20 et avatar carré rose.</done>
</task>

<task type="auto">
  <name>Task 3: Refonte requests.tsx — 2 sections eyebrow + Envoyées discrète, Sceller/Décliner inline via respondToTag, empty states (D-07/D-08/D-09)</name>
  <files>app/(app)/(tabs)/friends/requests.tsx</files>
  <read_first>
    - app/(app)/(tabs)/friends/requests.tsx (fichier complet — analogs : handleRespond l. 116-129 JUMEAU pour handleRespondTag ; sections Taguages l. 160-198 / Reçues l. 200-219 / Envoyées l. 221-252 ; tagItem note-box + « Répondre → » nav l. 191-195 à remplacer ; sectionHeader/sectionEyebrow l. 161-164 + styles l. 291-313 ; handleCancel direct-Supabase l. 131-139 — déviation rule-4 à NE PAS copier)
    - .planning/phases/04-listes-cercle/04-PATTERNS.md (l. 105-132 — assignations requests.tsx ; handleRespond analog l. 109-120 ; anti-pattern handleCancel l. 130)
    - .planning/phases/04-listes-cercle/04-UI-SPEC.md (§Demandes l. 166-187, §Copywriting Demandes l. 259-274, §Typography, rayons D-12)
    - hooks/useFriends.ts (respondToTag ajouté en Task 1 — à importer/consommer)
  </read_first>
  <action>Restructurer l'écran en deux sections principales à eyebrow mono + une 3e discrète. Titre « Demandes » (Title 36 `F.serifLight` italic). Section A — eyebrow « DEMANDES REÇUES » (amitié, source `pendingReceived`) : lignes `FriendRequestItem` avec labels « Accepter » (affirmatif) / « Refuser » (ghost), branchées sur `handleRespond` existant (l. 116-129, inchangé). Section B — eyebrow « TAGUAGES EN ATTENTE » (consentement partenaire, source `point_partners` en attente via `is_pending_partner`) : remplacer le `tagItem` actuel à note-box + CTA navigation « Répondre → » (l. 191-195) par l'archétype avatar+nom + DEUX boutons texte INLINE « Sceller » (affirmatif) / « Décliner » (ghost) — SUPPRIMER le `router.push` vers `point/[id]`. Ajouter `handleRespondTag(pointPartnerId, accept)` calqué sur `handleRespond` : `const ok = await respondToTag(pointPartnerId, accept)` (méthode hook de Task 1) ; si ok → `accept ? haptics.success() : haptics.warn()`, snackbar `accept ? 'Page scellée.' : 'Taguage refusé.'`, puis `await loadRequests()` ; sinon → snackbar « Action impossible. Réessayez. » + `haptics.error()`. Section C — « Envoyées » (D-09) : CONSERVER comme 3e bloc discret sous les deux principales (eyebrow secondaire, moins d'accent). Si « Envoyées » garde une action « Annuler », NE PAS copier le `handleCancel` direct-Supabase (l. 131-139, déviation rule-4) ; le laisser tel quel cette phase OU le router via un hook `useFriends.cancelRequest()` SI trivial — sinon le signaler comme déviation existante non corrigée dans le SUMMARY (ne pas l'étendre). Empty states (D-07) : si une section principale est vide → masquer entièrement son header (pas de section vide affichée) ; si les DEUX sections principales sont vides → empty state unique centré « Pas de page en attente. ». Headers de section = eyebrow mono uppercase + filet (réutiliser le style existant l. 291-313). Conserver `SkeletonRow` au chargement et `Snackbar`. Pattern thème + `useSafeAreaInsets()` inchangés. Aucun appel `supabase.` AJOUTÉ dans le composant pour le taguage (passe par `respondToTag`). Aucun hex/fontFamily en dur ; aucune occurrence de `F.sans*`/`F.serifMedium`.</action>
  <verify>
    <automated>npx tsc --noEmit | grep -c "error TS" → 36 (0 nouvelle)</automated>
    <manual>TestFlight dark+light : deux eyebrows « DEMANDES REÇUES » + « TAGUAGES EN ATTENTE » + 3e « Envoyées » discrète ; accepter/refuser une demande (haptique+snackbar) ; sceller un taguage INLINE → « Page scellée. » + haptics.success (pas de navigation vers le détail) ; décliner → « Taguage refusé. » + warn ; section vide → header masqué ; deux principales vides → « Pas de page en attente. »</manual>
  </verify>
  <acceptance_criteria>
    - Deux sections principales à eyebrow mono : « DEMANDES REÇUES » et « TAGUAGES EN ATTENTE » + 3e section discrète « Envoyées »
    - Taguage répondu INLINE : `handleRespondTag` appelle `respondToTag` (hook) ; aucun `router.push('/(app)/point/...)` pour répondre ; aucun `supabase.` ajouté pour le taguage
    - Copie exacte : titre « Demandes », « Accepter »/« Refuser », « Sceller »/« Décliner », snackbars « Page scellée. » / « Taguage refusé. » / « Action impossible. Réessayez. »
    - Empty : section vide → header masqué ; deux principales vides → « Pas de page en attente. »
    - `handleCancel` direct-Supabase (l. 131-139) NON copié pour le taguage ; laissé inchangé ou routé via hook si trivial (sinon noté en SUMMARY)
    - Titre « Demandes » en F.serifLight ; aucune occurrence de F.sans*/F.serifMedium
  </acceptance_criteria>
  <done>L'écran Demandes présente deux sections eyebrow mono + Envoyées discrète, répond aux taguages inline via respondToTag (sans navigation, sans Supabase en composant), avec empty states conformes et copie éditoriale exacte.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Supabase (update) | Consentement de taguage via `respondToTag` (update `point_partners.status` sous RLS, mig 010/011) |
| client → Supabase (read) | Lecture des demandes en attente (`pendingReceived`, `is_pending_partner`) sous RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Elevation of Privilege | `respondToTag` / `point_partners` | mitigate | Update mono-table sur `point_partners` ciblé par `.eq('id', pointPartnerId)` ; la RLS (mig 010/011) n'autorise la mise à jour que pour le partenaire tagué (`partner_id = auth.uid()`). Pas de récursion croisée de policy (règle 18, pas de `42P17`). Jamais de service key côté client. |
| T-04-03-02 | Tampering | `is_visible` (points) | mitigate | `respondToTag` n'écrit QUE `status` (+ responded_at) ; `is_visible` est basculé server-side par le trigger `on_partner_consent` — interdit côté client (règle DB CLAUDE.md). Aucune écriture is_visible dans ce plan. |
| T-04-03-03 | Information Disclosure | lecture demandes en attente | accept | Aucune nouvelle requête ni champ exposé au-delà de l'existant (`pendingReceived`, `is_pending_partner`) ; RLS reste la frontière d'application unique. |
| T-04-03-SC | Tampering | npm installs | accept | Zéro nouvelle dépendance (UI-SPEC « zéro nouvelle dépendance ») — aucun install, pas de slopcheck requis. |
</threat_model>

<verification>
- `npx tsc --noEmit` → 36 erreurs (0 nouvelle sur la baseline capturée en 04-01 / documentée 04-VALIDATION) après chaque task.
- Manuel TestFlight (dark + light) : 2 sections eyebrow + Envoyées discrète, boutons texte, taguage scellé/décliné inline (respondToTag, sans navigation), empty states, copie éditoriale exacte.
- `git push` + build natif (OTA inopérant) selon workflow CLAUDE.md.
</verification>

<success_criteria>
- UI-07 : `friends/requests` adopte l'archétype table des matières — deux sections eyebrow mono (« DEMANDES REÇUES » + « TAGUAGES EN ATTENTE ») + Envoyées discrète (D-09), boutons texte solid/ghost.
- D-08 : consentement de taguage répondu INLINE via `respondToTag` (hook mono-table, RLS-safe, is_visible via trigger) — plus de navigation « Répondre → ».
- Empty states conformes (« Pas de page en attente. », section vide masquée) ; aucun nouvel appel Supabase en composant pour le taguage ; 0 nouvelle erreur tsc.
</success_criteria>

<output>
Create `.planning/phases/04-listes-cercle/04-03-SUMMARY.md` when done (noter si la déviation rule-4 de handleCancel a été routée via hook ou laissée inchangée).
</output>
