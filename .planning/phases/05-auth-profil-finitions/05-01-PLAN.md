---
phase: 05-auth-profil-finitions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ui/AppText.tsx
autonomous: true
requirements: [IOS-04]
must_haves:
  truths:
    - "AppText accepts a `display` variant in addition to body/title/eyebrow"
    - "The `display` variant maps F.serifLight and caps maxFontSizeMultiplier at 1.15"
    - "Existing variants keep their caps unchanged: body 2.0, title 1.3, eyebrow 1.2"
    - "Passing maxFontSizeMultiplier as a prop overrides the per-variant cap (forwarded via spread)"
  artifacts:
    - path: "components/ui/AppText.tsx"
      provides: "Dynamic Type primitive with display variant + per-role caps (D-06 lever)"
      contains: "display"
  key_links:
    - from: "components/ui/AppText.tsx"
      to: "constants/fonts.ts (F.serifLight)"
      via: "VARIANT_FONT.display = F.serifLight"
      pattern: "display.*F\\.serifLight"
---

<objective>
Extend the `AppText` Dynamic Type primitive with a `display` variant (F.serifLight, cap 1.15) so the « page de couverture » heroes (Cover 56) and the large bento tile in the auth/profile restyles can route their text through a single capped primitive. This is the central D-06 lever and the foundational dependency for the auth (05-02) and profile (05-03) restyles.

Purpose: Establish the Dynamic Type ceiling for the Cover 56 hero role before any screen consumes it, so the cover layout never overflows at the iOS accessibility maximum (Pitfall 2).
Output: `components/ui/AppText.tsx` with a 4th variant `display` and confirmed per-role caps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-auth-profil-finitions/05-UI-SPEC.md
@.planning/phases/05-auth-profil-finitions/05-PATTERNS.md

<interfaces>
<!-- Current AppText build (the analog to extend) — from components/ui/AppText.tsx:9-34 -->
Variant union (line 9): 'body' | 'title' | 'eyebrow'
VARIANT_FONT (lines 12-16): body→F.sans, title→F.serif, eyebrow→F.mono
MAX_SCALE (lines 19-23): body→2.0, title→1.3, eyebrow→1.2
Component (lines 25-33): renders <Text allowFontScaling maxFontSizeMultiplier={MAX_SCALE[variant]} style={[{fontFamily: VARIANT_FONT[variant]}, style]} {...props} />
F.serifLight already importable from '@/constants/fonts' (constants/fonts.ts:9) — no new import.
The `{...props}` spread already forwards a caller-supplied maxFontSizeMultiplier (overrides the per-variant default).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 0: Capture fresh tsc baseline</name>
  <files>(none — measurement only)</files>
  <read_first>
    - .planning/phases/05-auth-profil-finitions/05-VALIDATION.md §Wave 0 (baseline capture instruction)
    - .planning/STATE.md line 67 (documented baseline = 21)
  </read_first>
  <action>Run the static gate command to record the current error count BEFORE any edit. Note the exact number in the plan SUMMARY as the phase reference for « 0 new errors ». The documented baseline is 21 (STATE.md). If the fresh count differs, record the fresh count and use it as the reference for all Phase 5 tasks.</action>
  <acceptance_criteria>
    - The SUMMARY records the exact pre-edit tsc error count (expected 21).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error TS"</automated>
  </verify>
  <done>Baseline error count recorded (expected 21) as the Phase 5 reference.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 1: Add `display` variant to AppText with per-role caps</name>
  <files>components/ui/AppText.tsx</files>
  <read_first>
    - components/ui/AppText.tsx (full — 34 lines; the file IS the analog to self-extend)
    - .planning/phases/05-auth-profil-finitions/05-PATTERNS.md §AppText (lines 26-60: exact edit pattern + Phase 4 title caveat)
    - .planning/phases/05-auth-profil-finitions/05-UI-SPEC.md §Typography → Dynamic Type table (caps 1.15/1.25/1.3/1.8/1.2) + implementation recommendation (lines 103-118)
    - constants/fonts.ts (F.serifLight = CormorantGaramond_300Light_Italic, line 9)
  </read_first>
  <action>Add the literal string `display` to the `Variant` union type. Add `display: F.serifLight` to the `VARIANT_FONT` record and `display: 1.15` to the `MAX_SCALE` record. Do NOT change the existing body (2.0), title (1.3), or eyebrow (1.2) caps. Do NOT add fontSize, color, lineHeight, or any other style to AppText — the component must stay thin (variant→fontFamily + cap only), with size/color always coming from the caller's `style` prop, per the file's own header comment and D-06. Keep `allowFontScaling`, the `maxFontSizeMultiplier={MAX_SCALE[variant]}` default, and the `{...props}` spread intact so a caller can override the cap per instance (e.g. `maxFontSizeMultiplier={1.8}` on cover fields, `={1.25}` on a Title 32 serifLight). Add a one-line French comment marking `display` as the Cover 56 hero variant. No new import (F.serifLight is already exported from the `F` object).</action>
  <acceptance_criteria>
    - The `Variant` type union includes `'display'`.
    - `VARIANT_FONT.display === F.serifLight` and `MAX_SCALE.display === 1.15`.
    - `body`, `title`, `eyebrow` entries are byte-for-byte unchanged (2.0 / 1.3 / 1.2).
    - AppText declares no fontSize/color/lineHeight (stays thin — size/color via `style`).
    - The `{...props}` spread is still present after `style`, so a prop-level `maxFontSizeMultiplier` still overrides the variant default.
    - No new import statement added.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error TS"</automated>
    <human-check>Grep components/ui/AppText.tsx for `display` → present in both VARIANT_FONT and MAX_SCALE; confirm the 3 existing caps are unchanged.</human-check>
  </verify>
  <done>AppText exports a `display` variant (F.serifLight, cap 1.15); existing variants unchanged; tsc count equals the Task 0 baseline (0 new errors); component remains thin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure presentation primitive — no input crosses a trust boundary; no network, no auth, no storage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | components/ui/AppText.tsx | accept | Display-only primitive; no untrusted input, no state, no side effects. Type-safe variant union enforced by tsc. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies this plan/phase (verified 05-RESEARCH §Standard Stack — "Aucune nouvelle dépendance"). No install task exists; supply-chain surface unchanged. |
</threat_model>

<verification>
- `npx tsc --noEmit` returns the same error count as the Task 0 baseline (expected 21) — 0 new errors.
- Visual diff review: only the `Variant` type, `VARIANT_FONT`, and `MAX_SCALE` changed; no fontSize/color introduced; no new import.
</verification>

<success_criteria>
- AppText supports `variant="display"` mapping F.serifLight at cap 1.15.
- Existing variants (body/title/eyebrow) keep their caps.
- 0 new tsc errors over baseline.
</success_criteria>

<output>
Create `.planning/phases/05-auth-profil-finitions/05-01-SUMMARY.md` when done (record the captured tsc baseline number).
</output>
