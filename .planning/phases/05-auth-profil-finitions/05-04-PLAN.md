---
phase: 05-auth-profil-finitions
plan: 04
type: execute
wave: 3
depends_on: ["05-02", "05-03"]
files_modified:
  - app/(app)/(tabs)/map/index.tsx
  - app/(app)/(tabs)/point/list.tsx
  - app/(app)/(tabs)/friends/index.tsx
  - app/(app)/(tabs)/friends/requests.tsx
  - app/(app)/point/new.tsx
  - app/(app)/point/[id].tsx
autonomous: true
requirements: [IOS-04]
must_haves:
  truths:
    - "The 6 non-refonte screens are verified for safe areas (useSafeAreaInsets, zero SafeAreaView) and home indicator"
    - "point/list, friends/index, friends/requests use insets.bottom for the list bottom padding (magic 100 reconciled with the opaque tab bar) instead of a bare literal"
    - "point/new, point/[id], map/index home-indicator padding verified correct (insets.bottom-based) — fixed only if a real break exists"
    - "Heroes / large serif on the 6 screens carry a Dynamic Type cap (AppText variant or explicit maxFontSizeMultiplier) so they do not overflow at the iOS accessibility maximum"
    - "Genuinely off-scale spacing/typo on the touched screens is reassigned to {4,8,16,24,32,48,64} (rowGap 12 allowed); no hardcoded hex/fontFamily introduced; clair+dark consistent"
    - "The documented out-of-scope debt (direct Supabase calls in requests.tsx/friends/index.tsx) is NOT extended or refactored"
  artifacts:
    - path: "app/(app)/(tabs)/point/list.tsx"
      provides: "IOS-04 sweep: home indicator + Dynamic Type cap on hero"
    - path: "app/(app)/(tabs)/friends/index.tsx"
      provides: "IOS-04 sweep: home indicator + Dynamic Type cap on hero"
    - path: "app/(app)/(tabs)/friends/requests.tsx"
      provides: "IOS-04 sweep: home indicator + Dynamic Type cap on section heroes"
  key_links:
    - from: "list/friends screens"
      to: "insets.bottom"
      via: "listContent paddingBottom"
      pattern: "insets\\.bottom"
---

<objective>
Run the IOS-04 audit + consistency sweep across the 6 screens not covered by the auth/profile refontes (map/index, point/new, point/[id], point/list, friends/index, friends/requests), finalizing the milestone's cross-cutting polish: home-indicator padding tied to insets.bottom, Dynamic Type caps on the layout-critical heroes, and reassignment of genuinely off-scale tokens — in both light and dark themes. This is the « passe de cohérence » (D-07) and the final pass of the milestone.

Purpose: Bring the remaining screens up to the same safe-area / Dynamic Type / token discipline the refonte screens now demonstrate, WITHOUT rewriting screens that are already coherent (Pitfall 3) and WITHOUT touching shared primitives or the documented out-of-scope Supabase-call debt.
Output: Targeted edits on the 6 screens (insets.bottom, hero caps, off-scale tokens) — surgical, not a rewrite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-auth-profil-finitions/05-UI-SPEC.md
@.planning/phases/05-auth-profil-finitions/05-PATTERNS.md
@.planning/phases/05-auth-profil-finitions/05-RESEARCH.md

<interfaces>
<!-- The audit baseline IS the work scope. Edit ONLY these verified sites (05-PATTERNS §IOS-04 Sweep Sites + 05-RESEARCH §IOS-04 Audit Baseline). -->
Tokens (useTheme()): T.* ; canonical spacing {4,8,16,24,32,48,64} + rowGap 12 exception.
AppText (after 05-01): display/title/body/eyebrow with per-variant caps; pass maxFontSizeMultiplier as a prop to cap a raw Text hero without routing it through AppText.

HOME-INDICATOR (insets.bottom) — fix vs verify:
- point/new.tsx scroll paddingBottom: insets.bottom + 32 (l.192) → VERIFY only.
- point/[id].tsx scroll paddingBottom: insets.bottom + 32 (l.222) → VERIFY only.
- map/index.tsx FAB/hint insets.bottom + N → VERIFY only.
- point/list.tsx listContent paddingBottom: 100 magic (l.265) → reconcile to insets.bottom + tab-bar clearance.
- friends/index.tsx listContent paddingBottom: 100 (l.392) → same.
- friends/requests.tsx listContent paddingBottom: 100 (l.339); top insets.top + 16 (l.167) → same.

DYNAMIC TYPE: maxFontSizeMultiplier exists in ZERO app/ files (only AppText). Cap the heroes/large serif on these screens only (NOT every Text): point/list « Le carnet » serif title; friends/index editorial header; friends/requests section heroes; point/new & point/[id] carnet heroes; map header text.

OFF-SCALE: only fix obvious 13/14/18/20 layout values IF encountered on these 6 screens. Do NOT rewrite coherent screens.

ALREADY CONFORMING (no action): 0 SafeAreaView anywhere; all 9 use useSafeAreaInsets()+insets.top; all use makeStyles(T)+useMemo([T]).

DO NOT EXTEND debt: direct Supabase calls in requests.tsx (handleCancel « Envoyées ») and friends/index.tsx (loadFriends) are documented out-of-scope — leave them; do not refactor or propagate.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Home-indicator + Dynamic Type sweep on the tabs list/social screens (list, friends/index, friends/requests)</name>
  <files>app/(app)/(tabs)/point/list.tsx, app/(app)/(tabs)/friends/index.tsx, app/(app)/(tabs)/friends/requests.tsx</files>
  <read_first>
    - app/(app)/(tabs)/point/list.tsx (locate listContent paddingBottom l.265 + the « Le carnet » serif hero)
    - app/(app)/(tabs)/friends/index.tsx (locate listContent paddingBottom l.392 + editorial header)
    - app/(app)/(tabs)/friends/requests.tsx (locate listContent paddingBottom l.339, top insets.top + 16 l.167, section heroes)
    - .planning/phases/05-auth-profil-finitions/05-PATTERNS.md §IOS-04 Sweep Sites (home-indicator table lines 226-238 + off-discipline weights + off-scale lines 249-253) + « Do NOT extend debt » (line 259)
    - .planning/phases/05-auth-profil-finitions/05-RESEARCH.md §IOS-04 Audit Baseline (home indicator + Dynamic Type + off-scale lists) + §Pitfall 3 (over-extension)
    - .planning/phases/05-auth-profil-finitions/05-UI-SPEC.md §Finitions IOS-04 (pass criteria table, lines 223-236)
  </read_first>
  <action>On each of the three tabs list/social screens, replace the bare literal `paddingBottom: 100` on listContent with an insets-based value that still clears the opaque tab bar: `insets.bottom + <tab-bar clearance>` (compute the clearance from the existing tab bar height constant if one is in scope, otherwise keep total visual clearance equivalent to the prior 100 but expressed as insets.bottom + a canonical constant so the home indicator is never overlapped on devices with a larger bottom inset). On friends/requests, leave the `insets.top + 16` header top as-is unless the 16 is genuinely off (it is canonical-adjacent — verify, do not force-change). Add a Dynamic Type cap to the layout-critical hero of each screen ONLY: route the « Le carnet » serif title (point/list) and the editorial header titles (friends/index, friends/requests section heroes « DEMANDES REÇUES »/« TAGUAGES EN ATTENTE ») through AppText (variant matching their family — title for serif, eyebrow for mono) OR, if leaving them as raw Text is lower-risk, add an explicit maxFontSizeMultiplier matching the role table (serif title 1.3, serifLight 1.25, eyebrow 1.2). Do NOT cap every Text on these screens (Pitfall 3) — heroes only. Fix only obviously off-scale layout values (13/14/18/20) if encountered; reassign to {4,8,16,24,32,48,64}; keep rowGap 12. Do NOT touch the documented out-of-scope direct-Supabase debt (handleCancel in requests.tsx, loadFriends in friends/index.tsx) — do not refactor, do not propagate. No edits to shared primitives. No hardcoded hex/fontFamily; test clair+dark.</action>
  <acceptance_criteria>
    - point/list.tsx, friends/index.tsx, friends/requests.tsx listContent paddingBottom is expressed with insets.bottom (no bare literal 100) and still clears the tab bar.
    - Each of the three screens' single layout-critical hero carries a Dynamic Type cap (AppText variant or explicit maxFontSizeMultiplier per the role table); non-hero Text is left untouched.
    - The direct-Supabase debt (requests.tsx handleCancel, friends/index.tsx loadFriends) is unchanged — not refactored, not extended.
    - No SafeAreaView introduced; useSafeAreaInsets() retained; no shared primitive edited; no hardcoded hex/fontFamily; off-scale fixes limited to genuine 13/14/18/20 sites.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error TS"</automated>
    <human-check>TestFlight (clair+dark): on list/friends/requests, the last row is never hidden under the home indicator; max iOS text size does not overflow the screen heroes; layout reads consistent with the refonte screens.</human-check>
  </verify>
  <done>The 3 tabs list/social screens use insets.bottom for list padding and have capped heroes, with no debt extension and no over-rewrite; 0 new tsc errors over baseline.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Verify-and-cap sweep on map/index, point/new, point/[id] + final dark/light consistency check</name>
  <files>app/(app)/(tabs)/map/index.tsx, app/(app)/point/new.tsx, app/(app)/point/[id].tsx</files>
  <read_first>
    - app/(app)/point/new.tsx (verify scroll paddingBottom insets.bottom + 32 l.192; locate carnet hero)
    - app/(app)/point/[id].tsx (verify scroll paddingBottom insets.bottom + 32 l.222; locate carnet note hero)
    - app/(app)/(tabs)/map/index.tsx (verify FAB/hint insets.bottom + N; locate any header text hero)
    - .planning/phases/05-auth-profil-finitions/05-PATTERNS.md §IOS-04 Sweep Sites (VERIFY-only rows lines 236-238)
    - .planning/phases/05-auth-profil-finitions/05-RESEARCH.md §IOS-04 Audit Baseline (already-conforming notes) + §Pitfall 3
    - .planning/phases/05-auth-profil-finitions/05-UI-SPEC.md §Finitions IOS-04 (pass criteria)
  </read_first>
  <action>Confirm the home-indicator padding on these three screens is already insets.bottom-based (point/new l.192, point/[id] l.222, map FAB/hint) — these are VERIFY-only per the audit; change them ONLY if an actual break is found, and if so align to insets.bottom + 32. Add a Dynamic Type cap to the single layout-critical hero of each screen if it is a raw Text without one: the carnet note hero in point/new and point/[id] (serifLight Display → cap 1.15 via AppText variant="display" or explicit maxFontSizeMultiplier={1.15}) and any large header text in map/index (match the role table). Do NOT cap every Text (Pitfall 3). Fix only obviously off-scale layout values (13/14/18/20) if encountered; reassign to {4,8,16,24,32,48,64}; keep rowGap 12. Do NOT reintroduce a BlurView on the tab bar (règle 13) and do NOT touch the map sheet/static-image logic. As the FINAL milestone pass, review all 6 swept screens in both light and dark for any hardcoded hex/fontFamily introduced during the sweep and any residual SafeAreaView (expected zero). No edits to shared primitives. French UI preserved.</action>
  <acceptance_criteria>
    - point/new and point/[id] home-indicator padding confirmed insets.bottom-based (unchanged unless a real break was found); map FAB/hint confirmed insets.bottom-based.
    - The single carnet/header hero on each of the three screens carries a Dynamic Type cap if it lacked one; non-hero Text untouched.
    - No BlurView on the tab bar; no map sheet/static-image logic changed; no shared primitive edited.
    - Final review confirms: zero SafeAreaView across the 6 swept screens, no hardcoded hex/fontFamily introduced, dark+light consistent.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error TS"</automated>
    <human-check>TestFlight (clair+dark): map FAB/hint and the create/detail sheets clear the home indicator; max iOS text size does not break the carnet heroes; tab bar stays opaque; the 9 screens read visually consistent across both themes.</human-check>
  </verify>
  <done>map/index, point/new, point/[id] verified/capped; final dark/light consistency pass complete across the 6 swept screens; 0 new tsc errors over baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure presentation sweep — no new input crosses a trust boundary; existing RLS-protected reads/writes on these screens are unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Tampering | the 6 swept screens | accept | Cosmetic edits (padding/caps/tokens) only; no data flow, RLS, auth, or query semantics changed. Documented direct-Supabase debt intentionally left untouched (not extended). |
| T-05-04-02 | Information Disclosure | requests.tsx / friends/index.tsx | accept | Out-of-scope direct-Supabase debt not refactored here; behavior unchanged, no new exposure introduced by the sweep. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies (05-RESEARCH §Standard Stack). No install task; supply-chain surface unchanged. |
</threat_model>

<verification>
- `npx tsc --noEmit` equals the 05-01 baseline (expected 21) — 0 new errors after each task.
- Diff review: edits limited to insets.bottom padding, hero Dynamic Type caps, and genuine off-scale tokens on the 6 named screens; no shared primitive touched; no BlurView; no SafeAreaView; debt untouched.
- Device checklist (05-VALIDATION §IOS-04): all 9 screens — no content under the home indicator, Dynamic Type max without layout break, clair+dark consistent.
</verification>

<success_criteria>
- 6 écrans hors-refonte: home indicator en insets.bottom, heroes plafonnés (Dynamic Type), tokens hors-échelle réalignés (IOS-04 / D-07).
- Passe finale clair+dark cohérente sur les 9 écrans ; dette hors-périmètre non étendue ; tab bar opaque (règle 13).
- 0 nouvelle erreur tsc.
</success_criteria>

<output>
Create `.planning/phases/05-auth-profil-finitions/05-04-SUMMARY.md` when done.
</output>
