---
phase: 05-auth-profil-finitions
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - app/(auth)/login.tsx
  - app/(auth)/register.tsx
autonomous: true
requirements: [UI-01, IOS-04]
must_haves:
  truths:
    - "Login presents as a magazine cover: mono eyebrow « LOVEMAP · ÉDITION INTIME » + Cover 56 serif hero « LoveMap » + visible underline fields + single rose CTA"
    - "Login fields are visible without scroll (not under the fold) and login flow is intact (signInWithPassword → /(app)/map)"
    - "Login errors surface via Paper Snackbar (D-01), not inline text"
    - "Register keeps its 2-step stepper; step 1 is a solemn age cover, step 2 matches login's cover grammar"
    - "The client age check < 18 actually blocks: register imports APP_CONFIG.MIN_AGE (D-11 bug fixed)"
    - "Register step 1 CTA copy is more specific than « Continuer » (« Vérifier mon âge », D-12)"
    - "login.tsx and register.tsx use insets.bottom + 32 for bottom padding (no literal 48) — no content under the home indicator"
  artifacts:
    - path: "app/(auth)/login.tsx"
      provides: "Login cover screen (UI-01) with Snackbar errors + insets.bottom fix"
      min_lines: 80
    - path: "app/(auth)/register.tsx"
      provides: "Register cover stepper (UI-01) with MIN_AGE fix + insets.bottom fix"
      min_lines: 120
  key_links:
    - from: "app/(auth)/login.tsx"
      to: "supabase.auth.signInWithPassword"
      via: "handleLogin (preserved)"
      pattern: "signInWithPassword"
    - from: "app/(auth)/register.tsx"
      to: "constants/config.ts (APP_CONFIG.MIN_AGE)"
      via: "import { APP_CONFIG } + APP_CONFIG.MIN_AGE in calcAge guard"
      pattern: "APP_CONFIG\\.MIN_AGE"
    - from: "app/(auth)/register.tsx"
      to: "supabase.auth.signUp"
      via: "handleRegister (preserved, with date_of_birth)"
      pattern: "signUp"
---

<objective>
Restyle the two auth screens into the « page de couverture » archetype (UI-01) — magazine-cover eyebrow + Cover 56 serif hero + immediately-visible underline fields + a single rose CTA — while preserving every auth handler, the register 2-step stepper, and the server age trigger verbatim. Fix the latent `MIN_AGE` client bug (D-11) and the home-indicator padding (IOS-04) on both screens.

Purpose: Deliver the auth half of the cover refonte. This is a presentation-only rewrite — no new auth flow, no provider change, no migration. The dominant risk is regressing the working flow, so handlers are copied, not rewritten.
Output: `login.tsx` and `register.tsx` restyled, MIN_AGE client gate functional, errors via Snackbar, bottom padding tied to insets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-auth-profil-finitions/05-UI-SPEC.md
@.planning/phases/05-auth-profil-finitions/05-PATTERNS.md
@.planning/phases/05-auth-profil-finitions/05-RESEARCH.md
@app/(auth)/login.tsx
@app/(auth)/register.tsx
@constants/config.ts

<interfaces>
<!-- Tokens & primitives the executor consumes (no exploration needed) -->
Tokens (constants/theme.ts via useTheme()): T.bg, T.surface, T.surface2, T.border, T.text, T.textDim, T.textFaint, T.primary, T.danger, T.radiusSm (12), cardRadius (16), pill (999).
Fonts (constants/fonts.ts, object F): F.serifLight (Cover/Title 300 italic), F.serif (Heading 400 italic), F.mono (eyebrow), F.sans (functional body 16).
AppText (after 05-01): variants display(F.serifLight, cap 1.15) | title(F.serif, cap 1.3) | body(F.sans, cap 2.0) | eyebrow(F.mono, cap 1.2); pass maxFontSizeMultiplier as a prop to override per instance.
Primitives (do NOT fork — D-10): components/ui/Button (variants solid/coral/ghost/danger/underline, base borderRadius:0), components/ui/Input (underline, label mono, value F.serif 20 by default — override via style), components/ui/PressableScale.
Haptics: lib/haptics.{tap,select,success,warn,error} (fire-and-forget, never await).
config.ts exports APP_CONFIG.MIN_AGE = 18 (NESTED — there is NO bare `export const MIN_AGE`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Restyle login as a magazine cover (UI-01, D-01) + Snackbar + insets fix</name>
  <files>app/(auth)/login.tsx</files>
  <read_first>
    - app/(auth)/login.tsx (full — handlers to PRESERVE: handleLogin l.31-38 signInWithPassword→router.replace('/(app)/map'); handleForgot l.41-45; show/hide password; loading/error state)
    - .planning/phases/05-auth-profil-finitions/05-PATTERNS.md §login.tsx (lines 64-104: preserve list, chrome-to-replace l.52-58, editorial-header analog point/list.tsx:160-161 & :222-229, field override pattern, IOS-04 sites l.48/l.122)
    - .planning/phases/05-auth-profil-finitions/05-UI-SPEC.md §Screen Composition — Connexion (lines 148-166: 8-step vertical structure) + §Copywriting → Connexion (lines 244-255: exact French copy) + §Typography (Cover 56) + §Color (single accent)
    - constants/theme.ts (token names) ; constants/fonts.ts (F.*)
  </read_first>
  <action>Replace the current wordmark chrome (love♥map + IcoHeartDashed + tagline « De retour, voyageur. ») with the cover structure from UI-SPEC §Connexion: (1) container paddingTop insets.top + 48 (3xl); (2) eyebrow via AppText variant="eyebrow" reading « LOVEMAP · ÉDITION INTIME » (uppercase, letterSpacing 2.5, color T.textFaint); (3) hero via AppText variant="display" reading « LoveMap » at fontSize 56 lineHeight 56 color T.text numberOfLines={1}; (4) 2xl (32) gap then the form block visible without scroll. Render the E-mail and Mot de passe fields with the existing components/ui/Input, label « E-MAIL » / « MOT DE PASSE », keeping keyboardType="email-address" autoCapitalize="none" on email and secureTextEntry on password; override the value font to F.sans 16 via style and pass maxFontSizeMultiplier={1.8} per instance (do NOT lower AppText body globally; do NOT edit Input.tsx — Pitfall 4). Space fields by xl (24). CTA « Se connecter » via components/ui/Button variant="coral"; apply cover radius per instance via style borderRadius T.radiusSm borderCurve:'continuous' (D-10 — do NOT edit Button.tsx); height ≥ 52; onPress calls haptics.tap() then handleLogin(); loading label « Connexion… ». Secondary link « Créer un compte » (mono eyebrow, T.textDim, discreet underline) → register. Optional mono hem « ÉDITION N°01 · 2026 » in T.textFaint near the bottom. Migrate the inline error (current l.88) to a Paper Snackbar with copy « Connexion impossible. Vérifiez vos identifiants. » (do not render raw Supabase error strings in the UI — Information Disclosure mitigation). Keep handleLogin, handleForgot, router.replace('/(app)/map'), and show/hide-password logic byte-for-byte. IOS-04: set scroll/container paddingBottom to insets.bottom + 32 (replace the literal 48 at l.122) and paddingTop to insets.top + 48 (replace insets.top + 56 at l.48). Keep a light KeyboardAvoidingView/scroll so the CTA stays reachable with the keyboard open without pushing the form under the fold. Reassign any off-scale spacing encountered (36/44/40/20/12/17/13/14) to the canonical set {4,8,16,24,32,48,64} (rowGap 12 allowed). All copy in French; zero emoji.</action>
  <acceptance_criteria>
    - login renders an AppText variant="eyebrow" with text « LOVEMAP · ÉDITION INTIME » and an AppText variant="display" with text « LoveMap » (fontSize 56).
    - The email and password Inputs are rendered above any scroll fold (no field requires scrolling on a standard iPhone), each passing maxFontSizeMultiplier={1.8} and style fontFamily F.sans fontSize 16.
    - The CTA uses Button variant="coral", calls haptics.tap() before handleLogin, and applies borderRadius via per-instance style (Button.tsx unedited).
    - Login errors render through a Paper Snackbar with the generic French copy; no raw Supabase message and no inline error Text remain.
    - handleLogin still calls supabase.auth.signInWithPassword and router.replace('/(app)/map'); handleForgot preserved.
    - Container paddingBottom === insets.bottom + 32 and paddingTop === insets.top + 48; no literal 48/56 bottom/top padding remains.
    - No SafeAreaView; useSafeAreaInsets() used; no hardcoded hex or fontFamily literal introduced; no edit to Button.tsx/Input.tsx/AppText.tsx.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error TS"</automated>
    <human-check>TestFlight (clair+dark): login shows the cover (eyebrow + Cover 56 hero + visible fields + one rose CTA); sign in succeeds → map; a bad password shows the Snackbar; nothing sits under the home indicator; raise iOS text size to max → hero does not overflow/truncate.</human-check>
  </verify>
  <done>Login is a magazine cover with visible underline fields, Snackbar errors, preserved flow, and insets.bottom-based bottom padding; 0 new tsc errors over baseline.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Restyle register stepper as solemn cover + FIX MIN_AGE (D-11) + CTA copy (D-12) + insets fix</name>
  <files>app/(auth)/register.tsx</files>
  <read_first>
    - app/(auth)/register.tsx (full — PRESERVE verbatim: `step` state + StepIndicator l.78,148-154; PickerCol JJ/MM/AAAA l.25-68,191-196; calcAge()/confirmAge() l.91-107; validate() l.116-125; handleRegister→signUp({…,data:{date_of_birth}}) l.127-145; back buttons l.163-170,229-236)
    - .planning/phases/05-auth-profil-finitions/05-PATTERNS.md §register.tsx (lines 108-127: THE BUG l.18/l.101, preserve list, step-1 chrome l.176-217, step-2 chrome l.241-275, IOS-04 sites l.159/l.225/l.296)
    - .planning/phases/05-auth-profil-finitions/05-RESEARCH.md §Pitfall 5 (MIN_AGE undefined → gate inert) + §Security Domain (age gate)
    - .planning/phases/05-auth-profil-finitions/05-UI-SPEC.md §Inscription (lines 170-187) + §Copywriting → Inscription (lines 257-267: exact copy) + checker_flag D-12 (frontmatter)
    - constants/config.ts (APP_CONFIG.MIN_AGE = 18, no bare export) ; app/(auth)/login.tsx (Task 1 result — step 2 must match this grammar)
  </read_first>
  <action>Fix the latent bug FIRST: change the import at register.tsx:18 from `import { MIN_AGE } from '@/constants/config'` to `import { APP_CONFIG } from '@/constants/config'` and update the age guard (l.101 region) to compare `calcAge() < APP_CONFIG.MIN_AGE`. Do NOT add a bare export to config.ts (keep blast radius zero). The server trigger handle_new_user remains the authoritative gate (règle 10) — do not touch it. Then restyle Step 1 (age) as a solemn cover per UI-SPEC §Inscription Step 1: eyebrow via AppText variant="eyebrow" « VÉRIFICATION D'ÂGE »; hero via AppText variant="display" « Quel âge avez-vous ? » (Cover 56); keep the existing JJ/MM/AAAA underline PickerCol date entry verbatim; help line « Vous devez avoir 18 ans ou plus pour entrer. » in F.sans 16; replace the custom enterBtn with components/ui/Button (variant="coral", per-instance cover radius, height ≥ 52); rename the CTA from « Continuer » to « Vérifier mon âge » (D-12 checker_flag). On submit call haptics.tap(); on age refusal show « Vous devez avoir 18 ans ou plus. » and call haptics.warn(). Drop the F.sansLight subtitle (l.354, off-discipline weight). Restyle Step 2 (form) to match login's cover grammar: head eyebrow (e.g. « N° 003 — INSCRIPTION »), serif hero, the existing 5 mapped Input fields kept (the `.map()` over field configs l.248-266 is reusable as-is) with value F.sans 16 + maxFontSizeMultiplier={1.8} per instance; CTA « Commencer le carnet » via Button variant="coral" with haptics.tap(); keep the « Retour » back button to step 1. Errors via Paper Snackbar (« Inscription impossible. Réessayez. ») — no raw Supabase strings. IOS-04: step-1 container paddingTop insets.top + 48 (already +48 at l.159 — verify), step-2 paddingTop insets.top + 48 (replace +56 at l.225), scroll paddingBottom insets.bottom + 32 (replace literal 48 at l.296). Reassign off-scale spacing (36/64/42/38/14/10/18/22) to {4,8,16,24,32,48,64} (rowGap 12 allowed). French only; zero emoji. PRESERVE the stepper, picker, signUp + date_of_birth, and back buttons exactly.</action>
  <acceptance_criteria>
    - register.tsx imports APP_CONFIG (not a bare MIN_AGE) and the age guard reads `calcAge() < APP_CONFIG.MIN_AGE`; entering a < 18 date now blocks and shows the refusal copy + haptics.warn().
    - Step 1 renders AppText variant="eyebrow" « VÉRIFICATION D'ÂGE » + AppText variant="display" hero; the JJ/MM/AAAA PickerCol is unchanged; CTA reads « Vérifier mon âge ».
    - Step 2 renders a login-consistent cover: eyebrow + serif hero + the 5 mapped Inputs (F.sans 16, cap 1.8) + Button « Commencer le carnet »; « Retour » back button preserved.
    - handleRegister still calls supabase.auth.signUp with data:{ date_of_birth }; `step` state, StepIndicator, calcAge/confirmAge, validate preserved.
    - No F.sansLight remains; CTAs use Button (no custom enterBtn weight); Button.tsx/Input.tsx/AppText.tsx unedited.
    - Step-1 paddingTop insets.top + 48; step-2 paddingTop insets.top + 48; scroll paddingBottom insets.bottom + 32; no literal 48/56 padding remains; config.ts unchanged.
    - Errors via Snackbar with generic French copy; no raw Supabase string in UI.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error TS"</automated>
    <human-check>TestFlight (clair+dark): step 1 is a solemn cover; entering a birthdate making age < 18 blocks with the refusal + warn haptic (MIN_AGE fix); a valid ≥ 18 date advances to step 2; step 2 matches login grammar and creates an account end-to-end; nothing under the home indicator; max iOS text size does not break the cover.</human-check>
  </verify>
  <done>Register is a 2-step cover with a working client age gate (APP_CONFIG.MIN_AGE), « Vérifier mon âge » CTA, preserved signup flow/trigger, Snackbar errors, and insets.bottom-based padding; 0 new tsc errors over baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Supabase Auth | Email/password and signup payload (incl. date_of_birth) cross from the device to Supabase Auth over TLS. |
| client age guard → server trigger | The client age check is UX only; the authoritative age gate is the DB trigger handle_new_user. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Elevation of Privilege | register age guard | mitigate | Fix MIN_AGE so the client UX gate actually blocks < 18 (defense-in-depth); server trigger handle_new_user remains authoritative (règle 10) — unchanged. |
| T-05-02-02 | Information Disclosure | login/register error rendering | mitigate | Surface generic French Snackbar copy (« Connexion impossible… » / « Inscription impossible… »); never render raw Supabase error strings/stack in the UI. |
| T-05-02-03 | Spoofing | supabase.auth.signInWithPassword / signUp | accept | Auth flow unchanged; credential handling delegated to Supabase Auth over TLS. No new attack surface (restyle only). |
| T-05-02-04 | Tampering | RLS / handle_new_user trigger | accept | No RLS/trigger change in this phase; never use service-role key client-side (CLAUDE.md §1). |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies (05-RESEARCH §Standard Stack). No install task; supply-chain surface unchanged. |
</threat_model>

<verification>
- `npx tsc --noEmit` equals the 05-01 baseline (expected 21) — 0 new errors after each task.
- Diff review: handlers (signInWithPassword/signUp/calcAge/confirmAge/validate/stepper/picker) byte-for-byte preserved; config.ts untouched; Button.tsx/Input.tsx/AppText.tsx untouched; no hardcoded hex/fontFamily; no SafeAreaView.
- Device checklist (05-VALIDATION §UI-01): login + register 2-step end-to-end, Snackbar errors, MIN_AGE < 18 blocks, home indicator clear, Dynamic Type max without breakage, clair+dark.
</verification>

<success_criteria>
- login + register refondus selon l'archétype « page de couverture » (UI-01), flux intact.
- MIN_AGE client gate fonctionne (D-11) ; CTA step 1 « Vérifier mon âge » (D-12).
- login/register en insets.bottom + 32 (IOS-04, pas de littéral) ; erreurs via Snackbar.
- 0 nouvelle erreur tsc.
</success_criteria>

<output>
Create `.planning/phases/05-auth-profil-finitions/05-02-SUMMARY.md` when done.
</output>
