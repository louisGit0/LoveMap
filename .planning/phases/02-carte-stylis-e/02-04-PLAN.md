---
phase: 02-carte-stylis-e
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - components/map/MapHeader.tsx
  - components/map/FriendSelector.tsx
  - components/map/AppMapView.tsx
autonomous: true
requirements: [UI-02]
must_haves:
  truths:
    - "Le bandeau de contrôles est une carte flottante éditoriale lisible (surface semi-opaque ~0.92, PAS de BlurView lourd), coins arrondis continus"
    - "Le toggle segmenté affiche Points / Heatmap, segment actif rose+texte clair, et déclenche haptics.select()"
    - "Le sélecteur d'ami et le bouton Recentrer sont des pills/squircle arrondis continus avec haptique (select / tap)"
    - "Le bouton Recentrer est détaché en bas-droite au-dessus du FAB avec accessibilityLabel explicite"
  artifacts:
    - path: "components/map/MapHeader.tsx"
      provides: "Bandeau flottant éditorial (surface semi-opaque, radiusLg+borderCurve, titre AppText, toggle segmenté Points/Heatmap, haptics.select)"
      contains: "borderCurve"
    - path: "components/map/FriendSelector.tsx"
      provides: "Trigger pill restylé (radiusXs+borderCurve, semi-opaque, haptics.select) — body inchangé"
      contains: "borderCurve"
    - path: "components/map/AppMapView.tsx"
      provides: "Bouton Recentrer squircle 40×40 bas-droite + haptics.tap() + a11y"
      contains: "borderCurve"
  key_links:
    - from: "components/map/MapHeader.tsx (toggle segments)"
      to: "lib/haptics.ts"
      via: "haptics.select() au onPress de chaque segment"
      pattern: "haptics\\.select"
    - from: "components/map/MapHeader.tsx (surface)"
      to: "lisibilité (règle 13)"
      via: "fond rgba semi-opaque ~0.92, pas de BlurView"
      pattern: "0\\.92|rgba\\(10,10,10"
---

<objective>
Refondre les contrôles flottants de la carte en bandeau/cluster éditorial signature (toggle Points/Heatmap, sélecteur d'ami, recentrer), lisible et arrondi iOS, sans blur lourd (UI-02 / D-10 / D-12). Tranche visible : un bandeau flottant net en haut et un bouton recentrer détaché en bas-droite, tous arrondis continus avec retour haptique.

Purpose: Remplacer la barre pleine largeur à angles francs par une carte flottante éditoriale (hiérarchie mono/serif, surface semi-opaque LISIBLE — leçon règle 13 sur le BlurView). Appliquer le langage de formes arrondies D-12 aux contrôles.
Output: `MapHeader.tsx` (bandeau + toggle), `FriendSelector.tsx` (trigger pill), `AppMapView.tsx` (recentrer).

Note dépendance : consomme `T.radiusLg/radiusSm/radiusXs` du plan 02-01 → wave 2. Aucun chevauchement de fichier avec 02-03 (parallèle).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-carte-stylis-e/02-UI-SPEC.md
@.planning/phases/02-carte-stylis-e/02-PATTERNS.md
@.planning/phases/02-carte-stylis-e/02-01-SUMMARY.md

<interfaces>
<!-- Tokens consommés depuis constants/theme.ts (posés par 02-01) -->
T.radiusLg = 22 (bandeau flottant) · T.radiusSm = 12 (recentrer, conteneur toggle) · T.radiusXs = 8 (segments, pill ami)
T.surface · T.border · T.primary · T.text · T.textFaint
Discipline D-12 : tout borderRadius ≥ radiusSm ajoute borderCurve:'continuous' INLINE.

<!-- API lib/haptics.ts (Phase 1) — fire-and-forget -->
haptics.select()  // toggle + sélection ami
haptics.tap()     // recentrer (impact light)

<!-- AppText (Phase 1) — Dynamic Type borné -->
AppText variant 'title' (maxFontSizeMultiplier ≈ 1.3) / 'eyebrow' (≈ 1.2)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Refondre MapHeader.tsx en bandeau éditorial flottant + toggle segmenté</name>
  <files>components/map/MapHeader.tsx</files>
  <read_first>
    - components/map/MapHeader.tsx (fichier modifié — skeleton useTheme/insets :18-21, titre :30-46, toggle :48-71, toggleBtnActive :142-144, fond actuel T.bg+'e8')
    - components/ui/PageHeader.tsx (pattern eyebrow + titre) + components/ui/AppText.tsx (:25, maxFontSizeMultiplier par variant)
    - 02-PATTERNS.md §`components/map/MapHeader.tsx` (rework détaillé) + §S1 (themed skeleton) + §S3 (haptics) + §S4 (icônes SVG)
    - 02-UI-SPEC.md §Bandeau de contrôles flottant (Surface anti-blur, Composition, haptiques) + §Copywriting (« Points »/« Heatmap », titres) + §Color (accent réservé) + §Spacing (gaps sm=8, pillPadX=12)
  </read_first>
  <action>
    Transformer la barre pleine largeur en CARTE FLOTTANTE (UI-SPEC §Bandeau) : `top: insets.top + 8`, marges latérales 16, fond surface semi-opaque LISIBLE `rgba(10,10,10,0.92)` (≈ `T.surface` @0.92 — AUCUN BlurView, règle 13), `borderRadius: T.radiusLg` (22) + `borderCurve:'continuous'` (inline), bord `1px T.border`. Bloc titre (reprendre :30-46) : eyebrow mono `lovemap` (F.mono, 7px, letterSpacing 2.5, uppercase, `T.textFaint`) + titre serif italic `mes moments · NN` (compteur 2 chiffres) ou `carte de {nom}` ; relier via `AppText` variant `eyebrow`/`title` pour le Dynamic Type borné. Toggle segmenté (rework :48-71) : conteneur pill `borderRadius: T.radiusSm`, segments `borderRadius: T.radiusXs`, `borderCurve:'continuous'` ; segment actif = fond `T.primary` + texte `T.text`, inactif = texte `T.textFaint` ; labels « Points » / « Heatmap » (renommer l'actuel « Map »). Ajouter `haptics.select()` au onPress de CHAQUE segment (S3). Le `FriendSelector` reste dans le slot droit du titre (`leftSlot`/`vueSlot` :43-45). Gaps internes du bandeau = `sm` (8px), padding horizontal des pills = `pillPadX` (12px). Accent `T.primary` réservé (segment actif uniquement ; inactifs en `T.textFaint`/`T.border`). Pattern S1 : `const T = useTheme(); const styles = useMemo(() => makeStyles(T), [T])`, insets via `useSafeAreaInsets()`. Tous les labels en français.
  </action>
  <acceptance_criteria>
    - `rg "borderCurve" components/map/MapHeader.tsx` matche (coins continus)
    - `rg "0\.92|rgba\(10,10,10" components/map/MapHeader.tsx` matche (surface semi-opaque lisible)
    - `rg "BlurView" components/map/MapHeader.tsx` ne matche PAS (anti-blur, règle 13)
    - `rg "Points|Heatmap" components/map/MapHeader.tsx` matche (labels renommés) ; l'ancien label « Map » seul ne subsiste pas comme libellé de segment
    - `rg "haptics\.select" components/map/MapHeader.tsx` matche (≥ 1, idéalement par segment)
    - `rg "T\.radiusLg" components/map/MapHeader.tsx` matche (rayon bandeau via token 02-01)
    - `npx tsc --noEmit` : 0 nouvelle erreur attribuable à MapHeader.tsx
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Bandeau flottant semi-opaque (radiusLg+borderCurve, pas de blur) + titre AppText + toggle segmenté Points/Heatmap + haptics.select() par segment ; tsc sans nouvelle erreur.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Restyler le trigger pill de FriendSelector.tsx (+ haptics.select)</name>
  <files>components/map/FriendSelector.tsx</files>
  <read_first>
    - components/map/FriendSelector.tsx (fichier modifié — trigger :39-46, handleSelect :32-35, styles trigger :106-128, IcoUser :44, body/sheet :48-100 INCHANGÉ)
    - 02-PATTERNS.md §`components/map/FriendSelector.tsx` (restyle trigger uniquement ; body inchangé) + §S3 (haptics)
    - 02-UI-SPEC.md §Bandeau Composition 2 (sélecteur d'ami = pill droite) + §Spacing (pillPadX=12)
  </read_first>
  <action>
    Restyler UNIQUEMENT le trigger pill (:39-46, styles :106-128) : `borderRadius: T.radiusXs` + `borderCurve:'continuous'` (inline), padding horizontal `pillPadX = 12`, fond `T.surface` semi-opaque + bord `1px T.border`, label mono. Conserver l'icône `IcoUser` (:44, SVG custom — S4). Ajouter `haptics.select()` dans `handleSelect` (:32-35). Le corps de la modal/sheet (:48-100) reste INCHANGÉ cette phase. Ajuster le label visible pour s'intégrer au bandeau (UI-SPEC : pill côté droit du titre). Pattern S1 conservé.
  </action>
  <acceptance_criteria>
    - `rg "borderCurve" components/map/FriendSelector.tsx` matche sur le style du trigger
    - `rg "T\.radiusXs" components/map/FriendSelector.tsx` matche
    - `rg "haptics\.select" components/map/FriendSelector.tsx` matche dans handleSelect
    - `rg "IcoUser" components/map/FriendSelector.tsx` matche (icône SVG conservée)
    - Le corps de la modal (lignes ~48-100) reste fonctionnellement inchangé (aucune suppression de la liste d'amis)
    - `npx tsc --noEmit` : 0 nouvelle erreur attribuable à FriendSelector.tsx
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Trigger pill restylé (radiusXs+borderCurve, semi-opaque) + haptics.select() ; modal/body inchangés ; tsc sans nouvelle erreur.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Détacher et restyler le bouton Recentrer dans AppMapView.tsx</name>
  <files>components/map/AppMapView.tsx</files>
  <read_first>
    - components/map/AppMapView.tsx (fichier modifié — recenter button :114-122, styles :129-145, handleRecenter :80-89, showRecenter gate :41/69/114, styleURL :95 INCHANGÉ)
    - 02-PATTERNS.md §`components/map/AppMapView.tsx` (rework Recentrer : détaché bas-droite, squircle 40×40, haptics.tap, a11y) + §S3
    - 02-UI-SPEC.md §Bouton Recentrer (détaché bas-droite au-dessus du FAB) + §Spacing (touch target 40) + §Copywriting (label « Recentrer » / a11y « Recentrer sur ma position »)
  </read_first>
  <action>
    Détacher le bouton Recentrer (UI-SPEC §Bouton Recentrer) : le positionner flottant bas-droite AU-DESSUS du FAB, forme squircle 40×40 (`borderRadius: T.radiusSm` + `borderCurve:'continuous'` inline), fond `T.surface` semi-opaque + bord `1px T.border`, label mono `Recentrer` (ou icône SVG si introduite — alors créer un `Ico*` dans components/icons.tsx, S4 ; sinon garder le label). Ajouter `haptics.tap()` (impact light) dans `handleRecenter` (:80-89). Ajouter `accessibilityLabel="Recentrer sur ma position"` (le label visible reste court). Conserver la garde conditionnelle `showRecenter` existante (:41/69/114). NE PAS toucher à `styleURL={APP_CONFIG.MAPBOX_STYLE}` (:95, déjà correct pour MAP-01 — géré au plan 02-05). Pattern S1 conservé.
  </action>
  <acceptance_criteria>
    - `rg "borderCurve" components/map/AppMapView.tsx` matche sur le bouton recentrer
    - `rg "T\.radiusSm" components/map/AppMapView.tsx` matche
    - `rg "haptics\.tap" components/map/AppMapView.tsx` matche dans handleRecenter
    - `rg "accessibilityLabel=\"Recentrer sur ma position\"" components/map/AppMapView.tsx` matche
    - `rg "showRecenter" components/map/AppMapView.tsx` matche toujours (garde conservée)
    - `rg "styleURL" components/map/AppMapView.tsx` matche toujours sur APP_CONFIG.MAPBOX_STYLE (non modifié)
    - `npx tsc --noEmit` : 0 nouvelle erreur attribuable à AppMapView.tsx
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Bouton Recentrer détaché bas-droite, squircle 40×40 (radiusSm+borderCurve), haptics.tap() + a11y ; showRecenter et styleURL inchangés ; tsc sans nouvelle erreur.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (aucune nouvelle) | Overlays RN au-dessus de la carte (toggle/sélecteur/recentrer). Pas d'entrée non fiable, pas de réseau nouveau. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure | MapHeader titre « carte de {nom} » | accept | Le nom d'ami affiché provient déjà des amitiés acceptées (RLS) ; rien de nouveau exposé. |
| T-02-06 | Tampering | Surface semi-opaque (lisibilité, règle 13) | mitigate | Surface ~0.92 alpha LISIBLE imposée (pas de BlurView) → contrôles toujours lisibles, conforme à la leçon règle 13. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept (N/A) | Zéro paquet installé ; dépendances déjà vérifiées (Phases 0/1). |
</threat_model>

<verification>
- Statique : `npx tsc --noEmit` (0 nouvelle erreur) + assertions rg par task.
- Manuel device (gate de phase) : bandeau flottant lisible (pas de blur lourd, texte net) ; toggle Points/Heatmap fonctionnel avec haptic ; sélecteur d'ami pill arrondi + haptic ; bouton Recentrer bas-droite arrondi + haptic light. Tester dark ET light (aucun hex en dur hors data-viz).
</verification>

<success_criteria>
- Bandeau éditorial flottant lisible (radiusLg+borderCurve, semi-opaque, pas de blur) + toggle Points/Heatmap.
- Sélecteur d'ami + Recentrer arrondis continus avec haptiques mappées (select/tap).
- Recentrer détaché bas-droite + a11y.
- tsc vert ; checklist device cochée au gate de phase.
</success_criteria>

<output>
Create `.planning/phases/02-carte-stylis-e/02-04-SUMMARY.md` when done
</output>
