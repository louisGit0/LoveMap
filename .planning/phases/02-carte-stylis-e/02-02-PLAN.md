---
phase: 02-carte-stylis-e
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - constants/config.ts
  - components/map/HeatmapLayer.tsx
autonomous: true
requirements: [MAP-02]
must_haves:
  truths:
    - "En mode heatmap, le dégradé va du rose froid (périphérie) à l'ambre/or chaud (cœur dense)"
    - "L'opacité de la heatmap décroît au zoom proche (lueur douce, pas un aplat ni une disparition)"
    - "Le premier stop de heatmapColor à densité 0 reste transparent (pas de teinte de toute la carte)"
  artifacts:
    - path: "constants/config.ts"
      provides: "Constantes data-viz ambre centralisées (#ffb020, #ffc24d) hors tokens dark/light"
      contains: "ffb020"
    - path: "components/map/HeatmapLayer.tsx"
      provides: "Expressions GL heatmapColor rose→ambre + heatmapOpacity interpolée sur le zoom"
      contains: "heatmap-density"
  key_links:
    - from: "components/map/HeatmapLayer.tsx (heatmapColor)"
      to: "heatmap-density"
      via: "interpolate linear sur ['heatmap-density'] avec premier stop transparent"
      pattern: "rgba\\(0,0,0,0\\)"
    - from: "components/map/HeatmapLayer.tsx (heatmapOpacity)"
      to: "zoom"
      via: "interpolate linear sur ['zoom'] décroissant 0.85→0.25"
      pattern: "heatmapOpacity"
---

<objective>
Refondre la heatmap de l'écran carte en dégradé « braise » rose→ambre cohérent avec l'identité, avec une opacité qui s'atténue en lueur douce aux zooms élevés (MAP-02 / D-08 / D-09). Tranche visible : en mode heatmap, l'utilisateur voit le nouveau dégradé et l'effet de lueur au zoom.

Purpose: Remplacer l'actuel violet→#e91e8c→orange-rouge par le dégradé braise verrouillé dans le UI-SPEC ; rendre la densité lisible (rose en périphérie, ambre sur les zones où les moments se superposent).
Output: `constants/config.ts` (hex ambre data-viz centralisés) + `components/map/HeatmapLayer.tsx` (expressions GL mises à jour).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-carte-stylis-e/02-UI-SPEC.md
@.planning/phases/02-carte-stylis-e/02-RESEARCH.md
@.planning/phases/02-carte-stylis-e/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Centraliser les constantes ambre data-viz dans constants/config.ts</name>
  <files>constants/config.ts</files>
  <read_first>
    - constants/config.ts (fichier modifié — bloc COLORS :8-16, MAPBOX_STYLE :4, mapBg :14)
    - 02-PATTERNS.md §`constants/config.ts` (centraliser #ffb020/#ffc24d/#08080a ; `mapBg #0d1a2e` rejeté par D-03 — ne plus l'utiliser)
    - 02-UI-SPEC.md §Color (data-viz hot = #ffb020 → #ffc24d, base carte #08080a)
  </read_first>
  <action>
    Ajouter au bloc `COLORS` (ou un nouveau const `MAP_COLORS` exporté) les constantes data-viz ambre du UI-SPEC §Color : `emberAmber = '#ffb020'` et `emberGold = '#ffc24d'`, plus la base carte `mapBaseDark = '#08080a'` (documentée comme base Studio — référence, non rendue par le code RN). Ce sont des constantes data-viz intentionnellement HORS des tokens dark/light `T` (elles ne changent pas selon le thème). Ne PAS référencer `mapBg: '#0d1a2e'` (rejeté par D-03) — le laisser tel quel ou le retirer, mais ne plus l'utiliser. Ne pas toucher à `MAPBOX_STYLE` (:4, déjà câblé pour MAP-01 — géré au plan 02-05). Tous les commentaires/labels restent cohérents avec le code existant.
  </action>
  <acceptance_criteria>
    - `rg "ffb020|ffc24d" constants/config.ts` matche (constantes ambre présentes)
    - `rg "08080a" constants/config.ts` matche (base carte documentée)
    - Les nouvelles constantes sont exportées (consommables par HeatmapLayer) : `rg "export" constants/config.ts` couvre le bloc ajouté
    - `npx tsc --noEmit` : 0 nouvelle erreur attribuable à config.ts
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Constantes ambre data-viz (#ffb020/#ffc24d) + base #08080a centralisées et exportées dans config.ts ; tsc sans nouvelle erreur.</done>
</task>

<task type="auto">
  <name>Task 2: Remplacer les expressions GL dans HeatmapLayer.tsx (rose→ambre + falloff zoom)</name>
  <files>components/map/HeatmapLayer.tsx</files>
  <read_first>
    - components/map/HeatmapLayer.tsx (fichier modifié — useMemo GeoJSON :10-25, expressions :35-54, cast `as any` :55)
    - 02-PATTERNS.md §`components/map/HeatmapLayer.tsx` (stops exacts verrouillés — table « Replace with »)
    - 02-UI-SPEC.md §Heatmap (table heatmapColor par densité + table heatmapOpacity par zoom + autres params)
    - 02-RESEARCH.md Unknown #2 (premier stop transparent OBLIGATOIRE ; heatmapColor non data-driven ; conserver `as any`)
  </read_first>
  <action>
    Dans l'objet `style={{...} as any}` (:35-54), remplacer les expressions par les stops verrouillés du UI-SPEC §Heatmap : `heatmapColor` = interpolate linear sur `['heatmap-density']` avec stops 0.0→`rgba(0,0,0,0)` (transparent, PREMIER STOP OBLIGATOIRE — Pitfall 3 sinon toute la carte se teinte), 0.1→`rgba(255,45,135,0.15)`, 0.3→`rgba(255,45,135,0.55)`, 0.5→`#ff5a7a`, 0.7→`#ff8a4c`, 0.9→`#ffb020`, 1.0→`#ffc24d` (consommer `emberAmber`/`emberGold` importés de config.ts pour les deux derniers stops). `heatmapOpacity` passe de scalaire `0.85` à un **tableau** interpolate linear sur `['zoom']` : 10→0.85, 13→0.80, 15→0.55, 17→0.35, 19→0.25 (décroissant → lueur douce, D-09). `heatmapRadius` interpolate zoom : 10→22, 14→44, 18→70. `heatmapIntensity` interpolate zoom : 10→0.8, 14→1.4. `heatmapWeight` INCHANGÉ (`['interpolate',['linear'],['get','weight'],0,0,1,1]`). Conserver le cast `as any` (:55) et le `useMemo` GeoJSON (:10-25, perf). Ne PAS rendre heatmapColor data-driven (impossible — RESEARCH Unknown #2). Le toggle pins/heatmap reste mutuellement exclusif (géré dans map/index.tsx — hors de ce fichier).
  </action>
  <acceptance_criteria>
    - `rg "rgba\(0,0,0,0\)" components/map/HeatmapLayer.tsx` matche (premier stop transparent conservé)
    - `rg "ffc24d|emberGold" components/map/HeatmapLayer.tsx` matche (cœur ambre/or présent)
    - `rg "e91e8c|ff5722|156,39,176" components/map/HeatmapLayer.tsx` ne matche PLUS (ancien dégradé violet/orange supprimé)
    - `rg "heatmapOpacity" components/map/HeatmapLayer.tsx` suivi d'une expression `interpolate` sur `['zoom']` (plus la valeur scalaire 0.85)
    - `rg "as any" components/map/HeatmapLayer.tsx` matche (cast conservé)
    - `npx tsc --noEmit` : 0 nouvelle erreur attribuable à HeatmapLayer.tsx
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>heatmapColor rose→ambre sur heatmap-density (premier stop transparent), heatmapOpacity interpolée décroissante sur zoom, radius/intensity ajustés, weight inchangé, cast as any conservé ; tsc sans nouvelle erreur.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (aucune nouvelle) | Rendu GPU d'expressions GL sur des données `points` déjà chargées via le hook existant. Pas d'entrée non fiable, pas de réseau nouveau. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Information Disclosure | HeatmapLayer (agrégat de positions) | accept | La heatmap agrège les `points` déjà visibles par RLS (rien de nouveau exposé) ; aucune position individuelle ajoutée. Comportement RLS inchangé. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept (N/A) | Zéro paquet installé (RESEARCH §Standard Stack) ; aucune surface supply-chain. |
</threat_model>

<verification>
- Statique : `npx tsc --noEmit` (0 nouvelle erreur) + assertions rg ci-dessus.
- Manuel device (gate de phase) : mode heatmap, zoomer/dézoomer → dégradé braise rose→ambre, opacité décroît en lueur douce au zoom proche (carte visible dessous), aucune teinte rose globale. À valider sur device physique / TestFlight (le rendu GPU diffère du simulateur — Pitfall 5).
</verification>

<success_criteria>
- Dégradé heatmap rose→ambre conforme aux stops UI-SPEC, premier stop transparent.
- Opacité décroissante au zoom (falloff lueur douce) via interpolate sur ['zoom'].
- tsc vert ; vérif device cochée au gate de phase.
</success_criteria>

<output>
Create `.planning/phases/02-carte-stylis-e/02-02-SUMMARY.md` when done
</output>
