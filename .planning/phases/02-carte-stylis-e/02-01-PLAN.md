---
phase: 02-carte-stylis-e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - constants/theme.ts
  - CLAUDE.md
autonomous: true
requirements: [UI-02]
must_haves:
  truths:
    - "Le Theme expose une échelle de rayons radiusXs..Xl + fab (mêmes valeurs dark et light)"
    - "cardRadius vaut 16 (était 4) et pill vaut 999 (était 4)"
    - "CLAUDE.md §Identité visuelle décrit les formes iOS arrondies + borderCurve, plus « angles francs »"
  artifacts:
    - path: "constants/theme.ts"
      provides: "Tokens de rayon D-12 (radiusXs/Sm/Md/Lg/Xl, fab, cardRadius=16, pill=999) sur le type Theme + darkTheme + lightTheme"
      contains: "radiusLg"
    - path: "CLAUDE.md"
      provides: "Conventions §Identité visuelle révisées (D-12) + ligne phases-table Phase 2"
      contains: "borderCurve"
  key_links:
    - from: "constants/theme.ts (type Theme)"
      to: "darkTheme + lightTheme"
      via: "mêmes clés radius présentes dans les deux objets"
      pattern: "radiusXl:\\s*28"
---

<objective>
Verrouiller le pivot design D-12 « éditorial sombre + formes iOS arrondies » au niveau du design system : étendre les tokens de rayon dans `constants/theme.ts` et réviser la §Identité visuelle de `CLAUDE.md`. C'est le contrat de formes consommé par les plans 02-03 (FAB squircle) et 02-04 (bandeau, recentrer, pills, toggle) — il doit exister avant eux.

Purpose: Remplacer l'ancienne règle « angles francs / borderRadius:0 » par l'échelle de rayons iOS (cross-phase 2→5). Sans ces tokens, le FAB et le bandeau ne peuvent pas être arrondis de façon cohérente.
Output: `constants/theme.ts` étendu (radius scale) + `CLAUDE.md` §Identité visuelle révisée.

Cadrage MVP : ce plan est la **lock du langage de formes** consommé par les tranches visibles 02-03/02-04. Il n'est pas une tranche visible en soi (pur contrat/config + doc), exception assumée pour un pivot de design system mandaté par le CONTEXT comme tâche Phase 2 (D-12).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-carte-stylis-e/02-UI-SPEC.md
@.planning/phases/02-carte-stylis-e/02-PATTERNS.md
@.planning/phases/02-carte-stylis-e/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Étendre les tokens de rayon D-12 dans constants/theme.ts</name>
  <files>constants/theme.ts</files>
  <read_first>
    - constants/theme.ts (fichier modifié — type Theme :1-15, darkTheme :17-31 cardRadius/pill :29-30, lightTheme :33-47 :45-46, export T :50)
    - 02-PATTERNS.md §`constants/theme.ts` (valeurs verrouillées + « ne pas ajouter borderCurve ici, ne pas toucher aux couleurs »)
    - 02-UI-SPEC.md §D-12 « Échelle de rayons » (table token→value→usage)
    - 02-RESEARCH.md Unknown #3 (borderCurve est un per-style prop, PAS un token — n'entre pas dans le type Theme)
  </read_first>
  <action>
    Étendre le type `Theme` avec six clés numériques : `radiusXs`, `radiusSm`, `radiusMd`, `radiusLg`, `radiusXl`, `fab`. Ajouter ces clés dans `darkTheme` ET `lightTheme` avec des valeurs identiques : `radiusXs:8, radiusSm:12, radiusMd:16, radiusLg:22, radiusXl:28, fab:18`. Réviser les valeurs existantes dans les deux objets : `cardRadius: 4 → 16`, `pill: 4 → 999`. Ne PAS ajouter `borderCurve` (per-style prop, pas un token — RESEARCH Unknown #3). Ne PAS toucher aux couleurs ni à `secondary/success/danger`. Conserver l'export de rétrocompat `export const T = darkTheme` (:50). Les valeurs radius doivent être strictement identiques entre dark et light (seules les couleurs diffèrent entre thèmes).
  </action>
  <acceptance_criteria>
    - `rg "radiusXs|radiusSm|radiusMd|radiusLg|radiusXl|fab" constants/theme.ts` retourne ≥ 3 occurrences de chaque (type + dark + light)
    - `rg "radiusXl:\s*28" constants/theme.ts` matche exactement 2 fois (dark + light)
    - `rg "cardRadius:\s*16" constants/theme.ts` matche 2 fois ; `rg "pill:\s*999" constants/theme.ts` matche 2 fois
    - `rg "cardRadius:\s*4|pill:\s*4" constants/theme.ts` ne matche plus (anciennes valeurs supprimées)
    - `rg "borderCurve" constants/theme.ts` ne matche pas (token absent du Theme — discipline RESEARCH Unknown #3)
    - `npx tsc --noEmit` : aucune NOUVELLE erreur attribuable à theme.ts vs baseline (~38)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Type Theme étendu + darkTheme + lightTheme contiennent radiusXs..Xl + fab avec valeurs verrouillées identiques ; cardRadius=16, pill=999 ; tsc sans nouvelle erreur.</done>
</task>

<task type="auto">
  <name>Task 2: Réviser CLAUDE.md §Identité visuelle pour le pivot D-12</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md §« Identité visuelle — Design Éditorial » (conventions « angles francs / borderRadius:0 », table tokens T.cardRadius=4/pill=4, « Avatars : carrés », « Inputs : underline only ») + table §Phases de développement
    - 02-PATTERNS.md §`CLAUDE.md §Identité visuelle` (table Old convention → New D-12)
    - 02-UI-SPEC.md §D-12 « CLAUDE.md à réviser » (table conventions)
  </read_first>
  <action>
    Réviser la §Identité visuelle de CLAUDE.md selon la table D-12 du UI-SPEC : remplacer « angles francs » / `T.cardRadius = 4` par « formes iOS arrondies » avec l'échelle `radiusXs..Xl` + `cardRadius = 16` + la discipline « tout borderRadius ≥ radiusSm s'accompagne de `borderCurve:'continuous'` ». Mettre à jour la table de tokens : `cardRadius` 4→16, `pill` 4→999, et ajouter les entrées radiusXs(8)/radiusSm(12)/radiusMd(16)/radiusLg(22)/radiusXl(28)/fab(18). Mentionner explicitement le FAB squircle (`fab=18`) en remplacement du FAB carré. **Ne PAS** modifier les conventions « Avatars : carrés (borderRadius:0) » ni « Inputs : underline only » — elles restent INCHANGÉES cette phase (réévaluation Phases 5/3 — UI-SPEC §D-12). Ajouter une ligne à la table §Phases de développement pour la Phase 2 (Carte stylisée) en statut en cours/terminé selon l'avancement. Tous les textes restent en français.
  </action>
  <acceptance_criteria>
    - `rg "borderCurve" CLAUDE.md` matche (nouvelle convention documentée)
    - `rg "radiusLg|radiusXl|squircle" CLAUDE.md` matche (échelle + forme FAB documentées)
    - `rg "Avatars\s*:\s*carrés|underline only" CLAUDE.md` matche TOUJOURS (avatars/inputs inchangés — non arrondis cette phase)
    - La table §Phases contient une ligne Phase 2 « Carte stylisée »
    - Aucune occurrence résiduelle affirmant « angles francs » comme règle active pour cards/FAB
  </acceptance_criteria>
  <verify>
    <automated>rg -n "borderCurve|squircle|radiusLg" CLAUDE.md</automated>
  </verify>
  <done>§Identité visuelle révisée (formes arrondies + borderCurve + échelle de rayons + FAB squircle), avatars/inputs préservés, ligne Phase 2 ajoutée à la table phases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (aucune nouvelle) | Phase de présentation : tokens de design + documentation. Aucune entrée utilisateur, réseau ou frontière de confiance nouvelle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | constants/theme.ts (valeurs de tokens) | accept | Constantes statiques sans donnée sensible ; régression purement visuelle, captée par tsc + revue device. Pas de surface d'attaque. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept (N/A) | **Zéro paquet installé cette phase** (RESEARCH §Standard Stack). Aucune surface supply-chain ; checkpoint de légitimité non requis. |
</threat_model>

<verification>
- `npx tsc --noEmit` : 0 nouvelle erreur vs baseline (~38).
- Assertions source rg (cf. acceptance_criteria de chaque task).
- Aucune vérif device nécessaire pour ce plan (contrat/doc) — les effets visuels sont vérifiés via les plans 02-03/02-04 qui consomment ces tokens.
</verification>

<success_criteria>
- Theme expose radiusXs..Xl + fab (identiques dark/light), cardRadius=16, pill=999.
- CLAUDE.md §Identité visuelle reflète le pivot D-12 (borderCurve + échelle), avatars/inputs préservés.
- tsc vert (0 nouvelle erreur).
</success_criteria>

<output>
Create `.planning/phases/02-carte-stylis-e/02-01-SUMMARY.md` when done
</output>
