---
phase: 02-carte-stylis-e
plan: 05
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [MAP-01]
user_setup:
  - service: mapbox-studio
    why: "Le style de carte sur-mesure noir/rose est créé et hébergé dans Mapbox Studio (service externe) ; le code ne référence qu'une URL"
    env_vars:
      - name: EXPO_PUBLIC_MAPBOX_STYLE
        source: "URL mapbox://styles/<user>/<id> publiée depuis Mapbox Studio (à coller dans .env.local + EAS env)"
    dashboard_config:
      - task: "Créer le style depuis la recette UI-SPEC §Mapbox Studio (base Mapbox Dark + hex par calque + densité de labels), puis publier"
        location: "Mapbox Studio (studio.mapbox.com)"
must_haves:
  truths:
    - "La carte affiche le style sur-mesure : fond anthracite quasi-noir, touches rose désaturées (eau, grands axes), labels minimalistes"
    - "L'URL mapbox://styles/... est injectée via EXPO_PUBLIC_MAPBOX_STYLE et livrée par rebundle/OTA (pas de rebuild natif)"
  artifacts:
    - path: ".planning/phases/02-carte-stylis-e/02-MANUAL-CHECKLIST.md"
      provides: "Checklist device de gate de phase (MAP-01/02/03 + UI-02) — artefact de vérification manuelle"
      contains: "MAP-01"
  key_links:
    - from: "EXPO_PUBLIC_MAPBOX_STYLE (env, inliné au bundle)"
      to: "APP_CONFIG.MAPBOX_STYLE → styleURL (AppMapView.tsx:95)"
      via: "déjà câblé — seule la valeur d'URL change"
      pattern: "MAPBOX_STYLE"
---

<objective>
Faire vivre le style Mapbox sur-mesure noir/rose (MAP-01 / D-01..D-04). La création du style est une étape MANUELLE utilisateur (checkpoint humain) : Claude fournit la recette Studio (déjà verrouillée dans le UI-SPEC), l'utilisateur la reproduit dans Mapbox Studio, publie, et fournit l'URL `mapbox://styles/...` injectée dans `EXPO_PUBLIC_MAPBOX_STYLE`. Le code ne change qu'une valeur d'URL (déjà câblée via `APP_CONFIG.MAPBOX_STYLE`).

Purpose: La signature visuelle de la carte (fond anthracite, touches rose désaturées, labels minimaux) dépend du style Studio. Ce plan livre la recette à l'utilisateur, gère l'injection OTA-safe de l'URL, et vérifie le rendu end-to-end sur device. Il ne bloque PAS les plans code (02-01..04), qui tournent contre le fallback `dark-v11`.
Output: Style Studio publié (côté utilisateur) + `EXPO_PUBLIC_MAPBOX_STYLE` mis à jour + checklist device de gate de phase.

Indépendant des plans code (aucun fichier en commun). Peut démarrer dès le début de la phase (tâche manuelle longue) ; sa vérification end-to-end peut conclure la phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-carte-stylis-e/02-UI-SPEC.md
@.planning/phases/02-carte-stylis-e/02-RESEARCH.md
@.planning/phases/02-carte-stylis-e/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rédiger la checklist device de gate de phase (artefact de validation manuelle)</name>
  <files>.planning/phases/02-carte-stylis-e/02-MANUAL-CHECKLIST.md</files>
  <read_first>
    - 02-VALIDATION.md §Manual-Only Verifications (4 lignes MAP-01/02/03 + UI-02) + §Sampling Rate
    - 02-UI-SPEC.md §Mapbox Studio (principes de rendu attendus) + §Heatmap + §Markers + §FAB/§Bandeau
    - 02-RESEARCH.md §Validation Architecture (Wave 0 : « author a short manual TestFlight checklist ») + Pitfall 5 (device-only)
  </read_first>
  <action>
    Créer `02-MANUAL-CHECKLIST.md` : une checklist device (TestFlight / device physique) avec une ligne cochable par requirement de la phase, dérivée du UI-SPEC. MAP-01 : fond anthracite quasi-noir, touches rose désaturées (eau/grands axes), labels minimalistes (villes/quartiers/grandes artères conservés, POI/rues mineures retirés). MAP-02 : mode heatmap → dégradé braise rose→ambre, opacité décroît en lueur douce au zoom proche, aucune teinte rose globale. MAP-03 : pins visibles à TOUS les zooms (garde STAB-02), tap → agrandissement + halo, chargement → cascade staggered. UI-02 : FAB squircle (pas carré), scale-on-press + haptic medium ; bandeau lisible (pas de blur lourd) ; toggle/sélecteur/recentrer arrondis + haptiques. Inclure la note Pitfall 5 (valider sur device physique, pas simulateur). Tous les libellés en français.
  </action>
  <acceptance_criteria>
    - Le fichier `.planning/phases/02-carte-stylis-e/02-MANUAL-CHECKLIST.md` existe
    - `rg "MAP-01|MAP-02|MAP-03|UI-02" .planning/phases/02-carte-stylis-e/02-MANUAL-CHECKLIST.md` couvre les 4 requirements
    - `rg "STAB-02|tous les zooms|device physique" .planning/phases/02-carte-stylis-e/02-MANUAL-CHECKLIST.md` matche (garde régression + device-only)
  </acceptance_criteria>
  <verify>
    <automated>rg -n "MAP-01|MAP-02|MAP-03|UI-02|STAB-02" .planning/phases/02-carte-stylis-e/02-MANUAL-CHECKLIST.md</automated>
  </verify>
  <done>Checklist device créée couvrant MAP-01/02/03 + UI-02 + garde STAB-02, avec note device-only.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Checkpoint humain — créer le style Mapbox Studio, injecter l'URL, vérifier le rendu device</name>
  <files>.env.local (côté utilisateur — non commité)</files>
  <action>
    CHECKPOINT HUMAIN (Claude ne peut pas créer le style à la place de l'utilisateur — Studio = service externe sans API d'authoring dans ce flux). L'utilisateur reproduit la recette `02-UI-SPEC.md §Mapbox Studio` dans Mapbox Studio, publie, colle l'URL `mapbox://styles/<user>/<id>` dans `EXPO_PUBLIC_MAPBOX_STYLE` (.env.local + EAS env), puis livre par OTA pur-JS (`eas update --branch main`, AUCUN bump runtimeVersion, AUCUN rebuild natif — RESEARCH Unknown #4 / Pitfall 4), et vérifie le rendu sur device. Voir les blocs what-built / how-to-verify ci-dessous.
  </action>
  <what-built>
    La recette de style Mapbox Studio est déjà entièrement verrouillée dans `02-UI-SPEC.md §Mapbox Studio` (base Mapbox Dark, hex par calque, règles de densité de labels). Le câblage code est déjà en place : `APP_CONFIG.MAPBOX_STYLE` (constants/config.ts:4) → `styleURL` (AppMapView.tsx:95). Claude ne peut PAS créer le style à la place de l'utilisateur.
  </what-built>
  <how-to-verify>
    1. Ouvrir Mapbox Studio (studio.mapbox.com), créer un style à partir de **Mapbox Dark**.
    2. Appliquer la recette `02-UI-SPEC.md §Mapbox Studio` :
       - Background/land `#08080a` ; water `#241019` (rose-prune désaturé) ; building fill `#0e0d10`.
       - Roads motorway/trunk/primary `#3a2630` (visibles) ; secondary/tertiary `#1e1a1e` ; minor/residential `#161416`.
       - Labels place/quartier/ville `#8a8a8a` (halo `#000` 1px) ; grandes artères `#b0a0a8`.
       - Densité : CONSERVER pays/villes/communes/quartiers/grandes artères ; RETIRER POI, rues mineures, transit, numéros ; icônes POI désactivées.
    3. **Publier** le style et copier l'URL `mapbox://styles/<user>/<id>`.
    4. Coller l'URL dans `.env.local` → `EXPO_PUBLIC_MAPBOX_STYLE` ET dans l'env EAS (le secret/var de bundle).
    5. **Rebundle/OTA** : `EXPO_PUBLIC_*` est inliné au bundle → lancer `eas update --branch main` (OTA pur-JS, AUCUN bump runtimeVersion, AUCUN rebuild natif — RESEARCH Unknown #4 / Pitfall 4) OU rebuild. Le token `pk.*` existant charge un style privé sans changement ; le `sk.*` (download natif) est inchangé.
    6. Ouvrir la carte sur device → vérifier la ligne MAP-01 de `02-MANUAL-CHECKLIST.md` : fond anthracite + touches rose désaturées + labels minimalistes.
  </how-to-verify>
  <resume-signal>Tapez « approved » (URL publiée + injectée + rendu device conforme) ou décrivez l'écart de rendu pour ajustement de la recette.</resume-signal>
  <verify>
    <human-check>Rendu device : style sur-mesure visible (anthracite + rose désaturé + labels minimaux), ligne MAP-01 de 02-MANUAL-CHECKLIST.md cochée.</human-check>
  </verify>
  <done>Style Studio publié + EXPO_PUBLIC_MAPBOX_STYLE injecté (.env.local + EAS) + livré par OTA/rebundle + rendu device conforme à la recette.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → Mapbox (service externe) | La carte charge un style distant via `styleURL`. Frontière existante (déjà utilisée pour `dark-v11`), pas nouvelle — seule l'URL change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Information Disclosure | Token `pk.*` (EXPO_PUBLIC_MAPBOX_TOKEN) inliné dans le bundle | accept | Token PUBLIC à scope `styles:read` (déjà en place, inchangé) ; conçu pour être exposé côté client. Le `sk.*` (download natif) n'est PAS exposé (secret EAS, inchangé). |
| T-02-08 | Tampering | EXPO_PUBLIC_MAPBOX_STYLE (valeur d'URL) | accept | URL de référence de style (pas un secret) ; inlinée au bundle, livrée par OTA pur-JS sans bump runtimeVersion. Une mauvaise valeur dégrade le rendu (visible immédiatement), pas de risque de données. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept (N/A) | Zéro paquet installé cette phase. |
</threat_model>

<verification>
- Code : aucun changement de fichier source (URL déjà câblée) → pas de gate tsc spécifique ici (le câblage est vérifié par les plans code).
- Manuel device (gate de phase, bloquant) : après injection de l'URL + OTA, la carte affiche le style sur-mesure (ligne MAP-01 de `02-MANUAL-CHECKLIST.md`). Valider sur device physique / TestFlight (Pitfall 5).
- Vérifier que le changement d'URL a bien été livré par rebundle/OTA (Pitfall 4 : éditer `.env.local` sans rebundle ne change rien dans un binaire déjà construit).
</verification>

<success_criteria>
- Style Studio publié conforme à la recette UI-SPEC (anthracite + rose désaturé + labels minimaux).
- `EXPO_PUBLIC_MAPBOX_STYLE` mis à jour (.env.local + EAS) et livré par OTA/rebundle.
- Rendu device conforme (ligne MAP-01 de la checklist cochée).
- Checklist device de gate de phase créée et disponible.
</success_criteria>

<output>
Create `.planning/phases/02-carte-stylis-e/02-05-SUMMARY.md` when done
</output>
