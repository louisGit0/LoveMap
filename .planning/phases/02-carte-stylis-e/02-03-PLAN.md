---
phase: 02-carte-stylis-e
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - components/map/PointMarker.tsx
  - app/(app)/map/index.tsx
autonomous: true
requirements: [MAP-03, UI-02]
must_haves:
  truths:
    - "Le pin au repos est raffiné (tête 24, point 9, tige 8, halo statique léger) et reste visible à tous les zooms"
    - "Au tap, le pin s'affiche en variante agrandie + halo rose (via re-snapshot refresh(), pas une transform animée)"
    - "Au chargement, les pins apparaissent en cascade (montage staggered), jamais de carte sans pins quand la preview est ouverte"
    - "Le FAB est un squircle 56/r18 (pas carré, pas Material), scale spring au tap, et déclenche haptics.press()"
  artifacts:
    - path: "components/map/PointMarker.tsx"
      provides: "Pin raffiné + variante PinIconSelected + onSelected/onDeselected + ref.refresh()"
      contains: "refresh"
    - path: "app/(app)/map/index.tsx"
      provides: "Montage staggered des markers + FAB squircle reanimated + haptics.press()/error()"
      contains: "borderCurve"
  key_links:
    - from: "components/map/PointMarker.tsx (selected state)"
      to: "PointAnnotation bitmap"
      via: "useEffect keyed sur selected → annRef.current.refresh()"
      pattern: "\\.refresh\\(\\)"
    - from: "app/(app)/map/index.tsx (FAB onPress)"
      to: "lib/haptics.ts + reanimated"
      via: "haptics.press() + withSpring sur transform scale"
      pattern: "haptics\\.press"
---

<objective>
Retravailler les markers de la carte (pin raffiné, état sélectionné agrandi+halo, apparition en cascade) sur `PointAnnotation` (MAP-03 / D-05/06/07) et refondre le FAB en squircle iOS animé avec haptique (UI-02 / D-11). Tranche visible : pins plus nets, sélection tactile au tap, cascade au chargement, et un FAB non-Material qui réagit au toucher.

Purpose: Donner aux markers un design soigné et un retour de sélection, dans la contrainte snapshot de `PointAnnotation` (re-snapshot via `refresh()`, montage staggered — PAS de transform reanimated dans les enfants de l'annotation, qui ne s'anime pas). Le FAB passe de carré sans feedback à un squircle avec micro-anim + haptic.
Output: `components/map/PointMarker.tsx` (pin + sélection) + `app/(app)/map/index.tsx` (cascade + FAB).

Note dépendance : consomme `T.fab` (FAB) du plan 02-01 → wave 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-carte-stylis-e/02-UI-SPEC.md
@.planning/phases/02-carte-stylis-e/02-RESEARCH.md
@.planning/phases/02-carte-stylis-e/02-PATTERNS.md
@.planning/phases/02-carte-stylis-e/02-01-SUMMARY.md

<interfaces>
<!-- Tokens consommés depuis constants/theme.ts (posés par 02-01) — pas d'exploration nécessaire -->
T.fab = 18 (rayon squircle FAB) · T.primary = #ff2d87 · T.bg · T.text · T.border
Discipline D-12 : tout borderRadius ≥ radiusSm ajoute borderCurve:'continuous' INLINE (jamais un token).

<!-- API lib/haptics.ts (Phase 1) — intent, fire-and-forget, pas d'await/try-catch -->
haptics.press()  // impact medium — FAB (création)
haptics.error()  // notification error — branche erreur de chargement
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Raffiner PinIcon + variante sélectionnée + refresh() dans PointMarker.tsx</name>
  <files>components/map/PointMarker.tsx</files>
  <read_first>
    - components/map/PointMarker.tsx (fichier modifié — PinIcon :47-69, annotation :83-90, commentaire MarkerView régression :79-82, Modal preview :92-176)
    - 02-PATTERNS.md §`components/map/PointMarker.tsx` (rework détaillé : dims pin repos + PinIconSelected + bloc selected/refresh) + §Shared Pattern S1 (useTheme/makeStyles)
    - 02-UI-SPEC.md §Markers (Pin au repos / État sélectionné / Animation d'apparition)
    - 02-RESEARCH.md Unknown #1 + Pattern 1 + Pitfall 2 (snapshot : variant swap + refresh() ; AVOID reanimated sur enfants ; AVOID MarkerView)
  </read_first>
  <action>
    Raffiner `PinIcon` (D-05) aux dimensions UI-SPEC : tête 24×24 `borderRadius:12` fond `T.bg` bord `2px T.primary`, point intérieur 9×9 plein `T.primary`, tige 2×8 `T.primary`, point bas 4×4 `T.primary`, halo statique léger via `shadowColor:T.primary, shadowRadius:6, shadowOpacity:0.5, shadowOffset:{0,0}` (capturé dans le snapshot, statique). Ajouter une variante `PinIconSelected` (D-06) : anneau halo 44×44 derrière la tête (fond `rgba(255,45,135,0.12)`, bord `1px rgba(255,45,135,0.45)`) avec tête 30×30 (≈×1.25) bord `2px T.primary`. Câbler l'état sélectionné par RE-SNAPSHOT, pas par animation : `const annRef = useRef<MapboxGL.PointAnnotation>(null)` + `const [selected, setSelected] = useState(false)` + `useEffect(() => { annRef.current?.refresh(); }, [selected])` (Pitfall 2 : refresh() obligatoire). Sur l'annotation : passer `ref={annRef}`, `selected={selected}`, `onSelected={() => { setSelected(true); setModalVisible(true); }}`, `onDeselected={() => setSelected(false)}`, enfant conditionnel `{selected ? <PinIconSelected T={T} /> : <PinIcon T={T} />}`. Conserver `anchor={{ x: 0.5, y: 1 }}` et `id={point.id}`. NE PAS mettre de transform reanimated/Animated sur les enfants du PointAnnotation (snapshot gèle → no-op). NE PAS réintroduire MarkerView (régression projet, commentaire :79-82). La Modal preview (:92-176) reste INCHANGÉE cette phase (sheet natif = Phase 3). Pattern S1 : `const T = useTheme(); const styles = useMemo(() => makeStyles(T), [T]);`, couleurs via `T.xxx`.
  </action>
  <acceptance_criteria>
    - `rg "PinIconSelected" components/map/PointMarker.tsx` matche (variante sélectionnée définie)
    - `rg "\.refresh\(\)" components/map/PointMarker.tsx` matche dans un useEffect keyé sur selected
    - `rg "onSelected|onDeselected|selected=" components/map/PointMarker.tsx` couvre les trois props
    - `rg "MarkerView" components/map/PointMarker.tsx` n'apparaît pas comme rendu actif (seulement éventuellement dans le commentaire de régression)
    - Aucun `useAnimatedStyle`/`Animated.View`/`withSpring` ajouté sur les enfants de PointAnnotation
    - `npx tsc --noEmit` : 0 nouvelle erreur attribuable à PointMarker.tsx
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>PinIcon raffiné + PinIconSelected + selected state via onSelected/onDeselected + ref.refresh() ; Modal inchangée ; pas de transform animée dans l'annotation ; tsc sans nouvelle erreur.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Montage staggered des markers (cascade D-07) dans map/index.tsx</name>
  <files>app/(app)/map/index.tsx</files>
  <read_first>
    - app/(app)/map/index.tsx (fichier modifié — rendu des markers :68-70, toggle viewMode :68/71, branche erreur de chargement :42, empty hint :107-111, Snackbar :113-115)
    - 02-PATTERNS.md §`app/(app)/map/index.tsx` (staggered mount : STAGGER_MS=40, CAP_MS=320, MAX_STAGGERED~30, render slice ; haptics.error() à la branche erreur)
    - 02-RESEARCH.md Pattern 2 (useStaggeredVisible — sketch complet) + Pitfall 1 (pas d'animation intra-snapshot)
    - 02-UI-SPEC.md §Markers Animation d'apparition + §Motion (opacity stagger index×40ms cap 320ms ; jamais de carte sans pins quand la preview est ouverte)
  </read_first>
  <action>
    Ajouter un hook local `useStaggeredVisible(count)` (sketch RESEARCH Pattern 2) : avec `STAGGER_MS=40, CAP_MS=320, MAX_STAGGERED=30`, programmer des `setTimeout` pour révéler progressivement les `Math.min(count, MAX_STAGGERED)` premiers markers (delay `min(i*STAGGER_MS, CAP_MS)`), puis monter le reste d'un coup à `CAP_MS` ; nettoyer les timers au cleanup. Rendre `points.slice(0, visible).map(...)` au lieu de tout rendre d'un coup (:68-70). La cascade est obtenue par le MONTAGE échelonné (le pin « pop in » en étant monté), PAS par une valeur d'opacité animée à l'intérieur du snapshot (Pitfall 1). NE JAMAIS laisser la carte sans pins pendant que la preview Modal est ouverte (UI-SPEC) — la sélection courante doit rester montée. Conserver l'exclusivité du toggle (:68/71) : en mode heatmap, AUCUN marker rendu. Ajouter `haptics.error()` dans la branche d'échec de chargement (:42, `if (!ok)`).
  </action>
  <acceptance_criteria>
    - `rg "useStaggeredVisible|STAGGER_MS|MAX_STAGGERED" app/\(app\)/map/index.tsx` matche (hook + constantes)
    - `rg "\.slice\(0," app/\(app\)/map/index.tsx` matche (rendu partiel staggered)
    - `rg "haptics\.error" app/\(app\)/map/index.tsx` matche dans la branche erreur de chargement
    - Le rendu markers reste sous condition viewMode==='pins' (exclusivité toggle préservée)
    - Aucun `Animated`/opacity tween ajouté à l'intérieur de PointMarker via ce fichier
    - `npx tsc --noEmit` : 0 nouvelle erreur attribuable à map/index.tsx
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Hook useStaggeredVisible + rendu slice (cascade par montage) + haptics.error() à l'erreur de chargement ; exclusivité toggle conservée ; tsc sans nouvelle erreur.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: FAB squircle reanimated + haptics.press() dans map/index.tsx</name>
  <files>app/(app)/map/index.tsx</files>
  <read_first>
    - app/(app)/map/index.tsx (fichier modifié — FAB :96-104, styles fab :144-158, handleFabPress :51-53, gate viewingFriendId :96)
    - 02-PATTERNS.md §`app/(app)/map/index.tsx` (rework FAB — dims/glow/position) + note reanimated (PressableScale = analog structurel legacy ; reanimated mandaté)
    - 02-RESEARCH.md Pattern 3 (FAB squircle + reanimated withSpring + haptics — sketch complet) + §Architectural Responsibility Map (reanimated OK sur le FAB, vraie View RN)
    - 02-UI-SPEC.md §FAB (table propriétés verrouillées) + §Motion (scale 1.0→0.92→1.0, damping ~14 stiffness ~220)
    - constants/theme.ts (T.fab posé par 02-01) + components/icons.tsx (IcoPlus :46) + lib/haptics.ts (press)
  </read_first>
  <action>
    Refondre le FAB (UI-SPEC §FAB, valeurs verrouillées) : 56×56, `borderRadius: T.fab` (18), `borderCurve:'continuous'` (INLINE — discipline D-12), fond `T.primary`, `IcoPlus size={24} color={T.text}`, glow `shadowColor:T.primary, shadowOffset:{0,6}, shadowOpacity:0.35, shadowRadius:16, elevation:8`. Position INCHANGÉE (`right:20, bottom: insets.bottom + 80`) et visibilité INCHANGÉE (masqué sous `viewingFriendId`, :96). Micro-anim (D-11) via reanimated v4 (PAS legacy Animated) : `useSharedValue(1)` + `useAnimatedStyle` sur `transform:[{scale}]`, `onPressIn → withSpring(0.92)`, `onPressOut → withSpring(1, {damping:14, stiffness:220})` — n'animer que `transform: scale` (compositor-friendly). Remplacer le `TouchableOpacity` par `Pressable` enveloppé dans `Animated.View`. Haptique (D-11) : `haptics.press()` dans `handleFabPress` (:51-53). `accessibilityLabel="Inscrire un moment"`. Référence canonique de la micro-anim : RESEARCH Pattern 3 (PressableScale n'est qu'un analog structurel legacy — ne pas copier son `Animated`).
  </action>
  <acceptance_criteria>
    - `rg "borderCurve" app/\(app\)/map/index.tsx` matche sur le style du FAB
    - `rg "T\.fab" app/\(app\)/map/index.tsx` matche (rayon squircle via token 02-01)
    - `rg "withSpring|useSharedValue|useAnimatedStyle" app/\(app\)/map/index.tsx` matche (reanimated, pas legacy Animated pour le FAB)
    - `rg "haptics\.press" app/\(app\)/map/index.tsx` matche dans handleFabPress
    - `rg "accessibilityLabel=\"Inscrire un moment\"" app/\(app\)/map/index.tsx` matche
    - `rg "borderRadius:\s*0" app/\(app\)/map/index.tsx` ne matche plus sur le FAB
    - `npx tsc --noEmit` : 0 nouvelle erreur attribuable à map/index.tsx
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>FAB squircle 56/T.fab + borderCurve continuous + glow + reanimated withSpring scale + haptics.press() + a11y label ; position/visibilité inchangées ; tsc sans nouvelle erreur.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (aucune nouvelle) | Rendu natif d'annotations + contrôle FAB. Les `points` viennent du hook existant (RLS). Pas d'entrée utilisateur non fiable nouvelle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Denial of Service | Montage staggered (timers) | mitigate | Plafonner le stagger (MAX_STAGGERED~30 puis montage groupé) et nettoyer les timers au cleanup → pas de fuite/queue longue sur cartes denses. |
| T-02-04 | Tampering | PointAnnotation.refresh() (re-snapshot) | accept | Rerendu visuel local sans donnée sensible ; comportement RLS/visibilité des points inchangé. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept (N/A) | Zéro paquet installé ; reanimated/gesture-handler/expo-haptics déjà vérifiés (Phase 1). |
</threat_model>

<verification>
- Statique : `npx tsc --noEmit` (0 nouvelle erreur) + assertions rg par task.
- Manuel device (gate de phase, sur device physique / TestFlight — Pitfall 5) :
  - Pins visibles à TOUS les niveaux de zoom (garde de régression STAB-02 — cette phase touche aux markers).
  - Tap sur un pin → agrandissement + halo rose immédiat (re-snapshot via refresh()).
  - Au chargement de la carte → cascade staggered ; la preview ne clignote jamais sans pins.
  - FAB squircle (pas carré, pas cercle Material) ; scale-on-press visible ; haptic medium au tap.
</verification>

<success_criteria>
- Pin raffiné + sélection agrandie+halo (refresh) + cascade au montage.
- Pins visibles à tous les zooms (pas de régression STAB-02).
- FAB squircle animé + haptics.press().
- tsc vert ; checklist device cochée au gate de phase.
</success_criteria>

<output>
Create `.planning/phases/02-carte-stylis-e/02-03-SUMMARY.md` when done
</output>
