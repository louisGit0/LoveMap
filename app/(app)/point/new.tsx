// TODO Phase 2 — Implémenté par Claude Code
// Formulaire de création d'un point : note, commentaire, durée, partenaire
import { View, Text } from 'react-native';

export default function NewPoint() {
  return (
    <View style={{ flex: 1, backgroundColor: '#0f0f0f', justifyContent: 'center', alignItems: 'center' }}>
      <Text style={{ color: '#fff' }}>Nouveau point — À implémenter</Text>
    </View>
  );
}
