// TODO Phase 7 — Implémenté par Claude Code
// Paramètres : mot de passe, suppression compte, visibilité
import { View, Text } from 'react-native';

export default function Settings() {
  return (
    <View style={{ flex: 1, backgroundColor: '#0f0f0f', justifyContent: 'center', alignItems: 'center' }}>
      <Text style={{ color: '#fff' }}>Paramètres — À implémenter</Text>
    </View>
  );
}
