// TODO Phase 3 — Implémenté par Claude Code
// Couche heatmap générée depuis les coordonnées des points visibles
import { View } from 'react-native';

interface Props {
  points: Array<{ latitude: number; longitude: number; weight?: number }>;
}

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function HeatmapLayer({ points }: Props) {
  // Placeholder — sera remplacé par Heatmap de react-native-maps ou lib compatible
  return <View />;
}
